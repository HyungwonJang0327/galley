# 2026-09-27

> BE13은 2026-09-26 밤 23:13에 시작해 자정을 넘겼다. B2e 마감(PR #141)은 worklog/2026-09-26.md.

## PR #141 마감 · BE13 타임라인 근거·검증 표시 — feat/timeline-evidence-verify

참여: 메인 에이전트(구현) · reviewer(리뷰, Fable 5.1 지정) · 사용자(결정 4건).

### PR #141 마감

- CI 첫 실행은 lockfile 버전 표기 불일치(설치 뒤 `^`를 붙이고 lockfile을 안 맞춤 — 메인 실수)로 실패 → `pnpm install --lockfile-only`를 chore 커밋에 fixup 후 재푸시, 4잡 초록(layout 잡 `next build`가 RSC react-markdown 렌더를 검증). 사용자 "ㅇㅇ" → rebase merge, main `2b8ab4b`. B2e 해시 정정은 이 브랜치 첫 커밋 `465baed`.
- 사용자 "응 진행해줘"(BE13). 착수 전 결정 질문 없이 루틴 판단으로 진행: 조각 미리보기 3줄 · 목록 텍스트는 unsupported>0일 때만 · 홈 "지금 할 일"은 AH5 미구현이라 범위 밖 · 목록 행마다 파일 읽기(성공한 검증 단계가 있는 Run만).

### 구현 (커밋 4)

- `352440d` pipeline `listRuns`의 `RunListStep`에 `sourceRunId`(DB 테스트) — 목록 행이 carried 검증 단계의 출처 Run 파일을 찾는다.
- `52df72a` `lib/run-artifacts.ts` 확장: 근거 수집 → `EvidenceView`(연결·탐색·읽지 못함 수, 항목: 경로·7자 해시·날짜·출처·note·조각 3줄·hasMore·치환·잘림), 근거 검증 → `parseVerificationReport`(형식 검사) → `VerificationView`(counts, 주장을 unsupported → uncertain → supported·줄 순, 클린룸 자리 수, judge 상태). `getRunVerificationFlags(rows)` → Run id → 수. `StepArtifactView`에 `evidence`·`verification`.
- `e82fc30` `RunTimeline` 근거 수집 meta + 근거 검증 trailing `Badge`(unsupported ≥ 1이면 warning) + `RunEvidencePanels.tsx`(EvidencePanel·VerificationPanel, 한국어 라벨, CSS 모듈).
- `a857963` 목록 행: `getRunVerificationFlags(lists)` → 상태 배지 옆 `근거 없음 n`(주황, `evidenceFlagText`). dev 서버에서 6줄 펼침 확인(DATA_DIR 없음 → 안내 문구).

### 리뷰 — high 0 · med 4 · low 9 → 사용자 결정 4건, 나머지 추천대로

- **M1(사용자 결정: 현행 유지 + 기록)** 목록 행마다 `verification.json` 읽기에 상한 없음(목록 limit 없음, 폴링마다 재읽기, 선택 Run은 상세와 중복). 홈 "지금 할 일"(AH5)을 만들 때 검증 결과 수를 RunStep에 기록해 DB만 읽는 안을 같이 결정.
- `8e2b57c` **M2** 형식 검사를 화면이 쓰는 필드(kind·reason·judge.status·evidenceRef 형태·note)까지 — 깨진 파일은 예외 문구가 아니라 "형식이 맞지 않음" · **M3(사용자 결정: 표시)** 주장 줄에 판정 사유 한국어 라벨(근거에 없음·일반화된 표현·분석 요약에만·모델 판정·판정 상한 초과·판정 누락·출력 깨짐/잘림).
- `eb1658c` **L1(사용자 결정: 둘 다 한국어)** meta "linked n · discovered n" → "연결 n · 탐색 n"(layout.md·evidence-collection.md 문구 갱신) · **L8(사용자 결정: 숨김)** `filtered=false` 번들은 조각을 브라우저로 보내지 않고 안내만(식별 정보 필터 설명 요청에 답한 뒤 결정) · **L2** 끝 빈 줄·CRLF, 빈 조각은 `<pre>` 없음 · **L3** 가로 스크롤 `<pre>` tabIndex·aria-label · **L9** 테스트.
- `e120366` **L6** page 읽기 정리.
- **M4** 문서: CLAUDE.md 폴더 트리 "근거·검증 펼침은 BE13" → 구현됨. layout.md·evidence-collection.md "근거 링크" → **근거 위치(텍스트)** — 링크할 라우트가 없어 표기 변경(결정 변경 아님). 스펙에 없던 표시(읽지 못함 n·치환됨·잘림·필터 없음 안내) 한 줄 반영.
- 기록만: **L4** 검증 배지가 토글 버튼 밖(trailing)이라 버튼 이름에 수치 없음(li 안에서 이어 읽힘 — 의도) · **L5** 테스트가 Badge tone을 CSS 모듈 클래스명으로 판정 · **L7** 슬러그가 내용 파생이라 출처 Run 파일이 옛 슬러그 폴더에 있으면 missing(B3a M3 재승인 슬러그 결정과 같은 뿌리).

### 검증

- dashboard 316 · pipeline runQueries 17 · typecheck · lint 초록. 실제 번들·리포트 렌더는 단위 테스트(happy-dom)뿐(로컬 `.env`에 DATA_DIR 없음).

### 미결·다음

- PR "feat: 타임라인 근거·검증 결과 표시 (BE13)"(마이그레이션 없음) → CI → 승인 → rebase merge → 해시 정정.
- 다음(합의 순서): M3·M4(B3a) → BE12 → BM5~9.
