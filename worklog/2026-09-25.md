# 2026-09-25

> PR #133 마감·BX3 착수 기록은 worklog/2026-09-24.md 끝 절에 있다.

## BX3 글쓰기 단계 시리즈 처리 — feat/series-writing

참여: 메인 에이전트(구현) · reviewer(리뷰) · 사용자(제목 표기·리뷰 결정 4건).

### 결정 — 발행 제목 표기

- 메인이 선택지 제시(공백 N편 / 괄호 (N편) / 시리즈명 N편, Zenn 붙임 여부) → 사용자: 벨로그 `<제목> | <시리즈명> N편`, Zenn `<제목> (第N回)`. decisions/series.md의 ⚠️ 항목을 확정으로.

### 구현 (커밋 4 + 리뷰 수정 4 — 해시는 main 기준, PR #134 rebase 후 정정)

- `ff54b08` `steps/series.ts`(순수): `toSeriesStepInfo`, 제목 접미사·인용 줄·다음 편·Zenn 제목·はじめに 문장·SYSTEM 서술 지시, `stripSeriesLines`. `StepContext.series`.
- `4468990` `WorkerDeps.series` + `runOnce` + `createSeriesSource`(DB seriesKey → 읽기 전용 컨텍스트) + `bin/worker.ts` BLOG_DIR 배선.
- `566e44b` 벨로그: SYSTEM 한 줄, 출력 뒤 표기, `(기존 글)` 차단.
- `aeb8ff4` 링크드인·Zenn: 입력에서 표기 떼기, Zenn 제목 `(第N回)`·はじめに 문장.

### 판단

- 워커는 아직 Mock 단계 러너(BS5 전) — 이번 배선은 `StepContext.series`를 채우는 통로까지. 실제 산출물 적용은 BS5 뒤.
- 렌더 함수는 계획의 `writing.ts` 대신 새 파일 `steps/series.ts`(순수 함수 묶음, 테스트 분리).

### 리뷰 — high 0 · med 7 · low 10 → 사용자 결정

- **M5·M7** `(기존 글)` 차단이 velog에만(재실행이 뒤 단계부터면 안 걸림, 근거 수집 비용은 나감), 조회가 시도마다·글쓰기 단계만 → 선택지(단계 시작 1회+전 단계 차단 / 글쓰기 단계만 / Run 스냅샷) → 사용자 "단계 시작마다 1회 + 전 단계 차단" `741cd49`.
- **M6** DB엔 시리즈 편인데 파일 편 줄과 짝이 안 맞으면 조용히 "시리즈 아님" → 사용자 "실패시킨다"(`SERIES_EPISODE_NOT_MATCHED`) · **L7** BLOG_DIR 없으면 → "시리즈 편만 실패"(`SERIES_SOURCE_UNCONFIGURED`) · **L6** 실패 분류(DB 일시 오류는 `SERIES_LOOKUP_FAILED` 재시도 가능) `6f02eef`.
- **L8** 이전 편 제목·시리즈명 redact → 사용자 "지금은 기록만" → todo TD8. 링크 텍스트 대괄호 이스케이프는 반영.
- 나머지 추천대로: **M1·L2** 떼기가 넓어 시리즈 아닌 `# React | Vue 비교 2편`이 잘리고 인용문·`다음 편:` 문장이 지워짐 → 이 시리즈 이름·위치만, 이름을 정규식에 안 넣음 · **M3** apply가 멱등이 아님(모델이 쓴 표기와 겹침) → 떼고 붙이기 · **M2** 펜스 안 빈 줄까지 접음 · **L1** 펜스 닫힘 CommonMark · **L4** `## 5. 결과` `39270cd` · **M4** Zenn 블록(`:::message`·링크 카드 등)에 문장이 붙음 → 확실한 문단만 · **L5** 第N回 중복 `6f79ae3`.
- 기록만: **L3** H1 없는 출력은 제목 접미사가 빠짐 · **L9** promptHash는 어투 파일 해시 의미 유지(시리즈 줄 미반영) · **L10** evidence-collection.md에 서술 지시 한 줄 예외 명시.
- 작업 중: 디스크가 가득 차(ENOSPC) 명령이 전부 실패 → 사용자가 공간 확보 후 재개(잃은 작업 없음) · velog 시리즈 테스트가 번들 없는 runId를 써 실패 → `sources.evidence`로.

### 검증

- pipeline **589**(+41) · 스모크 4 · ui 329 · dashboard 249 · lint · typecheck · format:check · Node 직접 import.

### 미결·다음

- ~~PR → CI → 사용자 승인 뒤 rebase merge → 해시 정정~~(PR #134 CI 4잡 초록, 사용자 "일단 머지까지만"으로 rebase merge — main `cb102b7`, 해시 정정은 BX4 첫 커밋) → **BX4**(발행정보 `## 벨로그 시리즈` 절·`completeTopic`·체크리스트). 마이그레이션 없음.
- 스케줄 프롬프트 1-1절에 같은 제목 표기 반영(사용자, Galley 밖) — 두 실행 경로가 같은 결과가 되려면 필요.

## PR #134 마감 · BX4 착수

- PR #134 머지 후 멈춤 → 사용자 "다음거하자"로 BX4 착수(브랜치 feat/series-publish-info). BX3 해시 main 기준 정정.

## BX4 발행정보·완료 이동 — feat/series-publish-info

참여: 메인 에이전트(구현) · reviewer(리뷰, Fable 5.1 지정) · 사용자(결정 3건).

### 구현 (커밋 4 + 리뷰 수정 2 — 해시는 브랜치 기준, rebase 후 정정)

- `a188ce9` docs: PR #134 마감(BX3 해시 main 기준 정정), BX4 착수.
- `ad6a4c4` `queue/completeQueue.ts`(순수): `completeTopic` — 완료 섹션 맨 끝(날짜 오름차순, 실제 파일 모양)에 `- YYYY-MM-DD [A-1] <글 제목> (<메모>, posts/<슬러그>)`, 태그 유지·카테고리 벗음, 후보에서 빠지면 정의 줄 위치 당김(moveTopic과 같은 규칙). 실패는 `TOPIC_MISMATCH`·`INVALID_COMPLETION`.
- `713b43d` `steps/series.ts` `renderSeriesPublishSection` — `## 벨로그 시리즈` `시리즈명 · N/M편`, 시리즈 아니면 빈 문자열.
- `7147a87` `steps/publishChecklist.ts` `seriesChecklist` — id `velog-series-add`·`velog-previous-link`(이전 편 URL 없을 때만), 문구는 대시보드. 배럴 export.

### 세션 중 일

- 세션 시작에 ~/Desktop TCC 권한 복구 확인(읽기·쓰기 정상). 첫 reviewer 호출은 사용자 "잠깐만 스탑"으로 중단 → 모델을 Fable 5.1로 되돌린 뒤(사용자 "원래 이 프로젝트 fable5.1로 하던 거라서") reviewer도 Fable 지정으로 재실행.
- 사용자 질문: 큐 화면에 `[D-N]`이 안 보이는데 시리즈 반영이 안 된 것 아닌가 → 의도된 상태. BX1부터 태그는 표시 제목에 안 들어가고(decisions/series.md), 화면 배지·그룹 헤더는 BX5 몫(todo BX1 이월 메모 그대로). 사용자 "기존 진행대로".

### 리뷰 — high 1 · med 3 · low 7 → 사용자 "추천대로"

- **H1** 제목·메모의 개행이 섹션 구조를 깸(`articleTitle: '제목\n## 대기\n- …'`이면 두 번째 대기 절 생성) → 거부 `947e65d`.
- **M1** 메모 괄호가 완료 줄 괄호·힌트 파서를 어긋나게 함(슬러그는 막고 메모는 안 막았음) → 메모 `[()（）]` 거부. 글 제목의 괄호는 파일 형식상 힌트로 읽히는 한계 — docstring에 명시만 `947e65d`.
- **M2** 완료 줄 제목이 큐 제목과 달라지면 매칭 키가 갈라져 재적재 때 완료 줄이 새 행·원래 행은 사라진 줄(Run 이력·seriesKey 안 붙음). completeTopic 버그가 아니라 결정 형식의 귀결 → 사용자 (b) 머리 주석에 제약만 적고 **B3a 설계에서 처리**(호출부가 파일 쓰기 전 QueueItem title·status·completedOn 갱신이 후보). 유령 행 정리 보류 이슈와 맞닿음.
- **M3** 테스트 누락 → 보류 출발·후보 마지막 편·빈 완료 절·index 범위 밖·실제 모양 픽스처에서 완료 이동 뒤 나머지 바이트 동일 `947e65d`.
- **L1** 제목 맨 앞 `[B-3] `·`(기존 글)`은 재파싱에서 뜻이 바뀜 → 거부 `947e65d`. **L3** `params: Record<string,string>` → id별 판별 유니온(`PublishChecklistId`는 파생) `d427af7`. **L5** `(기존 글)` 이전 편 케이스 정상 — 이름 붙인 테스트 추가 `d427af7`.
- 기록만: **L2** 날짜는 형식만 검사(`2026-13-45` 통과 — 호출부가 clock에서 만들면 문제 없음) · **L4** 편 번호 비연속(1·2·5)이면 `5/3편` — decisions "M = 파일에 있는 편 수"와 일치, 사용자 **유지** · **L6** 같은 슬러그 완료 줄 중복 검사 없음(호출부 몫) · **L7** decisions/series.md 완료 줄 형식에 `<메모>` 항 누락(코드·픽스처가 맞음) → 이번 /log에서 반영.
- 문제 없음: 완료 절 순서(맨 끝 = 날짜 오름차순, 백데이트 입력은 정렬하지 않음 — 줄 순서 보존 원칙) · 정의 줄 위치 규칙 moveTopic과 동일 · 배럴 export 3묶음 순수 함수 + `import type`뿐이라 server-only 경계 무해 · `.ts` 확장자·enum 없음, Node 직접 import 통과 · 커밋 단위·scope·에러 값 반환.

### 검증

- pipeline **607**(+8) · ui 329 · dashboard 249 · lint · typecheck · Node 직접 import(completeQueue·publishChecklist).

### 미결·다음

- PR → CI → 사용자 승인 뒤 rebase merge → 해시 정정(다음 브랜치 첫 커밋) → **BX5**(큐 화면 배지·그룹 헤더·실행 상세 편 정보). 마이그레이션 없음.
- B3a 설계로 이월: completeTopic 호출 순서(M2), 완료 줄 중복(L6), 날짜 유효성(L2).
