# 2026-09-25

> PR #133 마감·BX3 착수 기록은 worklog/2026-09-24.md 끝 절에 있다.

## BX3 글쓰기 단계 시리즈 처리 — feat/series-writing

참여: 메인 에이전트(구현) · reviewer(리뷰) · 사용자(제목 표기·리뷰 결정 4건).

### 결정 — 발행 제목 표기

- 메인이 선택지 제시(공백 N편 / 괄호 (N편) / 시리즈명 N편, Zenn 붙임 여부) → 사용자: 벨로그 `<제목> | <시리즈명> N편`, Zenn `<제목> (第N回)`. decisions/series.md의 ⚠️ 항목을 확정으로.

### 구현 (커밋 4 + 리뷰 수정 4 — 해시는 main 기준, PR #134 rebase 후 정정)

- `ff54b08` `steps/series.ts`(순수): `toSeriesStepInfo`, 제목 접미사·인용 줄·다음 편·Zenn 제목·はじめに 문장·SYSTEM 서술 지시, `stripSeriesLines`. `StepContext.series`.
- `4468990` `WorkerDeps.series` + `runOnce` + `createSeriesSource`(DB seriesKey → 읽기 전용 컨텍스트) + `bin/worker.ts` BLOG_DIR 배선.
- `566e44b` 벨로그: SYSTEM 한 줄, 출력 뒤 표기, `(기존 글)` 차단.
- `aeb8ff4` 링크드인·Zenn: 입력에서 표기 떼기, Zenn 제목 `(第N回)`·はじめに 문장.

### 판단

- 워커는 아직 Mock 단계 러너(BS5 전) — 이번 배선은 `StepContext.series`를 채우는 통로까지. 실제 산출물 적용은 BS5 뒤.
- 렌더 함수는 계획의 `writing.ts` 대신 새 파일 `steps/series.ts`(순수 함수 묶음, 테스트 분리).

### 리뷰 — high 0 · med 7 · low 10 → 사용자 결정

- **M5·M7** `(기존 글)` 차단이 velog에만(재실행이 뒤 단계부터면 안 걸림, 근거 수집 비용은 나감), 조회가 시도마다·글쓰기 단계만 → 선택지(단계 시작 1회+전 단계 차단 / 글쓰기 단계만 / Run 스냅샷) → 사용자 "단계 시작마다 1회 + 전 단계 차단" `741cd49`.
- **M6** DB엔 시리즈 편인데 파일 편 줄과 짝이 안 맞으면 조용히 "시리즈 아님" → 사용자 "실패시킨다"(`SERIES_EPISODE_NOT_MATCHED`) · **L7** BLOG_DIR 없으면 → "시리즈 편만 실패"(`SERIES_SOURCE_UNCONFIGURED`) · **L6** 실패 분류(DB 일시 오류는 `SERIES_LOOKUP_FAILED` 재시도 가능) `6f02eef`.
- **L8** 이전 편 제목·시리즈명 redact → 사용자 "지금은 기록만" → todo TD8. 링크 텍스트 대괄호 이스케이프는 반영.
- 나머지 추천대로: **M1·L2** 떼기가 넓어 시리즈 아닌 `# React | Vue 비교 2편`이 잘리고 인용문·`다음 편:` 문장이 지워짐 → 이 시리즈 이름·위치만, 이름을 정규식에 안 넣음 · **M3** apply가 멱등이 아님(모델이 쓴 표기와 겹침) → 떼고 붙이기 · **M2** 펜스 안 빈 줄까지 접음 · **L1** 펜스 닫힘 CommonMark · **L4** `## 5. 결과` `39270cd` · **M4** Zenn 블록(`:::message`·링크 카드 등)에 문장이 붙음 → 확실한 문단만 · **L5** 第N回 중복 `6f79ae3`.
- 기록만: **L3** H1 없는 출력은 제목 접미사가 빠짐 · **L9** promptHash는 어투 파일 해시 의미 유지(시리즈 줄 미반영) · **L10** evidence-collection.md에 서술 지시 한 줄 예외 명시.
- 작업 중: 디스크가 가득 차(ENOSPC) 명령이 전부 실패 → 사용자가 공간 확보 후 재개(잃은 작업 없음) · velog 시리즈 테스트가 번들 없는 runId를 써 실패 → `sources.evidence`로.

### 검증

- pipeline **589**(+41) · 스모크 4 · ui 329 · dashboard 249 · lint · typecheck · format:check · Node 직접 import.

### 미결·다음

- ~~PR → CI → 사용자 승인 뒤 rebase merge → 해시 정정~~(PR #134 CI 4잡 초록, 사용자 "일단 머지까지만"으로 rebase merge — main `cb102b7`, 해시 정정은 BX4 첫 커밋) → **BX4**(발행정보 `## 벨로그 시리즈` 절·`completeTopic`·체크리스트). 마이그레이션 없음.
- 스케줄 프롬프트 1-1절에 같은 제목 표기 반영(사용자, Galley 밖) — 두 실행 경로가 같은 결과가 되려면 필요.

## PR #134 마감 · BX4 착수

- PR #134 머지 후 멈춤 → 사용자 "다음거하자"로 BX4 착수(브랜치 feat/series-publish-info). BX3 해시 main 기준 정정.
