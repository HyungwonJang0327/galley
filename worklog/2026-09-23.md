# 2026-09-23

## BS2 벨로그 본문 단계 — feat/velog-step (2026-09-22 착수, 09-23 마감)

참여: 메인 에이전트(구현) · reviewer(리뷰) · 사용자(어투 파일 제공, 결정 "추천대로" — ArtifactStore·`truncated` 🔒 인터페이스 변경 승인).

### 구현 (커밋 4 + 리뷰 수정 5 + docs 2 — 해시는 main 기준, PR #127 rebase 후 정정)

- `1a36bf2` 어투 프롬프트 실제 내용(사용자 파일 §0 공통 + 채널 블록, §4 셀프 체크는 README).
- `9468e1a` 번들 `analyses`(옵션) — 근거 수집이 항목이 담긴 글의 제목·요약만 담는다(BE8 ⑨ 이행).
- `8d833b5` StepContext `modelId`·`sources`(carried 단계의 출처 runId, 워커는 옮기기만) — `evidence?: unknown` 자리 제거.
- `96febfe` `createVelogStepRunner`: `VelogInput { topic, bundle, tone, instruction? }`(리포 경로·분석 글 원문 자리 없음), 번들은 `sources.evidence ?? runId`로 EvidenceStore에서, 어투는 system, 근거 0개는 `VELOG_EVIDENCE_EMPTY`(재시도 불가), `classifyModelError`·`tonePromptFailure`(BS3·BS4 재사용), `WRITING_LIMITS`.
- `e8c8cd9`·`38d2f79` decisions BS2 7건 + 리뷰 반영 7건 · `ebf1ee5` worklog·todo.

### 리뷰 — high·med·low → 사용자 "추천대로"

- **1** 워커가 artifacts를 버려 실모델로 돌리면 본문이 사라짐 → `ArtifactStore`(DATA_DIR `artifacts/<슬러그>/<runId>/<파일>`)를 단계가 직접 쓴다, posts 복사는 B3a `c288c95`.
- **2** 제목의 괄호 힌트(리포 별칭)가 프롬프트에 감 → `stripTopicHints` `bd7b7dd` + 적용 `eab76d8`.
- **5(🔒)** 출력 절단 미감지 → `GenerateResult.truncated`(Anthropic/OpenAI/Mock/scripted) `9f3d35e` + `VELOG_OUTPUT_TRUNCATED`.
- **4·6·8·10·11·13** 동적 펜스 · 호출을 signal과 경주(`abortable`) · SYSTEM 머리 정리(사실은 근거만, 식별자는 어투대로 일반화) · 테스트(절대경로·env 미포함, 펜스 충돌, 호출 중 abort, 실제 SDK 오류 클래스, 잘림, 깨진 번들) · `ECONNREFUSED`·거부 판정 · 전체 펜스 벗기기 `eab76d8`.
- 기록만: **7** SDK×워커 재시도 곱(BS5) · **9** 클린룸 경계는 어투+검수, 기계 검사는 BE10 · **12·14~23** 사소·비용 추정·실모델 스모크 방법(BS5 뒤).
- 작업 중: 병렬 호출 cwd 리셋으로 프롬프트 파일이 `packages/pipeline/.galley`에 생겨 루트로 이동 · 편집 스크립트가 prettier 정렬에 두 번 빗나가 파일을 통째로 다시 씀 · 테스트 체인을 `;`로 이어 깨진 상태가 커밋돼 amend로 정정.

### 검증

- pipeline **461**(+12) · 스모크 4 · ui 329 · dashboard 249 · lint · typecheck · format:check.

### 미결·다음

- ~~PR → CI → 사용자 승인 뒤 rebase merge~~(아래 PR #127 마감) → **BS3** 링크드인(입력 = ArtifactStore의 velog.md, `sources.velog ?? runId`) → **BS4** Zenn → **BE10** 검증(BE10 기록 2건) → **BS5** 라우팅(DATA_DIR·PROMPTS_DIR 검증, `maxRetries`·재시도 대기, 실모델 스모크).

## PR #127 마감 · BS3 착수

- PR #127(BS2) CI 통과 → 사용자 지시로 rebase merge(main `ebf1ee5`). 위 BS2 절과 todo BS2의 해시 정정(이 브랜치 feat/linkedin-step 첫 커밋).
- 사용자 "BS3 진행해" → **BS3 링크드인 단계 착수 승인**.

## BS3 링크드인 요약 단계 — feat/linkedin-step

참여: 메인 에이전트(구현) · 사용자("BS3 진행해" — 착수 승인).

### 구현 (커밋 1 + 리뷰 수정 2 + docs 3 — 해시는 main 기준, PR #128 rebase 후 정정)

- `bc54e48` `createLinkedinStepRunner`(`src/steps/linkedinStep.ts`): `LinkedinInput { topic, body, tone, instruction? }` — 근거 번들·리포 경로·분석 글 자리 없음(본문에서만 파생). 본문은 `ArtifactStore.read(slug, sources.velog ?? runId, 'velog.md')`(BS2 ⑧ 규칙의 첫 소비자), 결과 `linkedin.md`는 이 Run 폴더에. 어투 `linkedin.md`는 system(고정 머리: 사실은 본문만·[확인 필요] 주석은 확정 사실 아님·식별자는 이미 일반화·새 코드 없음·대괄호 토큰 유지), 본문은 동적 펜스로 user에. BS2 헬퍼(`tonePromptFailure`·`classifyModelError`·`abortable`·`fenceFor`·`unwrapFence`)는 `velogStep.ts`에서 import. 실패 `LINKEDIN_MODEL_UNKNOWN`·`_UNAVAILABLE`·`_BODY_MISSING`·`_BODY_EMPTY`·`_OUTPUT_TRUNCATED`(재시도 불가)·`_MODEL_FAILED`(분류)·`_OUTPUT_EMPTY`(재시도)·`_STORE_WRITE_FAILED`. `WRITING_LIMITS.linkedinMaxOutputTokens` 2,048. 배럴 export.
- `e94aa95`(리뷰) 출력 상한 4,096 · SYSTEM 머리 정정 · `f8c3d65`(리뷰) 테스트 5건 보강.
- `977b684` decisions/evidence-collection.md — "링크드인" 단계 줄 신설 + 갱신 이력 BS3 5건(입력은 본문뿐 · sources.velog · 헬퍼 재사용 자리 · 링크 자리는 어투가 정하고 코드는 검사 안 함 · `LINKEDIN_*` 접두).

### 판단

- 벨로그 링크 자리 `[벨로그 링크]`를 코드가 검사하지 않는다 — 어투 파일이 정하는 형식이라 파일을 고치면 코드도 고쳐야 하는 결합을 피했다(BS1 ⑧ "링크 자리는 어투에" 연장).
- 본문 입력 상한은 두지 않았다 — velog 출력이 8,192 토큰으로 이미 묶여 있다.
- 공용 헬퍼를 별 파일로 옮기지 않았다 — BS4 Zenn까지 소비자 둘, 셋째가 생기면 결정.

### 리뷰 — high 0 · med 4 · low 10 → 사용자 "추천대로, L1은 검수가 잡으니 상관없다"

- **M1** 출력 상한 2,048이 한국어 1,300자에 빠듯(최대 1,900 토큰) → 4,096(절단 상한이라 비용 무관) `e94aa95`.
- **M2** SYSTEM 머리 "식별자는 이미 일반화됐으니 그대로"가 어투 공통 규칙과 반대 → "이미 일반화된 것은 그대로, 남은 회사 고유 이름은 어투대로" — 근거 검증 flag는 게이트가 아니라 링크드인이 클린룸 2차 방어선 `e94aa95`. decisions 문구도 정정.
- **M3·M4·L4·L5·L6** `LINKEDIN_BODY_UNREADABLE`(EISDIR 픽스처)·`STORE_WRITE_FAILED`(cause·문구에 경로 없음)·401 영구·러너 수준 `ctx.instruction`·HOME 미포함 테스트 `f8c3d65`.
- **L2** `[벨로그 링크]`를 "본문의 치환 표기" 예시에 넣은 문장 분리 `e94aa95` · **L3** todo 완료조건 옛 코드명 `MISSING_INPUT` 정정.
- **L1(사용자 결정: 현행 유지)** `[확인 필요]` HTML 주석이 링크드인에선 노출 텍스트 — 업로드 전 사람이 검수하므로 어투 파일 안 고침.
- 기록만: **L7** 러너 골격 90% 중복 + 범용 헬퍼가 `velogStep.ts`에 → BS4 첫 커밋에서 `steps/writing.ts`로 이동 · **L8** 배럴 export는 그때 · **L9** 기능 커밋 한 덩어리(다음부터 limits·배럴 분리) · **L10** UNREADABLE 영구 처리 현행 유지 · BS2에도 UNREADABLE·저장 실패 미테스트.

### 검증

- 리뷰 뒤 pipeline **467**(+6) · lint · typecheck · format:check.
- 리뷰 전 pipeline **466**(+5, `linkedinStep.test.ts`: 프롬프트 구성·정상 경로·carried 출처·실패 8종·호출 중 abort·펜스 벗기기) · 스모크 4 · lint · typecheck · format:check · Node 직접 import 확인(`.ts` 확장자).

### 미결·다음

- ~~reviewer 리뷰 → PR → CI → 사용자 승인 뒤 rebase merge~~(아래 PR #128 마감) → **BS4** Zenn(첫 커밋 헬퍼 이동, 같은 골격: 본문 + 어투, frontmatter `published: false` 고정) → **BE10** 검증 → **BS5** 라우팅.

## PR #128 마감 · BS4 착수

- PR #128(BS3) CI 4잡 통과 → 사용자 지시로 rebase merge(main `7cbd1fd`). 위 BS3 절과 todo BS3의 해시 정정(이 브랜치 feat/zenn-step 첫 커밋). 마이그레이션 없음.
- 사용자 "ㅇㅇ" → **BS4 Zenn 일본어판 단계 착수 승인**(첫 커밋 = 공용 헬퍼 이동).

## BS4 Zenn 일본어판 단계 — feat/zenn-step

참여: 메인 에이전트(구현) · 사용자("ㅇㅇ" — 착수 승인).

### 구현 (커밋 5 + 리뷰 수정 2 + docs 3 — 해시는 main 기준, PR #129 rebase 후 정정)

- `ceb4782` refactor: 공용 헬퍼 5개를 `steps/writing.ts`로 이동(동작 변화 없음, BS3 리뷰 L7·L8). 순수 함수 테스트는 `writing.test.ts`로, 러너와 엮인 것(펜스 충돌·호출 중 abort)은 단계 테스트에. 배럴은 `abortable`·`fenceFor`·`unwrapFence`까지 export.
- `aaac9dd` `WRITING_LIMITS.zennMaxOutputTokens` 12,288(2,500~4,000자 + 벨로그판 코드).
- `8afa3ed` `createZennStepRunner`(`src/steps/zennStep.ts`): 링크드인과 같은 골격(본문 `sources.velog ?? runId` + 어투 `zenn.md` + 지시). **frontmatter는 코드가 만든다** — `ZENN_FRONTMATTER`(`published: false` 고정·emoji 📝·type tech·topics 빈 배열), 모델이 낸 `---` 블록·전체 펜스는 `stripFrontmatter`·`unwrapFence`로 버림. 제목은 첫 H1을 `splitTitle`로 떼어 `title`에(없으면 힌트 뗀 주제 제목), `renderZennArticle`이 JSON 문자열로 직렬화(따옴표·콜론 안전). 실패 `ZENN_*`(링크드인 갈래 + 제목만은 `ZENN_OUTPUT_EMPTY` 재시도).
- `82597e5` 배럴 export · `dffbe92` decisions "Zenn 일본어판" 줄 + BS4 이력 5건.
- `0c9fb5d`(리뷰) frontmatter 반복 제거·빈 블록·구분선 보존, 주석 건너뛴 H1·닫는 `#`·CRLF 통일·`ZENN_TITLE_MISSING`, 내부 헬퍼 배럴 제외 · `13831b3`(리뷰) 테스트 9건 보강.
- 작업 중: `splitTitle` 첫 정규식이 게으른 `.+?`에 `\n?`가 붙어 한 글자만 잡음 → `[^\r\n]*?` + `(?:\r?\n|$)`로 정정(테스트가 잡음) · 배럴 커밋이 `grep Done` 체인에 막혀 한 번 빠짐(filter 실행은 "Done" 줄을 안 찍는다).

### 판단

- 러너 골격 팩토리는 만들지 않았다 — 세 단계의 입력(번들/본문)·후처리(frontmatter)가 달라 옵션 뭉치가 되고, 남는 중복은 60줄 안팎.
- `topics`는 비운다 — 힌트 키워드는 리포 별칭이라 공개 태그로 부적합. 검수 때 사람이.
- H1은 frontmatter로 옮기고 본문에서 뺀다 — Zenn이 제목을 frontmatter에서 그린다.

### 리뷰 — high 0 · med 2 · low 9 → 사용자 "추천대로"

- **M1** `splitTitle`이 맨 앞 줄만 봐서 첫 줄이 `[挿絵]` 주석·들여쓰기면 한국어 제목 + 일본어 H1 중복 → 앞쪽 빈 줄·HTML 주석 건너뛰고 첫 H1 `0c9fb5d`.
- **M2** `stripFrontmatter`가 한 블록만 버려 두 번 온 블록·H1 뒤 블록·빈 블록의 `published: true`가 본문에 노출(Zenn은 오프셋 0만 파싱해 뒤집히진 않음) → 반복 제거 + 제목 뒤 한 번 더 + 빈 블록 `0c9fb5d`.
- **L1** 구분선으로 시작하는 본문의 첫 문단 무음 손실 → 블록 안 `key:` 줄 없으면 보존 · **L3** 닫는 ATX `#` · **L4** CRLF → LF 통일 · **L5** 빈 대비책 제목 → `ZENN_TITLE_MISSING`(재시도) · **L6** 범용 이름 헬퍼 배럴 제외 · **L8** `writing.ts` 주석의 없는 `SYSTEM_PREFIX` `0c9fb5d`.
- **L7** 테스트 9건(반복·빈·구분선 frontmatter, 주석·들여쓰기·CRLF 제목, 백슬래시·개행 직렬화, 빈 대비책, 거부 경로, `PROMPT_UNREADABLE`) `13831b3`.
- **L2** Zenn CLI 검증(제목 70자·topics 1개 이상) → decisions "Zenn 일본어판" 줄 + Phase 2 push 전 검사 todo · **L9** 링크 토큰은 어투 파일과 SYSTEM 머리가 짝 → prompts README.
- 기록만: 리팩토링 글자 단위 동일·순환 import 없음·공개 API 파손 없음 · 상한 12,288은 어댑터 기본 16,000 이내.

### 검증

- 리뷰 뒤 pipeline **479**(+1 순증: zenn 10 · writing 3) · lint · typecheck · format:check.
- 리뷰 전 pipeline **478**(+9 zenn · +2 writing 순증, 이동분 상쇄) · lint · typecheck · format:check · Node 직접 import.

### 미결·다음

- ~~reviewer 리뷰 → PR → CI → 사용자 승인 뒤 rebase merge~~(아래 PR #129 마감) → **BE10** 근거 검증 → **BS5** 라우팅(DATA_DIR·PROMPTS_DIR 검증, `maxRetries`·재시도 대기, 실모델 스모크에서 linkedin 4,096·zenn 12,288 확정).

## PR #129 마감 · BE10 착수

- PR #129(BS4) CI 4잡 통과 → 사용자 지시로 rebase merge(main `b4dd374`). 위 BS4 절과 todo BS4의 해시 정정(이 브랜치 feat/verify-step 첫 커밋). 마이그레이션 없음.
- 사용자 요청으로 남은 todo 37개를 묶어 보고(다음 최단 경로 BE10 → BS5 → B3a → B2e → BE13). 사용자 "BE10 진행해" → **BE10 근거 검증 단계 착수 승인**.

## BE10 근거 검증 단계 — feat/verify-step

참여: 메인 에이전트(구현) · 사용자("BE10 진행해" — 착수 승인).

### 구현 (커밋 6 + decisions 1, 해시는 브랜치 기준)

- `144be05` refactor: 근거 조각 프롬프트 렌더링을 `writing.renderEvidenceItems`로 추출(벨로그 본문·판정 프롬프트가 같은 표현).
- `1987d66` `VERIFY_LIMITS`(`src/evidence/verifyLimits.ts`): 서술 40개·8~300자·클린룸 3줄(8자 미만 줄 제외)·판정 출력 4,096.
- `3ef043f` `src/evidence/verification.ts`: `VerificationReport`(claims·counts·cleanRoom·judge·sources) + 순수 함수 — `splitBodyLines`(펜스 안팎), `extractMechanicalClaims`(인라인 백틱→경로/식별자, 경로·식별자·숫자 정규식, 목록·제목 번호·단위 없는 연도·한 자리 수·HTML 주석 제외), `matchMechanicalClaims`(숫자 supported/unsupported, 경로·식별자 supported/uncertain, 요약 일치는 `in-analysis-summary`), `extractStatements`(종결 어미 뒤에서만 문장 분리), `findVerbatimRuns`(위치·줄 수만), `countClaims`.
- `37b5c10` `createVerifyStepRunner`(`src/steps/verifyStep.ts`): 본문 `sources.velog`·번들 `sources.evidence` → 기계 대조 → 서술은 모델 JSON 판정(`buildJudgePrompt`·`parseJudgments`, 0개면 호출 없음) → `verification.json`(ArtifactStore) + `flags`. 판정 깨짐·잘림은 uncertain + `judge` 기록(실패 아님). 실패 `VERIFY_*`.
- `d93af0a` 배럴 · `f1a8316` decisions "근거 검증" 구현 줄 + BE10 이력 7건.
- 작업 중: 문장 분리 정규식이 `useInfiniteScroll.ts`의 마침표에서 끊김 → 종결 어미 뒤 공백에서만 나누도록 · 최소 길이 12자가 "결론은 관찰자였다."(10자)를 떨어뜨려 8자로 · 경로 대조 lookbehind가 `/` 뒤를 막아 `src/…/x.ts` 안의 `x.ts`를 못 찾음 → 정정 · vitest를 `--root /`로 돌려 디버그하려다 파일 시스템 전체를 스캔해 멈춤(TaskStop) → Node 직접 import 스크립트로 교체.

### 판단

- 경로·식별자 미발견은 uncertain, 숫자 미발견만 unsupported — 어투가 일반화를 강제하므로 기계는 "지어낸 것"과 "바꾼 것"을 구분할 수 없다.
- 판정 JSON 실패를 단계 실패로 두지 않았다 — 검증은 표시 조건이고, 큰 프롬프트를 3번 재시도하는 비용이 크다. 모델 호출 자체의 오류는 다른 단계처럼 실패.
- `flags`는 DB에 안 남긴다 — 워커 `StepOutcome`에 자리가 없고 보고서 파일이 진실. BE13이 파일을 읽는다.

### 리뷰 — high 2 · med 5 · low 12 → 09-24 사용자 "다음 진행하자"(추천대로)

- **H1** 판정 `reason`이 검열 없이 `verification.json`에 실림 — 모델이 근거 조각을 보고 사유를 쓰므로 코드 줄 인용이 posts로 새는 경로. 제안: 프롬프트 인용 금지 + 코드 방어(백틱 제거·조각 유의미 줄 포함 시 사유 폐기) + 테스트.
- **H2** `IDENT_LIKE` 지수 백트래킹 — 대문자 28연속+`(`에 2.4s, 40개면 시간 단위(워커 정지). 제안: 반복 그룹 `(?:[A-Z][a-z\d_$]*)+` + 회귀 테스트.
- **M1** 여러 줄 HTML 주석 속이 주장·서술로 뽑힘 · **M2** 날짜·시간·버전이 잘려 숫자(`2024-03-05`→`03`·`05`) · **M3** 조사 붙은 경로가 잘림(`page.tsx를`→`page`) · **M4** `verifiedAt` 전역 시계(clock 주입 관행) · **M5** `reason`에 코드·한국어·모델 문장 혼재(`not-judged (judged)` 혼동) → 기계 사유는 코드만, 모델 사유는 `note`.
- **L1~~L6·L9~~L12** 소수점 경계·콤마 비대칭·`Array.from` 경로 오분류·`getFoo(x)` 누락·4칸 코드 블록·단위 `월`·`VERIFY_EVIDENCE_UNREADABLE` 분리·범용 헬퍼 배럴 제외·테스트 9건·decisions 옛 줄 모순.
- 기록만: **L7** 제품명(`iOS`·`GitHub`)이 uncertain을 채움 · **L8** 서술 0개여도 키 없으면 실패(현행 유지 제안) · 리팩토링 글자 단위 동일 · 판정 프롬프트 누출 없음 · 숫자 대조는 값만 봐 `v20`에도 맞는 한계.
- 메인 에이전트 추천: H1·H2·M1부터 M5·L1부터 L6·L9부터 L12 수정, L7·L8 기록. 사용자 "오늘은 일단 여기까지"로 09-23 종료(브랜치 미푸시) → 09-24 "다음 진행하자" = 추천대로 반영.
- 반영(09-24): `649be04` verification.ts — H2 정규식 선형화(회귀 테스트 100ms) · M1 여러 줄 주석·L5 들여쓰기 코드·닫는 펜스 · M2 ISO 날짜 통째, 시간·버전·부호 뒤 숫자 제외, L6 `월` · M3 조사 붙은 경로·L3 경로 판정·L4 인자 있는 호출 · L1 소수점·L2 콤마 대칭 · M5 사유 코드(`ClaimReason`). `0397633` verifyStep.ts — H1 `sanitizeNote`(백틱 제거·120자·조각 줄/note 인용 시 폐기) + 프롬프트 인용 금지 · M5 `note` 분리(`judged`·`judge-missing`·`judge-unparsed`·`judge-truncated`) · M4 `clock` 주입 · L9 `VERIFY_EVIDENCE_UNREADABLE` · L10 배럴 정리 · L11 테스트(회귀·주석·날짜·경로·경계·여러 조각 verbatim·id 중복·retryable·verifiedAt·H1 인용) · L12 decisions :64 정정.
- 작업 중: 편집 스크립트가 prettier 줄바꿈에 앵커가 빗나가 두 번 헛돎 → 실제 형태를 grep으로 본 뒤 재적용 · prettier가 worklog의 `M1~M5`를 취소선(`~~`)으로 바꿔 놓아 "부터"로 고침.

### 검증

- 리뷰 반영 뒤 pipeline **502**(verification 13 · verifyStep 10) · lint · typecheck · format:check.
- 리뷰 전 pipeline **495**(+16: verification 7 · verifyStep 9) · lint · typecheck · format:check · Node 직접 import. 완료조건(숫자 supported 2 · unsupported 1) 테스트 있음.

### 미결·다음

- ~~reviewer 리뷰 → 리뷰 반영~~ → PR → CI → 사용자 승인 뒤 rebase merge → **BS5** 라우팅(evidence·velog·verify·linkedin·zenn 배선, DATA_DIR·PROMPTS_DIR 검증, `maxRetries`·재시도 대기, 실모델 스모크 — 상한 linkedin 4,096·zenn 12,288·서술 40개 확정) → B3a posts 쓰기 → B2e 미리보기 → BE13 타임라인.
