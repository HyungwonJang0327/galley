# 2026-09-26

> BX4 마감·BX5 착수는 2026-09-25 밤에 시작해 자정을 넘겼다. BX4 기록은 worklog/2026-09-25.md.

## PR #135 마감 · BX5 큐 화면 시리즈 표시 — feat/queue-series-ui

참여: 메인 에이전트(구현) · Explore(사전 조사) · reviewer(리뷰, Fable 5.1 지정) · 사용자(결정 4건).

### PR #135 마감

- CI 4잡 초록 → 사용자 "ㅇㅇ" → rebase merge, main `6018ba4`, 브랜치 삭제. 마이그레이션 없음. BX4 해시 main 기준 정정은 이 브랜치 첫 커밋 `37419d5`.

### 사전 조사(Explore)와 결정

- 조사 결과: `QueueEntry`에 id·seriesKey·episodeNo 없음, 시리즈명·편 수는 DB에 없음(파일에서), ui에 그룹 헤더 컴포넌트 없음, 큐 화면에 실행 버튼 없음(행 ⋮ "지금 실행"이 항상 비활성, `맨 위 실행`은 미구현), 실행 상세 헤더는 `runs/page.tsx` 인라인, `verify:layout`은 갤러리만 대상.
- 사용자 결정 ①: 그룹 헤더는 **앱 마크업**(`QueueGroupHeader` + CSS Module, 토큰만) — ui에 `ListGroupHeader`를 추가하면 갤러리·verify:layout까지 따라와 범위가 커짐(메인 추천, 사용자 "추천대로").
- 사용자 결정 ②: `(기존 글)` 실행 비활성은 **⋮ "지금 실행" 사유**("이미 발행된 글입니다.")로 — 실제 실행 버튼은 BS5 뒤에 생기고, 그때도 같은 판정(`QueueRowSeriesView.alreadyPublished`)을 쓴다.
- 사용자 질문(착수 전): 큐 화면에 `[D-N]`이 안 보이는데 시리즈 반영이 안 된 것 아닌가 → BX1부터 태그는 표시 제목에 안 들어가고(decisions/series.md) 화면 표시는 BX5 몫이라 의도된 상태라고 답함 → "기존 진행대로".

### 구현 (커밋 4 + 리뷰 수정 4 — 해시는 main 기준, PR #136 rebase 후 정정)

- `01586a2` pipeline: `QueueEntry.id`·`series{key, episode, alreadyPublished}`(두 컬럼 다 있을 때만 시리즈, `isAlreadyPublished(title)`), `queue/queueSeries.ts` `summarizeQueueSeries`(순수)·`loadQueueSeries`(정의 줄 순서로 이름·후보 위치·편 수·상태별 수), `steps/seriesSource.ts` `readTopicSeriesInfo`(실행 상세용, 값으로 끝남 — 시리즈 아님·정의 없음·편 불일치는 undefined). 대시보드 테스트 픽스처 4곳에 새 필드.
- `9905410` dashboard: `QueueRowTitle`(Badge `A-1` + Tooltip `시리즈명 · N/M편`, 정의 줄 없으면 배지만), `QueueGroupHeader`(후보 탭 정의 줄 자리, "편 없음 · 대기 N편"), `lib/queue-view` rows에 id·series + `items`(행+헤더 순서, `interleaveGroups` — position ≤ row.index 전에 삽입, 나머지는 맨 끝, 카테고리 필터는 그 소제목 정의만), `lib/queue-data.getQueueSeries`(실패는 빈 배열).
- `ba6c77c` dashboard: `queueRowMenuEntries(status, { alreadyPublished })` → 사유 교체, `QueueRowMenu alreadyPublished` prop, page·DraggableQueueRows 배선.
- `5035fc8` dashboard: `lib/run-series`(server-only, `runSeriesView` 순수 + `getRunSeries`), 실행 상세 헤더 아래 "○○ 시리즈 N/M편 · 이전 편 · 다음 편".
- 실제 확인: 사용자가 띄운 :3000 개발 서버(건드리지 않음)에서 HTML로 — 대기 `D-1`~`D-4` 배지, 후보 정의 7개 헤더, D "편 없음 · 대기 4편", 툴팁 `… · 1/5편`. 시리즈 편 실행이 아직 없어 실행 상세 줄은 단위 테스트로만.
- 세션 중: 새 dev 서버(:3123)를 띄우려다 "Another next dev server is already running"으로 실패 → 기존 :3000 사용.

### 리뷰 — high 0 · med 3 · low 8 → 사용자 "medium은 추천대로, low는 다시 보자" → low도 추천대로

- 문제 없음 확인: server-only 경계(QueueRowTitle은 ui 값 + queue-view 타입만, 클라 파일은 pipeline 타입만), Badge가 props·ref를 span에 넘겨 Tooltip 트리거 가능, DnD 키(`row.title` 문자열 유지), alreadyPublished 판정이 DB title(태그만 뗀 원문)에 맞음, interleaveGroups 규칙(같은 position 여러 정의·맨 끝·필터).
- **M1·M2** 키보드로 툴팁 못 열음(`<span>` 트리거) + generic span·p의 aria-label은 보조기기가 무시 → 배지 `role="img"` + 접근성 이름에 툴팁 내용 포함 + 툴팁 있을 때만 `tabIndex=0`, li·p의 aria-label 제거 `9803f65`. happy-dom은 포커스 불가 요소에도 focus 이벤트를 쏘므로 기존 테스트는 실제 동작을 보증하지 않았음.
- **M3** `catch {}`가 원인을 삼킴 → `console.error` 한 줄(값은 그대로 비움) `a939030`.
- **L2** 정의 줄만 남은 후보 탭이 "주제 없음"이 되어 명세의 안내가 안 보임 → `items.length` 기준 · **L3** 같은 키 정의 둘이면 React key 중복·툴팁 이름 어긋남 → 요약은 첫 정의만(`fc6a526`) + 헤더 key에 position · **L5** 맨 끝 헤더 밑줄 이중 → `.header:last-child` · **L6** `QueueGroupHeader` 렌더 테스트 · **L8** 실행 상세 헤더 묶음 여백을 `.detailHeaderGroup` 한 곳에 `6b83236`.
- 기록만(사용자에게 상세 설명 후 유지): **L1** `### 시리즈`가 마지막 소제목이 아니면 편 없는 정의 헤더가 다음 소제목 첫 행 위에 껴 그 행이 편처럼 보임 — 지금 파일은 시리즈가 마지막이라 안 드러남 · **L4** 적재(`loadQueueSections`)와 요약(`getQueueSeries`)이 파일을 따로 읽어 그 사이 저장하면 헤더가 한 칸 밀림(새로고침으로 풀림) — 근본은 적재 결과에서 `seriesDefs`를 같이 돌려주기(반환 타입 변경, nav-counts·next-run·queue-reload·홈 영향) · **L7** `verify:layout`은 갤러리 대상이라 비필수, 브라우저 확인 권장.

### 검증

- dashboard **269**(+20) · pipeline **616**(+9) · ui 329 · lint · typecheck · Node 직접 import(queueSeries·loadQueue).

### 미결·다음

- PR → CI → 사용자 승인 뒤 rebase merge → 해시 정정(다음 브랜치 첫 커밋) → **BX6**(문서 반영) 또는 BS5. 마이그레이션 없음.
- 이월: L1(헤더 위치, 시리즈 소제목이 중간으로 오면) · L4(파일 1회 읽기 리팩토링) · 실제 실행 버튼(BS5 뒤)은 `alreadyPublished` 같은 판정을 쓴다.

## PR #136 마감 · BX6 시리즈 문서 반영 — docs/series

참여: 메인 에이전트 · 사용자("BX6 하고 BS5 진행하자").

- PR #136 rebase merge, main `46ac2b8`, 브랜치 삭제. BX5 해시 정정은 이 브랜치 첫 커밋.
- 문서만: CLAUDE.md 파이프라인 단계 아래 시리즈 한 줄 · decisions/queue-sync-direction.md 적재 전략에 정의 줄 보존·태그/줄 끝 URL 매칭 키 제외 · decisions/evidence-collection.md 파서 확장 절에 태그·`(기존 글)`·URL은 힌트 아님 · README 핵심 기능에 시리즈 + 표기 예시(prettier가 예시의 빈 줄을 바꾸므로 `prettier-ignore`). 결정 변경 없음, 근거는 전부 decisions/series.md. 문서 간 표기(태그 문법·정의 줄 문법·산출물 형식) 일치 확인.
- 시리즈 대응 BX1~~BX6 완료(PR #131~~#137). 다음은 **BS5**(단계 라우팅 StepRunner — Mock 대신 실제 단계 구현으로 분기, 워커 배선).

## PR #137 마감 · BS5 단계 라우팅과 워커 배선 — feat/step-router

참여: 메인 에이전트(구현) · Explore(구조 조사) · reviewer(리뷰, Fable 5.1 지정) · 사용자(결정 5건).

### PR #137 마감

- 문서만 PR, CI 초록 → rebase merge, main `7546bae`. BX6 해시 정정은 이 브랜치 첫 커밋 `d208f21`(PR #138 rebase merge, main `b01197e`). 사용자 "ㅇㅇ 그렇게 하자"로 BS5 착수.

### 결정 (착수 전 3건 — 모두 메인 추천대로)

- **발행정보 단계는 B3a 전까지 Mock 위임** — 라우터는 배선에 집중하고 `StepRunnerDeps.publishInfo`를 B3a가 교체한다. (대안: 여기서 posts 쓰기까지 → 범위 초과)
- **Mock 러너 조건은 `NODE_ENV`** — `test`(스모크·CI)·`development`(모델 없이 화면 확인)면 Mock, 없으면 실제. Mock 어댑터가 development에만 노출되는 것과 같은 축(decisions/model-selection.md).
- **어댑터 SDK `maxRetries` 1 + 워커 재시도 사이 대기 10s·20s** — 즉시 재호출하면 429·5xx가 SDK 재시도까지 곱해져 몰린다(BS2 리뷰 ⑭). 대기는 `timers.after`로 종료 신호에 바로 깨어난다.

### 구현 (커밋 5)

- `01d2b4c` `createStepRunner(deps)`·`routeStepRunner` — 단계명 → evidence(BE8)·velog(BS2)·verify(BE10)·linkedin(BS3)·zenn(BS4)·publishInfo(Mock) 분기. 모르는 단계명은 프로그래머 오류(throw). 배럴 export.
- `8f02892` 어댑터 옵션 `maxRetries`(Anthropic·OpenAI) + `ADAPTER_MAX_RETRIES = 1`을 레지스트리가 넘긴다.
- `9d20b24` `RETRY_DELAYS_MS = [10s, 20s]`·`retryDelayMs`·`waitBeforeRetry`(종료 신호에 즉시 해제). 테스트 `drive()`(가짜 일회 타이머를 손으로 넘겨 틱을 끝까지).
- `f69516d` `bin/worker.ts` 배선 — `resolveDataDir`(비었거나 상대경로거나 BLOG_DIR 안이면 기동 거부, BE8 ⑩)·`resolveTonePromptsDir`(절대경로, BS1 ⑦)·redact 설정 → `createStepRunner`. 워커 스모크 4케이스 추가(실모드 DATA_DIR 없음·BLOG_DIR 안·정상 기동·Mock 모드).
- `4b4fb41` `liveModel.smoke.test.ts` — `GALLEY_LIVE_SMOKE=1`일 때만 실제 레지스트리 + 합성 번들(tmp DATA_DIR)로 벨로그 단계 한 번(토큰·비용 출력). CI는 건너뜀.
- 검증: pipeline 631 · dashboard 269 · ui 329 · lint · typecheck · 스모크 8 + 1 skip.

### 리뷰 — high 0 · med 3 · low 12 → 사용자 결정 2건, 나머지 추천대로

- 세션이 TCC 차단(2026-09-26 03:20)으로 끊겨 반영은 다음 세션(10:50)에. 사용자 결정: **M1** `~` 확장 안 함 + 안내만 수정(PROMPTS_DIR과 규칙 통일) · **L8** 실모델 스모크 기본 모델 Opus 5 → Haiku.
- `3cc9be2` fix(worker) — **M1** DATA_DIR 안내의 `~/.galley/data` 예시 삭제(Node `--env-file`은 `~` 미확장 → 리포 안에 `~` 폴더가 생김), `.env.example`·기동 힌트에 "절대경로만, `~` 불가" · **M2(a)/L9** 실모드 기동 로그에 `availableModels`(키 빠진 채 띄우면 Run이 MODEL_UNAVAILABLE로만 실패하던 것) · **M3** DB 테스트 가짜 `after`가 STEP_TIMEOUT_MS는 등록만(전엔 타임아웃 타이머도 즉시 발화해 Mock 시도를 끊을 수 있었음) · **L3** RETRY_DELAYS_MS 주석 정정(대기 중에도 runStep heartbeat 타이머가 돈다) · **L11** 대기 앞 aborted 중복 검사 제거.
- `edc709b` test(pipeline) — **L2** 재시도 대기 중 heartbeat 발화 단언 · **L1** `drive()` 주석에 blockingRunner 병용 금지(fireAfter가 단계 타임아웃까지 발화) · **L6** 워커 스모크 `exit` → `close`(stdio 잔여로 마지막 로그 누락 가능) + 모든 케이스 `spawnWorker` · **L7** 개발자 셸 REDACT_CONFIG_PATH 제거(결정성) · **L8**.
- **M2(c)** 대시보드·워커 NODE_ENV 조합표 → decisions/run-execution-model.md.
- 기록만: **L4** SDK maxRetries 전달 테스트 없음(클라이언트가 lazy라 생성자 인자를 볼 수 없음) · **L5** resolveDataDir는 심볼릭 링크·대소문자 다른 경로를 BLOG_DIR 안으로 못 잡음(macOS) · **L10** B3a 전에 승인된 Run은 posts 폴더가 없고 completeTopic도 안 돈다 → B3a 이월 · **L12** todo BS5 시그니처(`registry, prompts, storage`)와 구현(`StepRunnerDeps`) 표기 차이, `REDACT_CONFIG_PATH` 절대경로 규칙 미결(BS1 ⑦에서 BS5 때 결정하기로 했으나 상대경로 예시 유지 — 워커·대시보드 cwd가 다르므로 다음 배선 때 묶기).

### 미결·다음

- `git push` → PR "feat: 단계 구현 라우팅과 워커 배선 (BS5)"(마이그레이션 없음) → CI → 승인 → rebase merge → 다음 브랜치 첫 커밋에서 해시 정정.
- 다음 후보: **B3a**(posts 폴더 쓰기·발행정보, publishInfo Mock 교체, L10·BX4 M2 이월) 또는 BE11.

## PR #138 마감 · B3a posts 산출물 쓰기·발행정보 단계·승인 시 발행 준비 — feat/publish-posts

참여: 메인 에이전트(구현) · Explore(구조 조사) · reviewer(리뷰, Fable 5.1 지정) · 사용자(결정 8건).

### PR #138 마감

- CI 4잡 초록 → 사용자 "응응" → rebase merge, main `b01197e`, 브랜치 삭제. 마이그레이션 없음. BS5 해시 정정은 이 브랜치 첫 커밋 `a8b79f5`. 사용자 "B3a로 가자".

### 조사와 결정 (착수 전 4건 — 모두 메인 추천대로)

- 조사: posts에 쓰는 코드 없음, 승인은 상태만 `done`, 단계 산출물은 DATA_DIR(`artifacts/<슬러그>/<runId>/` + `evidence/<슬러그>/<runId>.json`)에 Run별로 있음, completeTopic·renderSeriesPublishSection·seriesChecklist는 호출부 없음. 기존 posts 폴더 관례(`ls`로 파일 이름만 확인, 발행정보 한 파일 읽음): `<제목_언더스코어>.md`·`_링크드인.md`·`_zenn.md`·`_발행정보.md`·`_썸네일.png`, 발행정보 절 = 소개(150자)·URL 슬러그·썸네일·삽화·태그·Zenn(タイトル·emoji·type·topics). 문서 충돌 2곳(evidence·verification 위치 루트 vs `runs/<runId>/`, "새 폴더에만" vs "루트 최신본").
- ① **쓰는 시점 = 승인 시 복사**(단계는 DATA_DIR에만, 승인이 posts 복사·큐 완료 이동·QueueItem 갱신까지) ② **기존 관례 이름 + 루트 7개**(썸네일은 B3b, run-execution-model의 `runs/<runId>/`는 결정 변경 — Run별 이력은 DATA_DIR이 맡음) ③ **Galley가 쓴 폴더만 덮어씀** — DB에 이 주제의 승인된 Run이 같은 슬러그에 있을 때, 아니면 `POSTS_DIR_EXISTS`(마커 파일 없음) ④ **소개·태그는 모델 호출 1회**(JSON, Zenn 절은 frontmatter에서 코드가).

### 구현 (커밋 5)

- `e0259f1` `steps/publishInfo.ts` 렌더러(순수): 기존 형식 + `## 벨로그 시리즈` + `## 근거`(`- <sha7> <경로>:L<a>-<b> (<날짜>) — <note>`, 0건 "근거 없음", 못 읽은 포인터 수) + `## 발행 체크리스트`(id → 한국어). `parsePublishTitle`(첫 줄 왕복). 삽화 절은 없음(Galley가 만들지 않음).
- `6cc2b82` `steps/publishInfoStep.ts`: velog H1(시리즈 표기 뗀 것, 없으면 힌트 뗀 주제 제목)·zenn frontmatter(`parseZennFrontmatter`)·번들 포인터, 모델 1회(제목·본문만 프롬프트, `PUBLISH_INFO_OUTPUT_INVALID`는 재시도) → `publish.md`(DATA_DIR). `publish/postFiles.ts`(`toFileStem` 공백→`_`, 금지 문자 제거, 60자). `createStepRunner` 발행정보 기본값 실제 러너. limits 2개.
- `529ec84` `publish/PostsWriter.ts` `LocalFsPostsWriter`: 6개 파일 임시+rename, 폴더 있으면 overwrite일 때만, 실패는 값. B3c 구조 테스트 포함.
- `044b0bc` `run/approvePublish.ts` `approveAndPublishRun`: 읽기·검증(부작용 없음) → posts 쓰기 → DB(QueueItem을 completedTitle·완료로 먼저 + Run done) → 재적재. 실패 코드 `ARTIFACT_MISSING`(detail 파일명)·`PUBLISH_TITLE_MISSING`·`TOPIC_NOT_IN_QUEUE`·`INVALID_COMPLETION`·`POSTS_*`. DB 통합 테스트 15.
- `6f1dbcd` dashboard `approveRunById` → approveAndPublishRun + BLOG_DIR·DATA_DIR(resolveDataDir 워커와 같은 규칙) 스토어 조립, 한국어 문구, `_shared` 상태표(충돌 409·설정/쓰기 500), 응답 `postsDir`.

### 리뷰 — high 0 · med 5 · low 9 → 사용자 결정 4건(추천대로), 나머지 추천대로

- `90de4ae` **M1** 큐 파일 쓰기를 DB 트랜잭션 안 Run 갱신 성공 뒤로(전엔 파일 먼저 → 롤백 시 파일만 완료로 남아 재적재에서 행 갈라짐) · **M2** posts `evidence.json`에서 `analyses`(분석 글 요약)도 뺀다(`toPostsEvidence`, "포인터만") · **M5** 커밋 뒤 재적재 실패는 승인 성공 · **L9** 롤백 2건·재적재 실패·시리즈 편(태그 유지·seriesKey 보존) 테스트.
- `99166d4` **L1** PostsWriter 실패 시 `.tmp` 정리(묶음 원자성 아님은 주석) · `fa69d01` **L4** 승인 Dialog 문구에 posts 쓰기·큐 완료 이동 명시.
- 기록만: **M3** 승인 뒤 큐 제목이 `글제목 (posts/슬러그)`가 되어 줄을 대기로 되돌려 재실행하면 `topicSlug(title)`가 힌트까지 포함해 **다른 슬러그** → 새 폴더, 덮어쓰기 경로에 실사용으로 도달하기 어려움(테스트는 손으로 되돌려 통과) — 슬러그 파생 규칙 변경은 별도 결정 · **M4** Mock 러너는 DATA_DIR에 안 써서 development 조합에서 승인이 `ARTIFACT_MISSING` — 문서 명시, Mock 산출물 쓰기는 후속 · **L2** 재승인 시 글 제목이 바뀌면 옛 이름 4개 파일 잔존 · **L3** 섹션을 DB 상태로 고름(재적재 전 순간 TOPIC_NOT_IN_QUEUE) · **L5** `approveRun` 호출처 없음(테스트·배럴 유지) · **L6** 소개·태그 redact 미적용(TD8) · **L7** 근거 줄에 리포 구분 없음(별칭은 식별 정보) · **L8** localDate는 프로세스 TZ 의존(대시보드가 루트 .env TZ를 읽는지 확인 필요).
- 문제 없음 확인: posts evidence는 snippet 없음, verification은 본문 텍스트·포인터·줄 번호만, 프롬프트에 리포 별칭·번들 없음, PostsWriter는 blog 폴더 밖 쓰기 불가, QueueItem 선갱신이 재적재 매칭(`normalizeTopicTitle`·`takeMatch` 같은 섹션 우선)에 맞음, `.ts` 확장자·enum·파라미터 프로퍼티 없음(Node 직접 import 확인).

### 검증

- pipeline **717**(+25) · dashboard **271**(+2) · ui 329 · 워커 스모크 8+1 skip · lint · typecheck.

### 미결·다음

- PR "feat: posts 산출물 쓰기·발행정보 단계·승인 시 발행 준비 (B3a)"(마이그레이션 없음) → CI → 승인 → rebase merge → 해시 정정.
- 이월: M3 재승인 절차·슬러그 파생(별도 결정) · M4 Mock 산출물 쓰기 · L2·L3·L5·L6·L7·L8 · B3a 전 승인된 Run은 posts 없음(재실행으로) · 실행 상세에서 `postsDir` 안내(발행 화면 때). 다음 후보: **B3b**(썸네일) 또는 BE11 잔여 정리.
