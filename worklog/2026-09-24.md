# 2026-09-24

> 오전의 PR #130 마감·BX 착수 기록은 worklog/2026-09-23.md 끝 절에 있다.

## BX P0 마감 — docs/series-plan (PR #131)

- 직전 세션이 `~/Desktop` TCC 차단으로 끊겨 확정 편집이 안 된 상태로 재개. decisions/series.md "확인 사항"을 "확정 사항"으로 바꿈(완료 줄 URL = 괄호 밖 줄 끝, 일본어 표기 = 정의 줄 `ja:`) `95a1a3f`.
- PR #131(문서만) CI 4잡 초록 → 사용자 승인 → rebase merge(main `eddce0e`), 브랜치 삭제.

## BX1 큐 파서 시리즈 태그·정의 줄 — feat/queue-series-parse

참여: 메인 에이전트(구현) · reviewer(리뷰) · 사용자(픽스처 방식·리뷰 반영 범위·M1 결정).

### 구현 (커밋 4 + 리뷰 수정 3 — 해시는 main 기준, PR #132 rebase 후 정정)

- `5ae088a` 매칭 키에서 시리즈 태그 제외 — `normalizeTopicTitle`·`stripTopicHints`가 맨 앞 `[A-1]`과 줄 끝 URL을 먼저 뗌.
- `82dfa82` 시리즈 태그 파싱과 정의 줄 보존 — `QueueTopic.series`, `ParsedQueue.seriesDefs`(필수 필드: 빠뜨리는 생성처를 타입이 잡음), 라이터가 같은 자리로 되돌림, `moveTopic`이 태그를 들고 가고 정의 줄 위치를 보정, 재정렬·복원은 `...queue`로 전달.
- `8c7889a` 시리즈 fixture 라운드트립(합성, 바이트 동일) + `.prettierignore`.
- `207da68` `(기존 글)` 메모를 근거 힌트에서 제외.

### 판단

- **커밋 순서 변경**: 계획은 파싱 → 픽스처 → 키였으나 키 커밋을 먼저. 파서가 태그를 떼는 순간 옛 DB 행(`[A-1] …`)과 새 줄의 키가 어긋나지 않게 — 커밋 하나씩 되돌려도 매칭이 유지된다.
- **픽스처 = 합성**: 실제 `~/Desktop/blog/주제_큐.md`를 리포로 `cp`하려다 auto mode 권한 검사가 거부(민감 출처). 우회하지 않고 사용자에게 (1) 직접 복사 (2) 합성을 물음 → 사용자 "합성"(추천). 구조만 동일(서문·줄 끝 탭·소제목 5·정의 7·편 없는 시리즈·`(기존 글)` 편·완료 줄 태그+URL), 제목은 가짜.
- 픽스처는 줄 끝 탭이 테스트 대상이라 `pnpm format`이 지우면 깨짐 → `.prettierignore`(CI는 format:check를 안 돌리지만 수동 format 대비).
- 사용자 "일단 여기까지, 나머지는 집가서"로 한 번 중단(커밋 2개 로컬) → 저녁 재개.

### 리뷰 — high 0 · med 3 · low 7 · 누락 테스트 6 → 사용자 선택 반영

- **M1** 다른 시리즈의 같은 제목이 한 항목·같은 슬러그로 합쳐짐 → 사용자: **현행 유지**, 대신 "시리즈물은 발행 글 제목 끝에 N편을 붙인다"(의미 확인 질문 → "발행 글 제목에 N편"). BX3 산출물 형식 변경 — decisions/series.md에 ⚠️ 표시, 표기·스케줄 프롬프트 동기화는 BX3 착수 때.
- **M2** 태그째 적재된 옛 주제는 새 실행부터 슬러그가 바뀜 → 로컬 DB(`sqlite3 -readonly`) 조회 결과 태그 제목 행 0건, 영향 없음.
- **M3** 사라진 줄 복원이 태그를 잃음(BX2 몫) → 주석 정정 `3f6338b`.
- **L2** `[A-0]`·`[A-01]`을 태그로 받음 → 정규식 `[1-9]\d?` `d335937`.
- **L6** `**/__fixtures__`가 넓음 → `packages/pipeline/src/queue/__fixtures__/*.md`로 좁혀 `8c7889a`에 fixup(미푸시라 이력 재작성).
- **누락 테스트 4종** `8bd3133` — 재정렬 seriesDefs 전달 · moveTopic 경계 3(정의 줄만 있는 후보·정의 줄 뒤 다른 소제목·후보 마지막 편 제거) · 복원 뒤 정의 줄 유지 · 완료 줄에 URL을 나중에 붙여도 같은 id.
- 기록만(이월): **L1** 이동 한 번에 정의 줄 표기 정규화(전각 괄호·공백, 괄호 둘인 이름의 note 분리) — BX2 `ja:` 추출 때 원문 보존 판단 · **L3** 완료 줄 URL 필드 분리(BX2) · **L4** 줄 끝 URL은 모든 섹션에서 뗌 — 의도대로, 결정 문서에 한 줄 · **L5** `SeriesTag`·`SeriesDef` 배럴 export(BX2) · **L7** 대시보드 태그 표시(BX5).
- 작업 중: 태그 줄 수를 20으로 잘못 세 테스트 실패(실제 18) · 대기만 있는 입력을 바이트 비교해 실패(라이터가 빈 섹션을 채움) → 제목 비교로.

### 검증

- pipeline **528**(+20) · ui 329 · dashboard 249 · lint · typecheck · format:check.

### 미결·다음

- ~~PR → CI → 사용자 승인 뒤 rebase merge → main 해시로 이 절·todo 정정~~(PR #132 CI 4잡 초록, 사용자 "그렇게 하자"로 rebase merge — main `18dcf50`, 해시 정정은 BX2 첫 커밋) → **BX2**(마이그레이션 seriesKey·episodeNo + getSeriesContext, 이월 L1·L3·L5·M3 포함). 머지 보고에 마이그레이션 없음 명시(BX1은 스키마 변경 없음).

## BX2 시리즈 컬럼·컨텍스트 헬퍼 — feat/queue-series-columns

참여: 메인 에이전트(구현) · reviewer(리뷰) · 사용자(리뷰 결정 3건).

### 구현 (커밋 3 + 리뷰 수정 5 + docs 2 — 해시는 브랜치 기준, 머지 후 정정)

- `e41264a` PR #132 마감(BX1 main 해시 정정).
- `da7a516` `QueueItem.seriesKey`·`episodeNo` + 인덱스, 마이그레이션 `20260924120000_queue_item_series`(`prisma migrate diff` 무차이). `parsedQueueToRows`가 채우고 `isUnchanged`에 포함, 자동 보류된 행도 값을 남김(복원용).
- `c15ced9` 사라진 줄 복원 시 태그 재부착(BX1 리뷰 M3).
- `6252cd2` `getSeriesContext`·`buildSeriesContext`·`seriesNameJa`·`trailingUrl`, `SeriesTag`·`SeriesDef` 배럴 공개(BX1 리뷰 L3·L5). L3는 URL 필드 분리 대신 컨텍스트가 제목에서 읽는 쪽 — 파일 라운드트립을 건드리지 않게.

### 판단

- 처음 구현은 결정 문구 "편 목록은 DB" 그대로 — 함수가 적재를 돌린 뒤 DB 행을 읽었다. 정의 없는 키는 `SERIES_NOT_DEFINED`(값).

### 리뷰 — high 1 · med 4 · low 9 → 사용자 결정

- **H1** 워커가 부르면 적재가 프로세스 사이에서 경쟁(`importChain`은 프로세스 안에서만) → 같은 줄이 두 행. reviewer 선택지 (a) 읽기 전용 (b) DB 직렬화 (c) DB만 읽기. 메인 추천 (a) → 사용자 "읽기 전용" `00bcf57`(매칭 헬퍼 제네릭 export) · `9756e6b`(파일 한 번 파싱, 편은 파일 줄, DB는 id만). 함께 풀림: **M2** keep-done 행이 편으로 섞임 · **L10** 파일 두 번 읽기 · **L11** 정렬 비결정.
- **M3** 직접 보류한 편 포함 여부(결정 문구 "세 상태"와 구현 "네 섹션" 불일치) → 사용자 "넣는다"(편 번호·M이 어긋나지 않게).
- 나머지 → 사용자 "추천대로": **M5** 줄 끝 URL이 닫는 괄호를 삼킴 `b9ad09f` · **M4** `(기존 글)` 편은 대기·후보여도 URL·슬러그 / **L6** velog.io만 / **L7** `posts/` 항 경계 / **L8** `(기존 글, 2025)` 인정·힌트 파서와 `isAlreadyPublished` 공유 / **L9** `ja：` `252c2a4` · **L12** 복원 시 키·번호 하나만 있으면 태그 생략 + 재적재 updatedAt 테스트 `ca02897`.
- 기록만: **L13** 마이그레이션 직후 첫 적재에서 태그 행 일괄 update → 자동 연결 재계산 1회(머지 보고) · **L14** 자동 보류 행이 매 적재 update되는 것으로 보임(브랜치 밖) → todo TD7.
- 작업 중: prettier가 테스트 import를 한 줄로 합친 뒤 치환이 빗나가 `isAlreadyPublished`가 vitest import에 들어가고 중복 선언 → 첫 줄 직접 수정.

### 검증

- pipeline **548**(+20) · ui 329 · dashboard 249 · lint · typecheck · format:check · Node 직접 import(`seriesContext.ts`).

### 미결·다음

- PR → CI → 사용자 승인 뒤 rebase merge → **migrate deploy**(로컬 DB) → 실제 큐 재적재 시 사라진 줄 경고 0건 사용자 확인(BX2 완료조건) → 해시 정정 → **BX3**(워커에서 `getSeriesContext` 읽기 전용 호출, 발행 제목 끝 "N편" 표기 확정 필요).
