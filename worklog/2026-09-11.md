# 2026-09-11

## `layout` required check 등록 문서 반영 — docs/branch-protection-layout

### 참여

- 메인 세션 단독. 사용자 "무슨 작업하고 있었는지 알아?" → 메모리·git 기준으로 재개 지점 보고(main `772ce0a` clean, 다음 A5b) + 전 세션에서 끊긴 작업 1건 발견 → 사용자 "중간에 끊긴 작업부터 우선".

### 논의·결정

- **끊긴 작업**: 2026-09-10 사용자가 GitHub 설정에서 `layout`을 required check로 등록했지만, 문서 갱신은 macOS Desktop 접근 EPERM으로 중단됨. 확인 결과 decisions/branch-protection.md 체크 목록에 `layout` 없음, layout-measurement.md는 여전히 "등록은 사용자 몫", `docs/branch-protection-layout` 브랜치도 생성 안 됨.
- **사실 확인**: `gh api .../branches/main/protection/required_status_checks` → contexts `verify`·`gitleaks`·`layout`, strict true. 문서는 이 값 그대로 적음.
- **수정**(`e599926`): branch-protection.md 상태 체크 목록에 `layout`·strict 추가 + 갱신 이력 / layout-measurement.md CI 통합 항목 "사용자 몫" → "required check로 등록됨(머지 차단)" + 갱신 이력 / CLAUDE.md §7 `layout` 잡 문장에 "required check — 실패 시 머지 불가".
- 새 결정은 없음 — 2-B 결정에서 예약해 둔 후속(등록)을 기록만 함. 결정 변경 표시 불필요.
- **정리**: 로컬에 남아 있던 원격 추적 ref(`origin/ci/layout-job`·`origin/docs/log-2026-09-10-pr45`)는 원격에서 이미 삭제된 것 → `git fetch --prune`. 원격 브랜치는 main 하나.

### 검증

- prettier `--check` 3파일 통과, pre-commit gitleaks 통과. 제품 코드 변경 0.

### 미결·다음

- ~~PR → CI → rebase merge → 브랜치 삭제~~ → 아래 "PR #47 마감".
- 다음: A5b(상태→Badge 매핑, `lib/` 테스트) → AN4 → A5c → AN5(선행 UM2) → AH1~AH4.

## PR #47 마감 + 해시 정정

- PR #47 CI 초록(`verify` 49s · `gitleaks` 7s · `layout` 1m13s — `layout`이 required check로 걸린 뒤 첫 PR) → **rebase merge**, 원격·로컬 브랜치 삭제, main 동기화.
- rebase로 바뀐 main 해시: docs `e599926` · /log `a7d4576`. 위 절·todo의 브랜치 해시(`ef606ba`)를 main 해시로 정정(이 PR).
- 해시 정정을 다음 작업(A5b) /log에 묶는 안(추천 — docs PR 반복 회피)과 따로 PR 여는 안을 제시 → **사용자 결정: 따로 PR**.
- 참고: PR 생성 직후 `gh pr checks --watch`는 체크가 아직 안 잡혀 "no checks reported"로 바로 끝남 → 몇 초 뒤 재실행.

## A5b 큐 상태→Badge variant 매핑 — feat/dashboard-status-badge

### 참여

- 메인 세션 단독. 사용자 "A5b 진행하자".

### 논의·결정

- **스펙**: decisions/layout.md §5(후보 회색 / 대기 블루 / 실행 중 블루+펄스 / 승인 대기 주황 / 완료 초록 / 실패 빨강). 큐 상태 타입은 새로 만들지 않고 `@galley/pipeline`의 `QueueStatus`(대기·후보·보류·완료)를 재사용.
- **범위**: 큐 상태 4개만. 실행 중·승인 대기·실패는 Run 상태라 B1e(Run 스키마) 뒤 — 지금 앱에 Run 상태 타입을 만들지 않음(🔒 B1a 상태 머신이 정의할 몫).
- **스펙 누락 발견**: §5에 **보류** 색이 없음 → 선택지 neutral(추천 — 후보와 같은 "진행하지 않는 주제", 주황은 승인 대기 전용 유지) / warning(승인 대기와 신호 겹침) / danger(실패와 겹침, 과함) → **사용자 결정 neutral**. §5·갱신 이력에 기록(`080cecf`).
- **구현** `apps/dashboard/lib/queue-status-badge.ts`: `queueStatusBadgeVariant(status: QueueStatus)` + 내부 `Record<QueueStatus, BadgeVariant>` — 상태가 늘면 컴파일 에러로 누락이 드러난다. variant 타입은 `NonNullable<BadgeProps['variant']>`(ui 공개 배럴, 딥 임포트 없음). pipeline은 `import type`만이라 클라이언트 번들에 서버 코드가 들어가지 않는다.

### 검증

- 테스트 먼저(RED: 모듈 없음) → 구현 → GREEN. dashboard 테스트 6→10. dashboard typecheck·eslint·prettier 통과. 레이아웃 컴포넌트·갤러리 변경 없음 → `verify:layout` 대상 아님.
- 커밋: docs `080cecf` · feat `c3efdbe` · /log `2afecd7` (main 해시, PR #49 rebase merge 후 정정).
- `apps/dashboard/next-env.d.ts`가 `.next/dev/types` 경로로 바뀌어 있었음 — 실행 중인 `next dev`(사용자 dev 서버)가 재생성하는 파일로 이 작업과 무관, 커밋 제외(되돌리지 않음).

### 미결·다음

- PR → CI → rebase merge → 해시 정정은 별도 PR(사용자 선호).
- todo AN4의 의존 표기가 낡아 있었음("A4b·A5a 미완이라 보류") — 실제로는 A4b `0b2534d`·A5a `e9ca423` 모두 2026-09-09 완료. 착수 가능으로 정정.
- 다음: AN4(큐 탭 4개 `?tab=` + 카테고리 필터).

## PR #49 마감 + 해시 정정

- PR #49 CI 초록(`verify` 46s · `gitleaks` 6s · `layout` 1m29s) → **rebase merge**, 원격·로컬 브랜치 삭제, main 동기화.
- rebase로 바뀐 main 해시: docs `080cecf` · feat `c3efdbe` · /log `2afecd7`. 위 절·todo의 브랜치 해시(`b823564`·`c2b2780`)를 main 해시로 정정(이 PR, 사용자 선호대로 별도 PR).

## next-env.d.ts 처리 — 그대로 둠

- 사용자 "next-env.d.ts 파일은 계속 건드릴 일 없는거지?" → Next가 실행마다 재생성하는 파일(`next dev`는 `.next/dev/types`, `next build`는 `.next/types` = 커밋본, 스캐폴드 `efe8223` 이후 불변). 커밋에서만 빼면 되고 CI는 커밋본으로 돈다.
- 선택지: 그대로 두기(추천) / `.gitignore`+추적 해제(CI `verify`의 typecheck가 build 전이라 Next 타입 없이 깨질 가능성 → 타입 생성 스텝 필요) / `skip-worktree`(로컬 전용, 진짜 변경도 가림). 그대로 두기로.

## AN4+A5c 큐 화면 탭·카테고리 필터 + 데이터 로드 — feat/queue-tabs

### 참여

- 메인 세션 단독. 사용자 "AN4 진행하자".

### 논의·결정

- **범위(사용자 결정)**: AN4 완료조건("탭 4개가 섹션 4개와 1:1로 읽힘")이 실데이터를 요구하고 가짜 데이터는 금지 → **AN4+A5c 한 PR, 커밋 분리**. 기각: A5c 별도 PR 먼저(단독으론 화면 검증 불가) / AN4 빈 뼈대(완료조건 미충족, 재작업).
- **데이터 출처(사용자 결정)**: 요청마다 **파일→DB 재적재 후 DB에서 읽기**(decisions/queue-sync-direction.md 그대로, 홈 AH4·사이드바 "대기 n"과 같은 소스). 기각: 파일 직접 파싱(결정과 어긋나고 소스가 갈림).
- **env 위치 발견·결정(사용자 결정)**: 실제 env 파일은 `packages/pipeline/.env`(DATABASE_URL만)뿐이고 Next는 `apps/dashboard/.env`만 읽어 대시보드가 env를 못 받는 상태, `BLOG_DIR`은 어디에도 없음 → **루트 `.env` 하나**(README 전제) + Node 내장 로더(next.config `process.loadEnvFile`, Prisma CLI는 pipeline `db` 스크립트 `node --env-file`), engines `>=20.12`. 기각: 패키지별 `.env`(DATABASE_URL 중복) / 대시보드가 pipeline `.env` 읽기(결합) / dotenv·`@next/env`(불필요). decisions/env-location.md 신설, planning ⑫.
  - 확인한 사실(결정 문서에 기록): 이미 설정된 env가 `.env`보다 우선(`loadEnvFile`·`--env-file` 둘 다). 상대경로 `DATABASE_URL`은 CLI·Next 런타임 모두 `schema.prisma` 기준으로 풀림.
  - 로컬: 루트 `.env` 생성(`.env.example` 복사 + `DATABASE_URL`만 이전, 권한 600, gitignore 확인). `packages/pipeline/.env`는 `.env.bak`으로 옮겨 둠(삭제는 사용자 몫) — 옮긴 뒤 `db migrate status`가 루트 `.env`만으로 "up to date".
- **탭·필터 규칙**: 탭 값 waiting·candidates(기존 문서) + hold·done, 모르는 값은 대기. 개수는 대기·후보만(layout §4). 카테고리 필터는 후보 탭에서만, 목록에 없는 카테고리(낡은 URL)는 필터 없음. 선택은 `router.push(?category=)` — 로컬 상태 없음.
- **구조**: pipeline `loadQueueSections`(재적재 + 섹션별 읽기) / `lib/queue-data.ts`(BLOG_DIR 없음·로드 실패를 `{ ok: false, error }`로) / `lib/queue-tabs.ts`(쿼리 해석·링크) / `lib/queue-view.ts`(탭 개수·활성·필터 선택지·행 모델) / `queue/page.tsx`(그리기만 — Server Component는 렌더 테스트 안 함) / `CategoryFilter`(client, Select).
- **범위 밖으로 남긴 것**: 툴바 검색(layout §4에 있으나 AN4 항목 아님) · 대기 탭 맨 위 "다음 실행" 표시·DnD(A6a) · 행 ⋮(AN5) · 근거 n건(Phase 1-B) · 완료 행 실행 링크(B1e).

### 검증

- 단위마다 테스트 먼저(RED) → GREEN. dashboard 10→35(queue-tabs 12 · queue-data 3 · queue-view 7 · CategoryFilter 3), pipeline 48→50(loadQueue DB 통합 2). 전체 lint·typecheck·test·format:check 통과.
- 사용자 dev 서버 `/queue` → 200, "루트 .env에 BLOG_DIR이 없습니다." 안내.
- 프로덕션 빌드 통과(`/queue` ƒ 동적). 픽스처(스크래치 큐 파일 + 임시 SQLite, 셸 env로 주입)로 `next start -p 3100`: 6개 URL 전부 기대대로 — 기본 대기 2행 · 후보 3행+필터 · 후보+백엔드 1행 · 보류 · 완료+완료일 · 모르는 탭→대기. `dev.db` 수정 시각 불변, 픽스처 DB 섹션별 개수 = 파일. 상대경로 `file:./relcheck.db`로 한 번 더 띄워 schema 기준 DB에 7행, 대시보드 폴더엔 DB 없음 확인 후 삭제.
- 커밋(브랜치 해시, 머지 후 정정): docs `55ff2eb` · tabs `adce8ba` · loader `d67705f` · data `06ce55e` · env `eb1715b` · view `d3ac9e2` · screen `15460c7`.

### 미결·다음

- **사용자 몫**: 루트 `.env`의 `BLOG_DIR` 채우기 → 실제 주제_큐.md로 화면 확인. `packages/pipeline/.env.bak` 삭제.
- PR → CI → rebase merge → 해시 정정 별도 PR.
- 다음 후보: AN5(선행 UM2 Menu) · AH3~AH4(홈 골격·다음 실행 카드 — 이번 `getQueueSections` 재사용).
