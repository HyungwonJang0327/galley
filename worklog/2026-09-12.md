# 2026-09-12

## UM2 Menu 기본값 확정 + PR — feat/ui-primitives-menu

### 참여

- 메인 세션 단독. 사용자 "기본값 그대로 확정, PR 올려".

### 결론·결정

- **Menu 규칙 확정**(사용자 결정): 2026-09-11 에이전트 기본값 그대로 — 데이터형 API(items 항목|구분선 · onSelect(id) · trigger 요소 · align 기본 end · 비제어 open) + 팝업 폭 최소 160px · 상한 min(320px, 100vw−32). decisions/layout.md 갱신 이력에 기록.

### 미결·다음

- ~~PR → CI → rebase merge → 해시 정정(별도 PR)~~ → 아래 "PR #54 마감".
- 다음: AN5(행 ⋮ — 대기로/후보로/보류로/지금 실행 노출·배선, 파일 반영은 A6c).

## PR #54 마감 + 해시 정정

- PR #54 CI 초록(`verify` 53s · `gitleaks` 6s · `layout` 1m16s) → **rebase merge**, 원격·로컬 브랜치 삭제, main 동기화(`a05fd49`).
- rebase로 바뀐 main 해시: ui `eb3df11` · gallery `8aea198` · 실측 `925d54c` · /log `11171ee` · 확정 `a05fd49`. worklog 2026-09-11 UM2 절·todo UM2의 브랜치 해시를 main 해시로 정정(이 PR, 별도 PR). 이 절은 자기 커밋 해시를 적지 않는다.
- PR #55(해시 정정) CI `layout`이 `timeout: DevToolsActivePort`(러너에서 Chrome 기동 15초 초과 — 빌드 후 측정 전에 실패, 스크린샷 없음)로 1회 실패 → 실패 잡만 재실행해 통과(`layout` 1m31s) → rebase merge(main `6a44d5c`). 같은 코드가 PR #54에선 통과해 러너 일시 문제로 판단. 재발하면 `cdp.mjs` launchChrome 대기 15s→30s 수정을 제안할 것.
- 에이전트가 중간 보고를 영어로 씀 → 사용자 지적("갑자기 왜 영어써") → 한국어로 복귀.

## AN7 큐 수동 갱신 버튼 — feat/queue-reload-button

### 참여

- 메인 세션 단독. 사용자: "실제 큐 데이터는 보여" + "클로드 요금제 내에서 하려고 하는데 그러면 문서 갱신을 계속 해야 하니, 갱신 버튼 누르면 DB 데이터를 파일 기준으로 맞추기". AN5보다 먼저 진행.

### 논의·결정

- **사실 확인(에이전트)**: DB를 읽는 화면은 /queue 하나이고 이미 요청마다 파일→DB 재적재(AN4) → F5가 같은 일을 한다. 버튼의 가치는 화면을 떠나지 않는 갱신·결과 표시·앞으로 생길 홈·사이드바 배지까지 한 번에 맞추는 것.
- **위치(사용자 결정)**: 큐 화면 헤더. 기각: TopBar 전역(TopBar를 [☰]·Galley·모델 칩만 두는 layout §2 변경 필요).
- **사용자 질문** "페이지 열 때마다 적재 = AI API 호출?" → 아니오. Node fs로 주제_큐.md 읽기 → `parseQueue` → SQLite 전체 리셋(Prisma 트랜잭션)뿐, 모델 호출은 워커의 파이프라인 단계에서만(코드 확인).
- **자동 적재(사용자 결정)**: 유지 + 버튼. 기각: 버튼으로만 동기화(누르기 전까지 예전 목록, 스케줄이 옮긴 완료가 늦게 반영). → decisions/queue-sync-direction.md "수동 갱신 버튼" 절 + layout.md §4(결정 변경 없음), todo AN7 신설.
- **구조**: `lib/queue-reload.ts`(getQueueSections 재사용 + 탭 순서 섹션별 개수 요약) / `queue/actions.ts`(Server Action — 적재 후 `revalidatePath('/', 'layout')`) / `ReloadQueueButton`(client, action을 prop으로 받아 테스트는 가짜 — 진행 중 비활성·"불러오는 중…", 결과·실패 사유 `role=status` 한 줄, 요청 자체 실패는 "다시 눌러 주세요"). 정상·오류 두 PageHeader 모두에 버튼.

### 검증

- dashboard 테스트 35→45(queue-reload 3 · actions 2 · 버튼 5). lint·typecheck 통과, prettier 적용. verify:layout 대상 아님(ui 패턴·갤러리 변경 없음).
- 픽스처 실측(스크래치 주제_큐.md + 임시 SQLite `migrate deploy`, 셸 env 주입, `next start -p 3977`, CDP 프로브) **7/7 PASS**: 첫 클릭 결과 문구(대기 1 · 후보 3 · 보류 0 · 완료 1 — 셸 env가 루트 .env보다 우선해 실제 blog를 안 읽음) · 파일 수정 후 클릭 대기 2 · 새 행 표시 · 탭 개수 대기 2 · URL 불변 · 새로고침 없음(페이지 표식 유지) · 라벨 복귀. 스크린샷 육안 확인. 실제 blog 폴더 미접촉.
- 커밋(main 해시, PR #56 rebase merge 후 정정): docs `88299e9` · action `efc52a1` · button `b947237` · /log `55eaa49`.

### 미결·다음

- ~~PR → CI → rebase merge → 해시 정정.~~ → 아래 "PR #56 마감".
- 다음: AN5(행 ⋮ — 대기로/후보로/보류로/지금 실행 노출·배선, 파일 반영은 A6c).

## PR #56 마감 + 해시 정정

- 사용자 "PR 올려" → PR #56 CI 초록(`verify` 41s · `gitleaks` 9s · `layout` 1m12s, 이번엔 Chrome 기동 타임아웃 없음) → **rebase merge**, 원격·로컬 브랜치 삭제, main 동기화(`55eaa49`).
- rebase로 바뀐 main 해시: docs `88299e9` · action `efc52a1` · button `b947237` · /log `55eaa49`. 위 AN7 절·todo AN7의 브랜치 해시를 main 해시로 정정(이 PR, 별도 PR). 이 절은 자기 커밋 해시를 적지 않는다.

## Chrome 기동 대기 30초 — fix/dashboard-chrome-wait

### 참여

- 메인 세션 단독. 해시 정정 PR #57에서 CI `layout`이 두 번째로 타임아웃 → 에이전트가 수정 제안(AN5 전 작은 PR) → 사용자 "응 30초 수정 먼저 해".

### 논의·결정

- **원인**: `timeout: DevToolsActivePort` — CI 러너에서 Chrome이 기본 15초(60회 × 250ms) 안에 포트 파일을 쓰지 않음. 빌드는 끝났고 측정 전에 실패해 FAIL 목록·스크린샷 없음. PR #55·#57 둘 다 문서 전용 PR이고 실패 잡 재실행으로 통과 → 코드와 무관한 러너 쪽 지연.
- **결정(사용자)**: `launchChrome`의 DevToolsActivePort 대기만 `tries: 120`(30초). `CDP ready` 등 다른 대기는 그대로. 정상 기동 시 대기 시간은 같고, 정말 안 뜨는 경우만 실패가 15초 늦어진다. decisions/layout-measurement.md 갱신 이력(결정 변경 없음).
- **PR #57 마감**: `layout` 실패 1회 → 실패 잡 재실행 통과(`layout` 1m34s · `verify` 37s · `gitleaks` 10s) → rebase merge(main `e089328`). 수정 브랜치는 그 위로 rebase.

### 검증

- prettier·lint 통과, `cdp.test.mjs` 2/2, 로컬 `verify:layout --no-build` 105/105(로컬은 Chrome이 원래 빨리 떠서 결과 동일 — 효과는 CI에서 확인).
- 커밋(main 해시, PR #58 rebase merge 후 정정): fix `01de33d` · /log `764ba69`.

### 미결·다음

- ~~PR → CI → rebase merge → 해시 정정.~~ → 아래 "PR #58 마감".
- 다음: AN5(행 ⋮ — 대기로/후보로/보류로/지금 실행 노출·배선, 파일 반영은 A6c).

## PR #58 마감 + 해시 정정

- PR #58 CI 초록(`layout` 1m21s · `verify` 53s · `gitleaks` 9s — 30초 대기가 들어간 첫 CI, 타임아웃 없음) → **rebase merge**, 원격·로컬 브랜치 삭제, main 동기화(`764ba69`).
- rebase로 바뀐 main 해시: fix `01de33d` · /log `764ba69`. 위 절·todo 완료 섹션의 브랜치 해시를 main 해시로 정정(이 PR, 별도 PR). 이 절은 자기 커밋 해시를 적지 않는다.

## AN5 행 ⋮ 이동 메뉴 + A6b·A6c 섹션 이동 — feat/queue-row-menu

### 참여

- 메인 세션 단독. 사용자 "응 AN5 진행하자" → 범위 질문 3개에 답(아래 결정).

### 논의·결정

- **범위(사용자 결정)**: **AN5+A6b+A6c 한 PR, 커밋 분리**. 근거: DB는 파생 캐시라 파일에 쓰지 않으면 다음 적재 때 되돌아간다 → "이동"을 동작시키려면 A6b·A6c가 함께 와야 한다. 기각: AN5만(메뉴 노출, 이동 비활성 — 지금 실행도 BM6 전이라 누를 수 있는 항목이 하나도 없음).
- **대기로 옮길 때 위치(사용자 결정)**: **맨 아래**. 기존 대기 순서를 건드리지 않고, 대기 맨 위는 "다음 실행" 자리(layout §4)라 그 자리가 밀리지 않는다. 순서 변경은 A6a DnD. 기각: 맨 위.
- **완료 탭 행(사용자 결정)**: ⋮ **없음**. 되돌리면 완료일(YYYY-MM-DD)이 사라지고 스케줄이 썼던 기록과 어긋난다. 완료 행은 나중에 실행 상세 링크(B1e)만.
- **카테고리(파일 형식에서 따라오는 결과, 선택지 아님)**: 카테고리는 후보 섹션 `###` 소제목이라 후보를 떠나면 사라진다. 반대로 **후보로 옮길 때는 카테고리 없는 구간(첫 ### 앞) 끝**에 넣는다 — 맨 아래에 넣으면 마지막 소제목 카테고리가 붙어 재적재 때 카테고리가 생긴다(파서 확인).
- **동시 변경 방어**: 이동 입력은 `from`·`to`·`index`·`title`. 쓰기 직전 파일을 다시 읽어 그 위치의 제목이 다르면 `TOPIC_MISMATCH`로 아무것도 쓰지 않는다(스케줄·Claude Code가 그새 고친 경우).
- **"지금 실행"**: 실행 화면(BM6·B1e) 전이라 사유와 함께 비활성. 가짜로 동작시키지 않는다.
- **구조**: pipeline `queue/moveQueue.ts`(`moveTopic` 순수 + `moveQueueTopic` 파일 재작성·DB 재적재) / dashboard `lib/queue-move.ts`(BLOG_DIR Storage·문구 변환) · `lib/queue-row-menu.ts`(메뉴 항목·id 파싱, 순수) · `queue/actions.ts` `moveQueueRowAction`(성공 시에만 revalidate) · `QueueRowMenu`(client, action은 prop) · 행 모델에 섹션 내 위치(`index`, 카테고리 필터 전 기준).

### 검증

- pipeline 50→62(순수 이동 10 · 임시 SQLite 통합 2), dashboard 45→61(메뉴 항목 5 · 서버 이동 4 · 액션 2 · 컴포넌트 5 · 뷰 1). lint·typecheck·prettier 통과.
- 픽스처 실측(스크래치 큐 + 임시 SQLite, `next start -p 3977`, CDP 프로브) **10/10 PASS**: 대기→보류(행·빈 상태 문구·URL 불변·새로고침 없음) · 파일에 보류로 들어가고 대기에서 빠짐 · 완료일·안내문 보존 · 후보→대기(카테고리 잃고 대기 맨 아래) · 후보 `###` 프론트에서 빠짐. 스크린샷 육안 확인(행 끝 ⋮, 빈 상태).
- 커밋(main 해시, PR #60 rebase merge 후 정정): pipeline `3b2d1b4` · 액션 `6557b14` · 메뉴 `3792b18` · /log `1968151`.

### 미결·다음

- A6d(편집→파일 반영 무결성 테스트)는 A6a DnD 뒤로 남김 — 이동 경로는 이번 통합 테스트가 덮는다.
- ~~PR → CI → rebase merge → 해시 정정.~~ → 아래 "PR #60 마감".
- 다음: AH1~AH4(홈) 또는 A6a(대기 순서 DnD) — 사용자 선택.

## PR #60 마감 + 해시 정정

- 사용자 "PR 올려" → PR #60 CI 초록(`layout` 1m34s · `verify` 56s · `gitleaks` 6s) → **rebase merge**, 원격·로컬 브랜치 삭제, main 동기화(`1968151`).
- rebase로 바뀐 main 해시: pipeline `3b2d1b4` · 액션 `6557b14` · 메뉴 `3792b18` · /log `1968151`. 위 AN5 절·todo(AN5·A6b·A6c)의 브랜치 해시를 main 해시로 정정(이 PR, 별도 PR). 이 절은 자기 커밋 해시를 적지 않는다.
- **다음 작업(사용자 결정)**: **AH1~AH4 홈 화면**(A6a DnD는 뒤로). AH1(ui StatTile·EmptyState + 갤러리 + verify:layout) → AH2(사이드바 홈 항목·TopBar 링크) → AH3(홈 골격·다음 스케줄·타일 4개) → AH4(다음 실행 카드). AH5·AH6은 B1e·Phase 2 뒤.

## AH1 StatTile·EmptyState — feat/ui-stat-tile-empty-state

### 참여

- 메인 세션 단독. PR #60 머지 뒤 다음 작업 선택지(AH 홈 / A6a DnD)에서 사용자가 **AH**를 고름.

### 논의·결정

- **StatTile**: `label`·`value`·`icon` 슬롯 + `tone` + 링크(`href`, 라우터 링크는 `render`). 링크 요소를 받는 방식은 기존 `SidebarItem`과 동일(cloneElement로 className 병합) — ui는 next를 모른 채 남는다. 타일 자체가 카드 표면(홈 행 1에 grid로 나란히 놓임), 배경 색 블록 없이 **값 색만** 바꾼다, 링크일 때만 우하단 화살표(장식이라 aria-hidden).
- **tone에 `muted` 추가(에이전트 판단, 사용자 확인 필요)**: todo 스펙은 `default|warning`이지만 layout §4에 "0이면 회색"이 있다. ui는 값이 0인지 모르므로 앱이 톤 이름으로 지정하게 했다.
- **새 토큰 `--ui-text-stat: 26px`(에이전트 기본값, 사용자 확인 필요)**: 스펙 숫자 24~28px인데 기존 최대가 `--ui-text-title` 22px이라 하드코딩을 피하려면 토큰이 필요.
- **CardGrid는 만들지 않음**(todo가 "충분하면 만들지 말고 이유 보고"): 타일 4개 배치는 앱 CSS grid 4줄(`repeat(4, minmax(0,1fr))` + gap 토큰)로 끝난다. 갤러리에서 같은 폭으로 붙는 것을 확인. ui에 래퍼를 하나 더 두면 도메인 없는 레이아웃 컴포넌트만 늘어난다.
- **EmptyState**: 한 줄 메시지 + 선택 액션 슬롯(가운데 정렬, 그림 없음). 큐·홈·실행 빈 상태에서 재사용.
- **갤러리 배치**: 새 카드 2장은 "화면 하단" 카드 **앞**에 — 그 카드가 페이지 맨 아래여야 Select·Menu 뒤집힘 실측이 성립한다.

### 검증

- ui 테스트 66→77(StatTile 7 · EmptyState 4), ui build 통과. lint·typecheck·prettier 통과.
- `verify:layout` **105/105 PASS**(갤러리에 카드 2장을 더해도 기존 검사 유지). 스크린샷 육안 확인: 타일 4개 같은 폭, 0은 회색·2는 주황·비용 "—", 링크 타일만 화살표, 긴 라벨 한 줄 말줄임, EmptyState 메시지·버튼 가운데.
- 커밋(main 해시, PR #62 rebase merge 후 정정): ui `eb06069` · 갤러리 `14b6760` · /log `9fcdb93`.

### 미결·다음

- ~~**사용자 확인**: tone `muted` 추가 · 토큰 `--ui-text-stat` 26px · CardGrid 생략.~~ → 2026-09-12 설명 후 사용자 "AH1 3건은 그대로 진행"으로 **확정**, decisions/layout.md 갱신 이력에 기록.
- ~~PR → CI → rebase merge → 해시 정정.~~ → 아래 "PR #62 마감".
- 다음: AH2(사이드바 홈 항목 + TopBar Galley→/ 링크, verify:layout 대상).

## PR #62 마감 + 해시 정정

- 사용자 "PR 올려" → PR #62 CI 초록(`layout` 1m31s · `verify` 56s · `gitleaks` 10s) → **rebase merge**, 원격·로컬 브랜치 삭제, main 동기화(`9fcdb93`).
- rebase로 바뀐 main 해시: ui `eb06069` · 갤러리 `14b6760` · /log `9fcdb93`. 위 AH1 절·todo AH1의 브랜치 해시를 main 해시로 정정(이 PR, 별도 PR). 이 절은 자기 커밋 해시를 적지 않는다.
- AH1의 확인 대기 3건(tone `muted` · 토큰 `--ui-text-stat` 26px · CardGrid 생략)은 이 시점까지 미답이었다 → 이후 사용자가 "3번 다시 설명해줘"라고 해 풀어 설명했고, **2026-09-12 "그대로 진행"으로 확정**(decisions/layout.md).
- AH2 준비 조사(이번 세션): 사이드바 메뉴는 `lib/navigation.ts` 그룹 배열 하나 → 홈은 `SidebarGroup`(라벨 필수) 밖에서 `SidebarItem`만 렌더해야 한다. 구분선은 ui에 없음(앱 CSS vs ui 패턴은 착수 시 결정). TopBar 워드마크는 `<span>` → `<Link href="/">`, 스타일에 색·밑줄 지정이 없어 `color: inherit`·`text-decoration: none` 필요.

## AH2 사이드바 홈 항목 + TopBar 홈 링크 — feat/dashboard-home-nav

### 참여

- 메인 세션 단독. 사용자가 "PR #63 머지 뒤 AH2 진행"을 승인.

### 논의·결정

- **홈은 그룹 밖 단독 항목**: `NAV_GROUPS`와 별도로 `HOME_ITEM`을 export하고 Sidebar가 맨 위에 렌더. `SidebarGroup`은 라벨이 필수라 그룹으로 만들면 "홈"이 그룹 라벨과 항목으로 두 번 나온다.
- **구분선은 앱 CSS**(`Sidebar.module.css`): 한 줄뿐이라 `@galley/ui`에 컴포넌트를 두지 않았다. 여백은 `SidebarGroup`과 같은 토큰 값으로 맞춤.
- **TopBar 워드마크 → `<Link href="/">`**: 스타일에 색·밑줄 지정이 없어 링크가 되면 파란 밑줄이 생긴다 → `color: inherit`·`text-decoration: none` + 다크 배경용 포커스 링 추가.
- 활성 판정은 기존 `isNavItemActive`(정확히 일치)라 홈은 루트에서만 켜진다.

### 검증

- dashboard 테스트 61→63(navigation 홈 활성 1 · Sidebar 홈 항목 1). lint·typecheck·prettier 통과.
- `verify:layout` 처음 **104/105** — "[항목 40개] 열릴 때 선택 항목이 보임" 실패. 재실행에서도 같은 실패 → 프로브로 수치를 재서 **제품 회귀가 아니라 검사 타이밍 결함**임을 확인: Base UI는 팝업을 연 뒤 **비동기로** 선택 항목까지 스크롤한다(직후 `scrollTop` 0·선택 항목 top 1841 → 잠시 뒤 `scrollTop` 1484·top 357, 팝업 bottom 411). 목록이 보이자마자 재던 `select.mjs`가 페이지가 길어지자(AH1 카드 2장·AH2 홈 항목) 스크롤 완료 전에 측정. 스크롤이 멈출 때까지 기다리게 고친 뒤 **105/105**.
- 스크린샷: 사이드바 맨 위 홈 항목 + 구분선 확인.
- 커밋(main 해시, PR #64 rebase merge 후 정정): 실측 수정 `0b171e7` · AH2 `38dd8e7` · /log `20c3b43` · AH1 확정 기록 `8e564b4`.

### 미결·다음

- ~~AH1 확인 대기 3건은 여전히 미답~~ → 사용자 "AH1 3건은 그대로 진행" **확정**. decisions/layout.md 갱신 이력에 이유까지 기록(이 PR에 포함).
- ~~PR → CI → rebase merge → 해시 정정.~~ → 아래 "PR #64 마감".
- 다음: AH3(홈 골격 — `app/(dashboard)/page.tsx`, 다음 스케줄 시각 유틸, StatTile 4개. 기존 `app/page.tsx` redirect 삭제).

## PR #64 마감 + 해시 정정

- 사용자 "AH1 3건은 그대로 진행. PR올려" → AH1 확정을 decisions/layout.md에 기록한 커밋을 같은 PR에 넣고 올림 → CI 초록(`layout` 1m16s · `verify` 57s · `gitleaks` 7s) → **rebase merge**, 원격·로컬 브랜치 삭제, main 동기화(`8e564b4`).
- rebase로 바뀐 main 해시: 실측 수정 `0b171e7` · AH2 `38dd8e7` · /log `20c3b43` · AH1 확정 기록 `8e564b4`. 위 AH2 절·todo(AH2·실측 항목)의 브랜치 해시를 main 해시로 정정(이 PR, 별도 PR). 이 절은 자기 커밋 해시를 적지 않는다.
- **AH3 준비 조사**(이번 세션): `app/(dashboard)/layout.tsx`는 서버 컴포넌트라 카운트를 조회해 `Sidebar`(client)에 prop으로 넘길 수 있다 — 지금 배지는 `NAV_GROUPS` 정적 값이라 비어 있다. `lib/nav-counts.ts`에는 승인 대기 더미(0)뿐이라 대기 n은 `getQueueSections`에서 와야 한다. `.env.example`에 TZ 키 없음. 루트 `app/page.tsx`는 아직 AN3 redirect.
- **AH3 착수 시 물을 것 3건**: 사이드바 배지까지 이번에 채울지(완료조건이 "타일=배지 동일") · TZ 키 이름·기본값 · 데이터 없는 타일(발행 대기·이번 달 비용)을 `0`으로 둘지 `"—"`로 둘지.

## AH3 홈 페이지 골격 — feat/dashboard-home

### 참여

- 메인 세션 단독. 사용자 결정 4건: **AH3 진행** · **사이드바 배지도 함께** · 시간대 키 **`TZ=Asia/Seoul`** · 데이터 없는 타일은 **`0`으로 표시**(에이전트는 "—"를 추천했으나 사용자가 0 선택).

### 논의·결정

- **루트 `/` = 홈**: 기존 `app/page.tsx`(AN3 분기 redirect)를 삭제하고 `app/(dashboard)/page.tsx` 신설 — decisions/navigation.md 2026-09-09 결정대로.
- **스케줄 유틸**(`lib/schedule.ts`): 수·토 18:00 중 가까운 쪽. **시간대를 인자로 받아** `Intl`로 그 지역 요일·시·분을 구하고 남은 분을 가산한다(Asia/Seoul은 DST 없음). 정각이면 "이미 시작한 것"으로 보고 다음 회차. 스케줄 소유자는 기존 blog 쪽이라 pipeline이 아니라 앱 유틸(decisions/ondemand-execution.md).
- **카운트 한 소스**(`lib/nav-counts.ts` 확장): 홈 타일과 사이드바 배지가 같은 함수를 쓴다 → 숫자가 어긋나지 않는다. 대기만 실데이터(`getQueueSections`), 승인 대기·발행·비용은 0(B1e·Phase 2 전).
- **배지 배선**: 레이아웃(서버 컴포넌트)에서 조회해 `Sidebar`에 **경로별 배지 맵**을 넘긴다. `NAV_GROUPS` 정의는 건드리지 않고, 0은 표시하지 않는다.

### 검증

- dashboard 테스트 63→70(schedule 4 · nav-counts 2 · Sidebar 배지 1). lint·typecheck·prettier 통과. `verify:layout` 105/105.
- 픽스처 실측(홈 프로브) **8/8 PASS**: h1 "홈" · 루트가 redirect되지 않음 · "다음 자동 실행: 토 18:00" · 타일 4개·라벨 4종 · 대기 타일→`/queue?tab=waiting` · **대기 타일 값 = 사이드바 큐 배지** · 0인 배지는 미표시. 스크린샷 육안 확인.
- 함정 2건: (1) `typecheck`가 옛 `.next/types/validator.ts`를 봐서 삭제한 `app/page.tsx` 참조 오류 → 빌드 갱신 뒤 통과(코드 문제 아님). (2) 프로브가 타일 컨테이너(`page_tiles_*`)까지 타일로 세어 FAIL 2건 → 필터에서 컨테이너 제외.
- **커밋 단위 사고**: `git rm`으로 스테이징해 둔 루트 redirect 삭제가 첫 커밋(TZ)에 섞였고, `git add` 실패로 홈 커밋이 빠지며 삭제가 배지 커밋에 딸려 들어갔다 → push 전이라 `git reset` 후 4커밋으로 재구성(각 커밋 name-status 확인).
- 커밋(main 해시, PR #66 rebase merge 후 정정): TZ `cf2dc5a` · 유틸 `2f6fb92` · 홈 `6a2faa1` · 배지 `65787ed` · /log `9b22be1`.

### 미결·다음

- ~~PR → CI → rebase merge → 해시 정정.~~ → 아래 "PR #66 마감".
- 다음: AH4(홈 "다음 실행" 카드 — 대기 맨 위 1개 크게 + 2~4위 작은 행, `지금 실행`은 자리만, 빈 상태 EmptyState).

## PR #66 마감 + 해시 정정

- 사용자 "머지하자"(CI 통과 시 머지까지 승인) → PR #66 CI 초록(`layout` 1m17s · `verify` 38s · `gitleaks` 6s) → **rebase merge**, 원격·로컬 브랜치 삭제, main 동기화(`9b22be1`).
- rebase로 바뀐 main 해시: TZ `cf2dc5a` · 유틸 `2f6fb92` · 홈 `6a2faa1` · 배지 `65787ed` · /log `9b22be1`. 위 AH3 절·todo AH3의 브랜치 해시를 main 해시로 정정(이 PR, 별도 PR). 이 절은 자기 커밋 해시를 적지 않는다.
- **AH4 준비 조사**(이번 세션): 스펙(layout §4 행 2) "다음 실행" = 대기 맨 위 1개 크게(제목·카테고리·근거 리포 + `지금 실행`·`큐 편집`→/queue) + 2~4위 작은 행 최대 3 + 빈 상태("대기 중인 주제가 없습니다. 후보에서 골라 주세요." + `후보 보기`→/queue?tab=candidates). 데이터는 `getQueueSections` 그대로, 작은 행은 `ListRow`, 빈 상태는 AH1 `EmptyState` 재사용 가능.
- **AH4 착수 시 물을 것 3건**: 행 2 좌우 분할(좌 "지금 할 일"은 AH5로 보류 — 틀을 미리 둘지 "다음 실행"만 전체 폭으로 둘지) · `지금 실행` 버튼을 큐 행 ⋮처럼 사유 단 비활성으로 둘지 · 근거 리포는 Phase 1-B 데이터라 지금은 카테고리만 표시할지.

## AH4 홈 "다음 실행" 카드 — feat/dashboard-next-run

### 참여

- 메인 세션 단독. 사용자 결정 4건: **AH4 진행** · **"다음 실행"만 전체 폭**(좌우 분할은 AH5) · `지금 실행`은 **비활성+사유** · 보조 정보는 **카테고리만**.

### 논의·결정

- **카드 구성**: 대기 맨 위 1개 크게(제목·카테고리 + `지금 실행`·`큐 편집`) + 2~4위 작은 행 최대 3(`ListRow`) + 빈 상태(`EmptyState` + `후보 보기`→`/queue?tab=candidates`). 모델은 순수 함수 `lib/next-run.ts`, 카드는 표현 전용 컴포넌트(데이터는 홈이 주입) → 둘 다 단위 테스트 가능.
- **ui `Button`에 `render` 추가**: `큐 편집`·`후보 보기`는 버튼 모양 **링크**여야 하는데 Button이 `<button>` 전용이었다. `SidebarItem`·`StatTile`과 같은 방식(cloneElement·className 병합)으로 맞추고 갤러리에 예시 추가. 링크로 쓰면 브라우저 기본 밑줄이 생겨 `text-decoration: none`도 버튼 스타일에 넣었다(스크린샷에서 발견).
- **큐 1회만 읽기**: 홈이 `getQueueSections()`를 한 번 호출해 타일 카운트(`getNavCounts(preloaded)`)와 카드가 나눠 쓴다 → 한 요청에서 파일→DB 재적재가 두 번 일어나지 않는다.

### 검증에서 드러난 결함(중요)

- 픽스처 실측에서 홈 카드가 **픽스처가 아니라 실제 blog 주제**를 보여 줬다. 확인 결과 같은 서버의 `/queue`는 픽스처를 제대로 읽고 있었고, `process.loadEnvFile`도 셸 env를 덮어쓰지 않았다(둘 다 직접 검증). 원인은 **Next가 홈과 셸 사용 라우트를 정적 프리렌더**한 것 — `prerender-manifest.json`에 `/`·`/runs`·`/publish`·`/settings/*`가 들어 있었다. 빌드 시점 큐 값이 HTML에 굳어, 주제_큐.md를 고쳐도 화면이 그대로였다.
- **AH3의 허점**: 단위 테스트와 1회 실측이 모두 빌드 시점 값과 우연히 일치해 놓쳤다. AH4 완료조건("큐 순서를 바꾸면 홈도 바뀐다")이 이를 잡았다.
- 수정: `app/(dashboard)/layout.tsx`에 `export const dynamic = 'force-dynamic'` — 셸을 쓰는 라우트 전부가 요청마다 렌더된다. 재빌드 후 프리렌더 목록에 오류 페이지만 남고 홈·설정·발행이 모두 동적(ƒ)으로 바뀌었다. 별도 `fix` 커밋으로 분리.
- **실제 blog 파일 안전 확인**: 프로브의 쓰기 대상은 픽스처 경로뿐이었고, `~/Desktop/blog/주제_큐.md`의 수정 시각(10:19)이 작업 시각(12:30~40)보다 앞서며 대기 섹션도 원래 1줄 그대로였다.

### 검증

- ui 77→79(Button render 2), dashboard 70→81(next-run 5 · NextRunCard 5 · nav-counts preloaded 1). lint·typecheck·prettier 통과. `verify:layout` 105/105.
- 픽스처 실측(다음 실행 프로브) **10/10 PASS**: 카드 존재 · 맨 위 크게 · 2~4위 작은 행 · 중복 없음 · `지금 실행` 비활성+사유 · `큐 편집`→`/queue?tab=waiting` · **큐 순서를 바꾸면 홈도 바뀜**(A→B) · 빈 상태 문구·`후보 보기`·실행 버튼 없음. 스크린샷 육안 확인(밑줄 수정 전/후).
- 커밋(main 해시, PR #68 rebase merge 후 정정): ui `b801ac4` · 갤러리 `7b6b92e` · 셸 동적 `eb1e36b` · 카드 `5b310a2` · /log `62255f5`.

### 미결·다음

- ~~PR → CI → rebase merge → 해시 정정.~~ → 아래 "PR #68 마감".
- **Phase 1-A 홈(AH1~AH4) 완료.** 다음 후보: A6a(대기 순서 DnD) · AH5·AH6은 B1e·Phase 2 뒤 · Phase 1-B 진입(B1e Run 스키마 등)은 사용자 선택.

## PR #68 마감 + 해시 정정

- 사용자 "머지해줘" → PR #68 CI 초록(`layout` 1m27s · `verify` 53s · `gitleaks` 9s) → **rebase merge**, 원격·로컬 브랜치 삭제, main 동기화(`62255f5`).
- rebase로 바뀐 main 해시: ui `b801ac4` · 갤러리 `7b6b92e` · 셸 동적 `eb1e36b` · 카드 `5b310a2` · /log `62255f5`. 위 AH4 절·todo(AH4·셸 fix 항목)의 브랜치 해시를 main 해시로 정정(이 PR, 별도 PR). 이 절은 자기 커밋 해시를 적지 않는다.
- **오늘 누적**: PR #54~#68 머지. UM2(Menu) · AN7(큐 수동 갱신) · AN5+A6b+A6c(행 ⋮ 섹션 이동) · AH1~AH4(홈) + 실측 스크립트 fix 2건(Chrome 기동 30초, Select 팝업 스크롤 대기) + 셸 정적 프리렌더 fix.
- **다음 작업은 사용자 선택 대기**: A6a(대기 순서 DnD — pragmatic-drag-and-drop 신규 설치, 순서 저장은 A6c 확장, 끝나면 A6d 무결성 테스트까지 닫힘) vs Phase 1-B 진입(B1e Run 스키마 — 🔒 B1a·핵심 모듈은 사용자 몫, BE1은 "BE1 시작" 승인 후).
