# 2026-09-22

## PR #119 마감 · BE3 착수

- PR #119(BE2 식별 정보 필터) CI 4잡 통과 → 사용자 지시로 rebase merge(main `b30c469`). main 해시: 필터 `45e8710` · 예시+gitignore `78ad8e2`(docs `0c17fec`·`b30c469`). worklog 09-21 BE2 절과 todo BE2의 해시 정정(이 브랜치 feat/repo-indexer 첫 커밋).
- 사용자 "오늘은 여기까지" 뒤 "한 단계만 더" → **BE3 리포 인덱서 1 착수 승인**.

## BE3 리포 인덱서 1 — feat/repo-indexer

참여: 메인 에이전트(구현) · reviewer(리뷰) · 사용자(결정 "추천대로").

### 구현 (커밋 6 + docs 1 — 해시는 main 기준, PR #120 rebase 후 정정)

- `2b21d1a` `limits.ts`(INDEX_LIMITS·INDEX_IGNORE·OUTPUT_LIMITS) + `tree.ts` `planAreas`: 영역 = 최상위 디렉터리, `filesPerArea`를 넘고 하위가 있으면 한 단계 아래로 재귀(모노레포 `packages/*`), 루트는 `area:.`, 상한 안 선택은 소스 확장자·작은 파일 우선, 초과분 이름만(summaryOnly). 코드포인트 정렬(로케일 무관). 테스트 12.
- `f3d1523` `gitRead.ts`: `rev-parse`·`ls-tree -r -l -z --full-tree`·`show`만. 커밋 인자 형식 검증(hex/HEAD) + `--end-of-options`, `LC_ALL=C`(한국어 로케일 메시지 "깃 저장소가 아닙니다"로 판별이 깨졌음), `GIT_DIR` 등 env 제거, 심볼릭 링크 제외, NUL 바이너리 판별. 테스트 8(픽스처 리포).
- `80bb2b8` RepoAnalysis `filtered`·`redacted` 컬럼(마이그레이션 `repo_analysis_filtered`).
- `489da00` `areaAnalysis.ts` `analyzeArea`: 한국어 JSON 지시 프롬프트 → 코드 펜스 관용 파싱 → title·summary·keywords·note 전부 redact → 포인터는 실제 읽은 파일만·commit 채움·라인 정렬·clamp·범위 밖 버림, 없으면 읽은 파일 전체, 읽은 파일도 없으면 계획 파일 전체(라인 없음, summaryOnly). `MODEL_OUTPUT_INVALID`(usage 포함) vs `MODEL_FAILED`(errorName) 구분. `src/model/testing/scriptedAdapter.ts`(테스트 전용). 테스트 9.
- `a24feee` `repoAnalysisRepo.ts` `upsertRepoAnalysis`(JSON 컬럼 쓰기 유일 자리, (repoId,key) upsert) + `indexRepoAreas.ts` 오케스트레이터: HEAD → 목록 → 영역 → `git show` → analyze → upsert. readOnly + 설정 없음 → `REDACT_CONFIG_REQUIRED`, 깨진 출력은 `skipped`(과금 합산), 모델 호출 실패는 중단(partial report), `skipKeys` 재개, `onAreaDone(status·analysisId·usage)`. **Repo 상태·headSha는 쓰지 않음**(BE5 실행자 몫), report는 수치만. 통합 테스트 8(픽스처 git + 임시 SQLite): 저장된 모든 포인터를 `git show`로 열어 존재·줄 수 검증, `.env`가 프롬프트에 안 들어감, 리포 상태 불변.
- `b2af8d5` decisions/evidence-collection 갱신 이력 12건.

### 리뷰 — high 3 · med 9 · low 15 → 사용자 "추천대로"

- **H1** keywords가 redact를 안 거침 → 전부 적용 · **H2** 커밋 인자 옵션 주입(`--output=<경로>`로 파일 쓰기 실측) → 형식 검증 + `--end-of-options`(rev-parse는 인자가 상수라 제외 — git 2.49가 플래그를 출력에 섞음) · **H3** `MODEL_FAILED`가 skipped로 흡수돼 "분석 글 0개 ready 리포"가 남음 → 중단·실패 값.
- **M1** show 실패 파일이 include로 남음 → contents가 진실 · **M2** 라인 범위 상한 → clamp/버림 · **M3** skipped 영역 과금 합산 · **M4(결정)** Repo 상태 소유자 = BE5 실행자(인덱서는 report만) · **M5(결정)** 본문 못 넘긴 영역은 파일 전체 포인터로 summaryOnly 글 · **M6** `INDEX_IGNORE`에 비밀값 파일(.env 계열·키·인증서·자격 증명) · **M7(결정)** `skipKeys` + 콜백 정보 지금 추가 · **M8** `--full-tree` · **M9** 종단 포인터 검증·경계·MODEL_FAILED·유니코드·심볼릭 링크·리포 불변 테스트.
- **L1** 심볼릭 링크 제외 · **L2** localeCompare → 코드포인트 · **L3** ignore는 디렉터리 조각만 · **L4** NUL 바이너리 · **L5** 키워드 20개·40자, 제목 200자 · **L6** 펜스 실패 시 전체 재시도 · **L7** errorName · **L9(결정)** report·로그에 경로 없음(describeTree는 CLI 전용) · **L10** GIT_DIR 등 제거 · **L11** 테스트 `GIT_CONFIG_GLOBAL=/dev/null` 격리 · **L12** 스크립트 어댑터 `src/model/testing/`으로.
- 기록만(BE5): **L8** 두 쿼리 created 판정 · **L13** 사라진 영역 고아 행 정리 · **L14** 성능 · **L15** 마이그레이션 타임스탬프(09-21 UTC).
- 작업 중: fixup 스테이징 때 `git mv`로 미리 스테이지된 이동이 앞 fixup에 묶여 soft reset 후 다시 묶음 · 끝 개행 파일의 줄 수(마지막 빈 조각 제외) · `toMatchObject`의 `undefined` 키는 "키 없음"과 다름.

### 검증

- pipeline **340**(+38) · ui 329 · dashboard 249 · lint · typecheck. 워커 런타임 import 확인(reviewer).

### 미결·다음

- ~~PR → CI → 사용자 승인 뒤 rebase merge~~(아래 PR #120 마감) → **BE4** 인덱서 2(`git log` 기간·경로 묶음 → change 분석 글 + overview, IndexJob 토큰·비용) → BE5(증분·CLI·IndexJob 실행자: Repo 상태·progressCursor·고아 행).

## PR #120 마감 · BE4 착수

- PR #120(BE3 리포 인덱서 1) CI 통과 → 사용자 지시로 rebase merge(main `7d360fd`). main 해시: limits+tree `2b21d1a` · gitRead `f3d1523` · 컬럼 `80bb2b8` · analyzeArea `489da00` · 오케스트레이터 `a24feee` · decisions `b2af8d5` · docs `7d360fd`. 위 BE3 절과 todo BE3의 해시 정정(이 브랜치 feat/repo-indexer-changes 첫 커밋).
- 사용자 "다음 작업 시작하자" → **BE4 리포 인덱서 2 착수 승인**.

## BE4 리포 인덱서 2 — feat/repo-indexer-changes

참여: 메인 에이전트(구현) · reviewer(리뷰) · 사용자(결정 "그대로 하자" = 추천대로).

### 구현 (커밋 6 + 리뷰 수정 4 + docs 1 — 해시는 main 기준, PR #121 rebase 후 정정)

- `698b3d6` `gitLog`(gitRead.ts): `-z --no-merges --no-renames` + 커밋 형식 검증·`--end-of-options`·`from..to`·`maxCommits`. 이름 바꿈은 A+D로 풀어 경로가 항상 커밋에 실존, 삭제 파일 포인터용 첫 부모 `parentSha`.
- `94b681e` `changes.ts` `planChangeBatches`: 작성 월(author date)로 묶고 `commitsPerBatch` 초과 시 주 디렉터리(`primaryDir`)로 분할(`change:<월>[:<dir>]`), 그래도 초과면 앞쪽만 상세 → summaryOnly. `pointerCandidates`(삭제는 부모 기준). limits에 `commitBodyChars`·`filesPerCommit`·`maxCommits`.
- `28eb236` 리팩토링: JSON 추출·redact·제목·키워드 정리를 `analysisText.ts`로(area 동작 불변).
- `ee83a7e` `analyzeChange`: 커밋 메시지·파일 상태 목록 프롬프트(diff 없음), 포인터는 묶음 안 커밋의 실제 변경 파일만(해시 접두 해석, 라인 없음), 폴백은 상세 커밋마다 살아 있는 파일 하나.
- `179b6ab` `analyzeOverview`: area·change 글의 제목·요약·키워드 입력(`overviewSources` 120), 출처 글의 첫 포인터 상속.
- `97cef4e` `indexRepoChanges`(HEAD → log → 묶음 → analyze → upsert, `fromSha` 증분·`skipKeys`·`onBatchDone`) + `indexRepoOverview`(DB 읽기 → analyze → `overview` upsert, `NO_SOURCES`). 통합 테스트 6(픽스처 git + 임시 SQLite): 저장된 모든 포인터를 `git show`로 열어 실존 검증, Repo 상태 불변, 리포 git status 불변.
- `54a8054` decisions/evidence-collection 갱신 이력 BE4 9건 + 본문 "증분"·"식별 정보 필터" 줄 보강.

### 리뷰 — high 2 · med 6 · low 13 → 사용자 "그대로 하자"(추천대로)

- **H1** 커밋 본문에 `\x1e`가 있으면 가짜 커밋·가짜 포인터(실측) → `rev-list` sha 목록으로 레코드 대조, 불일치 조각은 앞 레코드 본문으로 · **H2** 서브모듈 경로가 포인터가 되고 `git show`로 안 열림(실측) → `--raw`로 바꿔 mode 160000·120000 제외(**M1** T→링크 포인터도 해결) — `f6dba72`(+L1 `bad object` 패턴·L2 `emptyCommits` 분리·L3 `truncated` 정확 판정·L8 NaN).
- **M6** 해시 접두 다중 매치 → 순회 `0afb0f4`.
- **M4** report `skipped: string[]`에 디렉터리명 → 수치로, BE3 `IndexAreasReport`까지 함께(사용자 결정) + **L5** `MAX_POINTERS`·`maxOutputTokens`를 `OUTPUT_LIMITS`로 `170dace`.
- **M5(결정)** 커밋 트레일러(사내 이메일·이슈 URL)가 모델에 감 → 마지막 문단 전체가 트레일러일 때 뗀다(`stripTrailers`), "모델 입력은 redact하지 않고 밀도 높은 조각을 뺀다" 원칙을 decisions에 명시 + **L6** 공허한 `.env` 내용 검사 → 경로 검사 `379a650`.
- **M2(결정)** 증분에서 change 키 충돌(같은 달 일부만 읽으면 덮어쓰기·중복) → 선택지 ①(BE5 실행자가 달 단위로 다시 읽고 그 달 행 삭제, 키 형식 유지) 채택. 코드 주석·decisions·todo BE5에 기록.
- 기록만(BE5 또는 다음 결정): **M3** 월 분할이 디렉터리 수만큼 쪼개짐 · **L4** 오프셋 다른 커밋 사전순 · **L7** overview 출력 불량 시 옛 행 잔존 · **L9** 커밋 본문 프롬프트 주입 · **L10** 서로게이트 절단 · **L11** `.gitmodules`를 INDEX_IGNORE에 · **L12** 테스트 누락 목록 · **L13** BE3 이월 항목 변화 없음.
- 작업 중: zsh에서 `echo ======`가 `=cmd` 확장으로 실패(구분선은 따옴표) · 테스트 실패인데 `;` 체인으로 커밋된 건 amend로 정정 · 4자 미만 해시 접두는 무효라 테스트 픽스처를 4자로.

### 검증

- pipeline **373**(+33) · ui 329 · dashboard 249 · lint · typecheck. 워커 런타임 import 확인(`node --input-type=module`로 배럴 로드).

### 미결·다음

- ~~PR → CI → 사용자 승인 뒤 rebase merge~~(아래 PR #121 마감) → **BE5**(증분·`--full`·CLI `bin/index.ts`·IndexJob 실행자: Repo 상태·headSha·progressCursor·토큰·비용·고아 행·change는 달 단위 재읽기).

## PR #121 마감 · BE5 착수

- PR #121(BE4 리포 인덱서 2) CI 4잡 통과 → 사용자 지시로 rebase merge(main `bced52c`). 위 BE4 절과 todo BE4의 해시 정정(이 브랜치 feat/index-job-runner 첫 커밋).
- 사용자 "BE5 시작하자" → **BE5 증분 재인덱싱·IndexJob 실행자·CLI 착수 승인**.

## BE5 증분 재인덱싱·IndexJob 실행자·index CLI — feat/index-job-runner

참여: 메인 에이전트(구현) · reviewer(리뷰) · 사용자(결정 "응" = 추천대로).

### 구현 (커밋 7 + 리뷰 수정 5 + docs 2 — 해시는 main 기준, PR #122 rebase 후 정정)

- `452cb4f` 인덱서 옵션: `planExecution`(batchControl.ts) 한 곳이 area·change의 배치 선택 — `skipKeys`/`resumeAfterKey`(재개)·`unchangedKeys`(증분)·`maxBatches`(틱마다 하나). `gitDiffPaths`(읽기만)·`areaKeyForPath`(가장 긴 디렉터리 접두). 결과에 `plannedKeys`(report 밖).
- `2255124` `pruneAnalyses`(kind별 keepKeys 밖 삭제 — 사라진 영역·옛 묶음, 연결은 cascade).
- `3cde2ca` `indexJobRepo.ts`(IndexJob·Repo 상태 쓰기 유일 자리: 클레임 조건부 updateMany·heartbeat 회수·진행·완료 트랜잭션) + `runIndexTick.ts`(한 틱 = 배치 하나, 커서 `<단계>:<키>`, 영역→변경→개요, 단계 끝에 prune). 통합 테스트 7.
- `0c82b66` `runOnce`가 Run이 없을 때만 `indexer.tick`(Run 우선, signal 전달) + `bin/worker.ts` 배선(레지스트리·필터 기동 시 한 번).
- `a1a5953` `enqueueIndexJob`(등록+큐잉 한 곳 — CLI·Phase 2 Dialog 공유: full/incremental/작업 없음 판정, 활성 중복·이름 충돌 거부) + `bin/index.ts` CLI(인자 파싱·레지스트리 검증·출력만, 실행은 워커) + 스모크.
- `b214855` `.gitmodules`를 INDEX_IGNORE에(BE4 L11 이월).
- `76d3f90`·`988203c` decisions BE5 7건 + 리뷰 반영.

### 리뷰 — high 2 · med 4 · low 13 → 사용자 "응"(추천대로)

- **H1** CLI `--read-only` 기본 false가 기존 readOnly 리포를 false로 되돌려 필터 필수 가드가 풀림 → 플래그가 있을 때만 true + **M3** 활성 작업 검사를 쓰기 앞으로·나머지 트랜잭션(실패면 리포 불변) + **L10** 상대경로는 `INIT_CWD` 기준(+L9 stale은 HEAD 다를 때만) `0ae7162`.
- **H2** 틱마다 지금 HEAD로 계획·완료 시 HEAD 기록 → 작업 중 커밋이 들어오면 영역이 섞이고 다음 enqueue가 못 잡음 → 선택지 ① 채택: 인덱서 `commit` 입력, 클레임 틱이 `toSha` 고정(`pinIndexJobCommit`), diff·완료(`Repo.headSha = toSha`)도 그 기준(+L8) `81b4b76`.
- **M1** 회수 뒤 옛 워커의 늦은 쓰기가 새 워커 기록을 덮어씀 → beat·진행·반환·완료 전부 `{id, workerId, running}` 조건 updateMany, 0건이면 `lost`(Repo 불변), 사용량은 increment(+L7 부분 과금·L13 행 삭제 시 heartbeat 예외) `5587762`.
- **M2** 영역 분할이 바뀌면 행 없는 키가 unchanged로 남고 부모 행이 prune돼 글 소실 → unchanged는 저장된 키와 교차(`listAnalysisKeys`) `cbef7d9`.
- **M4** 깨진 필터 설정(INVALID·UNREADABLE)을 "없음"과 같게 다룸(BE2 결정 위반) → 워커 기동 거부 exit 1·CLI exit 2 + **L5** `INDEX_UNEXPECTED` → `INDEX_TICK_FAILED` + **L6** heartbeat 상수는 runOnce 한 곳 `774b54c`.
- **L11(확인)** `bin/worker.ts`의 레지스트리 import는 조립 루트 예외로 decisions에 기록.
- 기록만: **L1** 커서 키가 계획에 없으면 처음부터 · **L2** 틱마다 재계획 비용(커밋 고정으로 인메모리 캐시 가능) · **L3** Run 도는 동안 자기 IndexJob 자기 회수(워커 1개면 틱 2개 낭비) · **L4** 틱 시작 시 abort면 반환 없이 idle(Run과 동일) · **L12** 테스트 누락 목록(H·M 수정과 함께 대부분 추가) · **L13** enqueue 동시 호출 unique 없음 · Run 쪽 `finishStep`도 소유 조건 없음.
- 작업 중: prettier가 새 파일을 재정렬해 두 번째 패치의 문자열 매칭이 빗나감(파일을 통째로 다시 쓰거나 현재 본문을 먼저 확인) · 병렬 Bash 호출 사이에 cwd가 리셋돼 상대경로 명령이 실패(절대경로 사용) · readOnly 픽스처를 없는 경로로 만들어 두면 H2 뒤로는 git 실패가 먼저 남.

### 검증

- pipeline **398**(+25) · 스모크 4(워커 2·CLI 2) · ui 329 · dashboard 249 · lint · typecheck.

### 미결·다음

- ~~PR → CI → 사용자 승인 뒤 rebase merge~~(아래 PR #122 마감) → **BE6**(주제_큐.md 괄호 힌트 파서 확장) 또는 BE7(자동 연결). 이월: BE4 M3(월 분할 잘게 쪼개짐 — 결정 필요)·L7(개요 옛 행)·BE5 L1~L4.

## PR #122 마감 · BE6 착수

- PR #122(BE5) CI 4잡 통과 → 사용자 지시로 rebase merge(main `6956ad5`). 위 BE5 절과 todo BE5의 해시 정정(이 브랜치 feat/queue-hint-parser 첫 커밋).
- 사용자 "BE6 시작하자" → **BE6 주제_큐.md 괄호 힌트 파서 확장 착수 승인**.

## BE6 주제_큐.md 괄호 힌트 파서 — feat/queue-hint-parser

참여: 메인 에이전트(구현) · reviewer(리뷰) · 사용자(결정: 기간 여러 개 "그런 건 없어" → 현행 유지, 나머지 "ㅇㅇ" = 추천대로).

### 구현 (커밋 2 + 리뷰 수정 2 + docs 2 — 해시는 main 기준, PR #123 rebase 후 정정)

- `cf3215b` `topicHints.ts`: `parseTopicHints`(괄호 묶음 전부 → 항·기간)·`resolveTopicHints`(Repo 이름·alias로 리포/키워드 판별). 파일 파서(`queueFile.ts`) 불변.
- `945092b` 적재 연결: `parsedQueueToRows(parsed, repos)`가 `QueueItem.repoNames`·`keywords`·`period`를 채운다(파일 → DB 경계). 제목은 힌트까지 원문. `runImport`가 Repo 목록을 읽어 넘긴다. 힌트만 고쳐도 같은 id·컬럼 재생성(DB 테스트).
- `659afef`·`2d84e28` decisions BE6 4건 + 리뷰 반영(⚠️ 결정 변경 1건: 기간 원문 → 정규형).

### 리뷰 — high 1 · med 4 · low 10 → 사용자 결정 뒤 추천대로

- **1(H)** `/`가 항·기간 구분자 둘 다라 `(2024/12)`가 기간이 못 되고 A/B·경로·버전이 쪼개짐 → 항 구분자에서 제거(기간은 `.`·`-`, `2024/12`는 키워드) + **3** 월 1~12·연 19xx/20xx 검증 + **4** Repo 조회 `createdAt` 순(alias 충돌 결정적) + **14** 테스트(슬래시·빈/짝 안 맞는/중첩 괄호·기간 둘·월 범위·alias 충돌·한글 alias NFC) `3821ace`.
- **2(결정 변경)** 기간 원문 저장 → 정규형 `YYYY`·`YYYY-MM`·`YYYY-MM~YYYY-MM`(`normalizePeriodHint`) — RepoAnalysis.period(`YYYY-MM`)과 바로 비교, 범위 전개는 BE7 `bb9fc53`.
- **5(결정)** 기간이 둘이면 둘째는 버림 — 사용자가 실제 큐에 그런 줄이 없다고 확인 → 현행 유지, "여러 달은 범위로" 기록.
- 기록만: **6** 중첩 괄호 · **7** 본문 괄호 잡음 키워드(BE7이 1~2글자·기호 거름) · **8** 전각 숫자·영문 NFC(현행 유지) · **9** 인덱서 키워드와 NFC 명시(BE7) · **10** 보류 시 힌트 컬럼 잔존(의도) · **11** `isPeriodHint` export · **12** 적재마다 Repo 조회 1회 · **13** 테스트 JSON 문자열 → 헬퍼로 수정 · **15** 커밋 subject 길이.
- 작업 중: `resolveTopicHints` 조회 키에 NFC가 빠져 한글 alias NFD 입력이 안 붙음(테스트로 발견) · prettier 재정렬로 테스트 패치가 빗나가 파일을 통째로 다시 씀.

### 검증

- pipeline **409**(+11) · ui 329 · dashboard 249 · lint · typecheck. 라운드트립(A4c) 그대로 통과.

### 미결·다음

- ~~PR → CI → 사용자 승인 뒤 rebase merge~~(아래 PR #123 마감) → **BE7** 주제↔분석 글 자동 연결(키워드·기간 매칭, 모델 없음, 워커 잡, `source=auto`). 입력: `QueueItem.repoNames`(정식 이름)·`keywords`(소문자)·`period`(정규형) ↔ `RepoAnalysis.keywords`·`period`. 이월: 잡음 키워드 거르기·NFC 명시·alias 유일성(리포 등록).

## PR #123 마감 · todo 정리 · BE7 착수

- PR #123(BE6) CI 4잡 통과 → 사용자 지시로 rebase merge(main `85c765b`). 위 BE6 절과 todo BE6의 해시 정정(이 브랜치 feat/topic-auto-link 첫 커밋).
- 사용자 요청으로 남은 TODO 정리(45개: BE 근거 축 7 · BS 단계 5 · B3 산출물 3 · BM 화면 5 · B2/AH 화면 4 · BW 운영 2 · TD 5 · P7 후속 11 · 정리 대상 3). 추천 순서 BE7 → BE8 → BS1·BS2 → BE10 → BS3·BS4 → BS5 → B3. **B1c·B1d·B1f는 BM·BW로 흡수된 항목이라 체크 처리**(사용자 지시).
- 사용자 "BE7 시작하자" → **BE7 주제↔분석 글 자동 연결 착수 승인**.

## BE7 주제↔분석 글 자동 연결 — feat/topic-auto-link

참여: 메인 에이전트(구현) · reviewer(리뷰) · 사용자(결정 "응 추천대로").

### 구현 (커밋 4 + 리뷰 수정 3 + docs 2)

- `639e9a7` `QueueItem.autoLinkedAt`(마이그레이션 `20260922120000_queue_item_auto_linked_at`, 손으로 쓴 SQL — reviewer가 `migrate diff`로 drift 없음 확인).
- `7b488b7` `src/link/matchTopic.ts`(순수): `usefulKeywords`(숫자·기호·1글자 잡음 제외, NFC·소문자) · `expandPeriod`(정규형 → 달 집합, 연 → 12달, 뒤집힘 바로잡음) · `matchTopic`(리포 좁힘, 키워드 겹침 1점씩 + 기간 일치 1점, 리포만이면 overview, 상한 `maxPerTopic`). `AUTO_LINK_LIMITS` 한 곳.
- `350cad5` `src/link/autoLink.ts`: `linkTopicAuto`(auto를 계산 결과와 같게, manual 불변) · `findStaleTopics`(autoLinkedAt 없음/힌트 재적재/리포 재인덱싱) · `runAutoLinkTick`(틱마다 `topicsPerTick`, 후보는 한 번 읽음, 신호는 주제 사이). DB 테스트(재적재 후 manual 유지 포함).
- `48a872b` `runOnce`: Run → IndexJob → 링커 순 위임(`linked`), `bin/worker.ts` 배선.
- `7f59670`·`51f2f0b` decisions BE7 5건 + 리뷰 반영 + BE6 구분자 표기 정정.

### 리뷰 — high 2 · med 5 · low 12 → 사용자 "응 추천대로"

- **H1** 힌트를 트랜잭션 밖에서 읽고 `updatedAt`을 읽기 이전 시각으로 덮어써 적재가 끼어들면 옛 힌트 연결이 최신으로 남고 적재 시각이 되돌아감 → 읽기·쓰기 한 트랜잭션 + `updatedAt` 조건부 updateMany(0건이면 롤백 `TOPIC_CHANGED`), `updatedAt` 불변 + **M2** P2002·P2003은 `LINK_CONFLICT`(틱 `skipped`) + **M5** 테스트(경합 2·리포+기간만·잡음 키워드 경계·stale 정렬·월~연 혼합) `76800af`.
- **H2(결정)** BE12에서 auto 연결 "제거"가 재계산에 되살아남 → `source`에 `dismissed` 추가(구현은 BE12), "추가"가 이미 auto면 승격. decisions에 기록.
- **M1(결정)** 적재가 변경 없는 행도 update해 페이지마다 전 주제 재계산 → 내용 같으면 skip(`isUnchanged`) `1748f67`.
- **M3** 시계 역행 시 tight loop → 워커가 `linked` 뒤에도 2s 대기 + **L1** 주석 12→36 + **L2(결정)** 상한 초과는 최근 달 우선 + **L3** `expandPeriod(period, limits)` + **L4** 동점 정렬에 리포 이름·후보 조회 정렬 `c22244b`.
- 기록만: **M4** 연결 이유는 DB에 없음(Dialog가 재계산) · **L5** 후보 키워드 반복 정규화 · **L6** idle 틱마다 전 주제 읽기 · **L7(결정)** 전 주제 대상 유지 · **L8** 한글 1글자 버림(현행) · **L9** 링커 예외는 루프 catch · **L10** non-null 단언 · **L11** 배럴 면적 · **L12** 정규식 연도.
- 작업 중: `updatedAt`을 안 건드리게 바꾸자 테스트의 가짜 시계(T0)가 실제 생성 시각보다 앞서 항상 stale → 시드에 `updatedAt`을 명시 · 경합 테스트는 `prisma.$extends` query 훅으로 "옛 updatedAt"·"빈 연결 목록"을 돌려주게 해 결정적으로 재현(동시 쓰기로 재현하면 SQLite 잠금에 걸린다) · `2024/12`는 BE6에서 키워드가 되지만 자동 연결의 잡음 규칙(글자 없음)으로 걸러짐.

### 검증

- pipeline **427**(+18) · 스모크 4 · ui 329 · dashboard 249 · lint · typecheck.

### 미결·다음

- PR → CI → 사용자 승인 뒤 rebase merge → 추천 **BE8** 근거 수집 단계 1(linked 포인터 → `git show` 라인 범위 → redact → snippet은 DATA_DIR, posts에는 포인터만). BE12 전 결정: `dismissed`.
