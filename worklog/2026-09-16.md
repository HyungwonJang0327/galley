# 2026-09-16

## 배포 전 리팩토링 항목 추가 — docs/ui-refactor-plan

### 참여

- 메인 세션 단독. 사용자 지시 "배포 직전에 리팩토링만 한 번 하자. 계획 정하고 P1a 시작, 시작 전에 세션 재시작".

### 논의

- 에이전트: 위치는 **P4-11 뒤·P5 앞**(README가 최종 API를 적어야 하므로 배포 직전보다 README 앞이 맞다). 한 번만·한 PR·동작·시각 변화 없음(테스트 수·verify:layout·스크린샷이 증거). 흐름은 reviewer 감사 목록(RF1) → 사용자 승인/기각(RF2) → 항목당 커밋(RF3).
- 미결로 남긴 것: **0.1.0 전이라 호환 깨는 API 정리를 허용할지.** 지시서의 "범위 밖: 기존 API 변경"과 충돌하지만 배포 전이 유일한 기회라 RF2에서 사용자가 정한다(에이전트 추천: 허용).

### 구현 (커밋 2, 문서만)

- 이전 마감 정리: PR #96·#97 main 해시 정정 · worklog 09-15 "PR #96·#97 마감" 절(gitleaks 403 원인 = 프라이빗 전환, main 보호 삭제 → API 재적용) · decisions/branch-protection.md 갱신 이력.
- todo/mvp-todo.md RF1~RF3 + ui-engineer·reviewer todo.

### 미결·다음

- **세션 재시작 후 P1a**(`chore/ui-rename`): `grep -F '@galley/ui'` 목록 제시 → 코드·설정 치환 커밋 → 문서·에이전트 치환 커밋(worklog 제외).

## P1a 패키지명 galley-ui로 변경 — chore/ui-rename

### 참여

- 메인 세션 단독(세션 재시작 후). 사용자 "P1a 시작할 준비 됐지?" → 목록 제시 → "응 시작하자".

### 논의

- 에이전트가 치환 목록을 코드·설정(32파일)과 P1b 문서(29파일)로 나눠 제시. `.changeset/config.json` ignore는 `dashboard`·`@galley/pipeline`뿐이라 변경 불필요. `.claude/settings.local.json` 3건은 gitignore라 치환만 하고 커밋에는 안 들어감. 사용자 승인.

### 구현 (커밋 1, `85efced`)

- `@galley/ui` → `galley-ui`: `packages/ui/package.json` name · dashboard 의존성·import 29건(`galley-ui/styles.css` 포함) · `eslint.config.js` no-restricted-imports 패턴 · `ci.yml` `--filter` ×2 · 주석 6건 · `pnpm install`로 lock 재생성. 35파일 41줄.
- 검증: `galley-ui` build · lint · typecheck · test(ui 124 · pipeline 254 · dashboard 242) · dashboard `next build` 전부 통과.
- `pnpm install`이 `next-env.d.ts`를 `.next/dev/` 경로로 바꿔 놓아 되돌림(치환과 무관한 부수 변경).

### 미결·다음

- **P1b** 문서·에이전트·커맨드 치환(같은 브랜치, worklog/ 제외) → PR.
