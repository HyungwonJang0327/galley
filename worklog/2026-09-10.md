# 2026-09-10

## Select 팝업 폭·아이템 레이아웃 수정 — fix (AppShell fix와 같은 PR)

### 참여

- 메인 세션 단독. 사용자가 스펙(팝업 폭 규칙·ItemContent API·스토리 4+1 케이스)을 직접 지정. `fix/ui-appshell-scroll` 브랜치에 이어서.

### 논의·결정

- **전제 표명**: 스펙의 "MenuItem도 공통 ItemContent"는 Menu 프리미티브가 아직 없어(UM1은 Dialog·Select) **ItemContent만 분리**하고 Menu(UM2, AN5 선행)가 생길 때 소비하도록 todo에 걸어 둠. 사용자에게 Menu까지 지금 만들지 확인 요청.
- **원인**: 팝업 폭이 `min-width: var(--anchor-width)`만 있고 `width`가 없어 트리거 폭에 묶였고, 보조 텍스트에 nowrap이 없어 좁은 열에서 한 글자씩 세로 줄바꿈.
- **수정**(`packages/ui`): Popup `width: max-content; min-width: var(--anchor-width); max-width: var(--ui-select-popup-max-width)`(토큰 `min(480px, calc(100vw - var(--ui-space-6)))`). Positioner는 위치만. 공통 **`ItemContent`**(`primitives/ItemContent/`, 배럴 비노출 — Select·Menu 내부용): `[본문 열: 라벨(line-clamp 2) / 보조(한 줄 ellipsis)] + [우측 메타(shrink 0·margin-left auto·tabular-nums)]`. `label`은 ReactNode라 `Select.ItemText`로 감싼다(타입어헤드·접근성 유지). SelectItem `trailing`→**`meta`** 개명(갤러리 데모 동반 수정).
- **스토리**: 라벨만 / 라벨+보조 / 라벨+보조+메타 / 긴 라벨(40자)+긴 보조 / 화면 하단(decorator로 100vh 하단 정렬). 갤러리에도 같은 5케이스(하단은 페이지 마지막 카드).
- **실측(CDP 스크립트 `verify-select.mjs`, 스크래치패드)**: 5케이스 전부 PASS — 라벨만 160→160(트리거보다 좁아지지 않음) · 메타 160→186 · 긴 라벨 471→480(상한) · 라벨 ≤ 2줄 · 보조 1줄+ellipsis · 메타 폭 유지 · 화면 하단 popup.bottom(728) ≤ trigger.top(732) 뒤집힘. 스크린샷 확인.
- **측정 함정**: Base UI는 닫히는 팝업을 exit 애니메이션 동안 DOM에 남긴다 → `document.querySelector('[role=listbox]')`가 잔여 노드를 잡아 0px로 측정됨. 트리거 `aria-controls`로 listbox를 찾고, 닫힌 뒤 사라질 때까지 기다리도록 수정.
- `JSX.Element`는 React 19 타입에서 전역이 아님 → 스토리 decorator 타입은 `ReactElement`.

### 검증

- RED→GREEN: ItemContent 4 + Select 개명. ui 테스트 60·build, dashboard build, lint·typecheck·prettier 통과. CDP 실측 5/5.

### 미결·다음

- PR(AppShell + Select fix) → CI → rebase merge.
- ~~Menu 프리미티브(UM2)를 지금 만들지 사용자 확인~~ → **사용자 결정: Menu는 지금 만들지 않음.** UM2에서 ItemContent를 쓰는 조건으로 todo(AN5 선행)에 기록. CDP 실측 스크립트 2개를 리포에 넣을지 미결.

### 추가 요구(같은 커밋에 흡수)

- **트리거**: `display: flex; width: 100%; min-width: 0`(min-width 160 제거) — 폭은 부모가 정하고 내용이 늘리지 않는다. 선택값은 기존 `.value`의 nowrap + ellipsis가 이제 실제로 동작(전에는 트리거가 내용대로 커져 잘릴 일이 없었음). 갤러리는 모든 Select 데모를 200px `.selectBox`로 감쌈.
- **팝업 높이**: 토큰 `--ui-select-popup-max-height: min(360px, calc(100vh - var(--ui-space-6)))` + `overflow-y: auto`. CSS는 `max-height: min(토큰, var(--available-height))`로 Base UI 뷰포트 여유와 작은 쪽. "열릴 때 선택 항목 보이기"는 Base UI(floating-ui 리스트 내비)가 이미 해줌 — 실측 selectedVisible=true, 추가 코드 없음.
- **라벨 `overflow-wrap: anywhere`**: ItemContent에 이미 있었음. 공백 없는 80자 경로 라벨 케이스로 실측(팝업 가로 넘침 없음).
- **스토리 3케이스 추가**: `ItemsPathLabelNoSpaces` / `ItemsForty`(35번째 선택) / `TriggerLongValue`(decorator 200px). 갤러리 "Select · 긴 내용" 카드 동일.
- **RED→GREEN(실측)**: 현재 CSS로 먼저 돌려 FAIL 4건 확인(경로 라벨 팝업 568>480 · 항목 40개 팝업 557>360 · 긴 선택값 트리거 378>200 · ellipsis 없음) → 구현 후 8케이스 전부 PASS. CSS 레이아웃은 happy-dom으로 RED가 안 나와 실측을 RED 테스트로 썼다.
- **PR 정리**: 사용자 요청으로 커밋 5개 → **2개**(AppShell fix / Select fix)로 squash(갤러리·worklog·todo는 각 fix에 흡수), feature 브랜치 force-push.
