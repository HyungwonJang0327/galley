# 2026-09-14

## 큐 유령 행 논의 — 현행 유지

### 참여

- 메인 세션 단독. 사용자 질문으로 시작.

### 논의

- 사용자가 `주제_큐.md`의 보류 주제 하나를 둘로 쪼개(하나는 대기로) `파일에서 다시 불러오기` → 원래 항목이 보류에 남고 새 항목 둘이 각각 생김. "어떻게 동작하는 건가".
- 에이전트: 설계대로다. 적재는 정규화 제목 매칭 upsert이고 사라진 줄은 삭제하지 않는다(decisions/queue-sync-direction.md 2026-09-12). 쪼개면 파서에게는 "삭제 + 추가 2". Run 없는 원래 항목은 `holdReason: removed-from-file`로 보류에 남고, 지우는 경로가 없다.
- 사용자: "SQLite 쓸 거면 DB 한 곳에서 관리하면 안 되나. 대시보드는 월 1~2회 검수용, 스케줄은 클로드 데스크톱 채팅으로 돌림. 오픈소스 배포도 고려."
- 에이전트: 파일을 보는 이유는 대시보드가 아니라 스케줄러다. 큐를 가장 자주 읽고 쓰는 쪽(수·토 스케줄)이 파일을 쓰는 한, DB 진실로 바꿔도 "가져오기 + 제목 매칭"이 자리만 옮겨 남는다. 이번 문제는 진실 위치가 아니라 매칭 키의 한계. 선택지 셋: (1) 유지 + Run 없는 유령 행 삭제 경로 (2) DB 진실 + 스케줄러를 Galley CLI/SQLite로 전환(INTENT 비목표 수정 필요) (3) DB 진실 + 수동 가져오기/내보내기(어중간). 오픈소스는 별개 — 파일 동기화를 선택 어댑터로 떼는 방향은 현 결정과 충돌 없음.
- 사용자: "파일 기준으로 대시보드를 덮어쓰는 건?" → 에이전트: 그게 2026-09-12 이전 전체 리셋이고 Run이 `QueueItem.id`를 키로 써서 바꾼 것. Run은 필수 FK라 Run 붙은 행은 삭제도 안 된다. 덮어쓰기 = 매칭 upsert + 미매칭 행 처리이며 차이는 그 처리뿐: (a) Run 없는 행만 삭제(스키마 무변경, 추천) (b) Run 있는 행도 삭제·Run은 제목만 보존(topicId nullable) (c) cascade(이력 유실).

### 결론·결정

- 사용자: **"우선 이렇게 두자."** 파일 진실·매칭 upsert·사라진 줄 보존 그대로. 코드·문서 변경 없음.
- 다시 꺼내면 (a)안이 정리된 추천안. 에이전트가 먼저 재제안하지 않는다.

## /runs 500 수정 + `server-only` 경계 — feat/dashboard-run-detail

### 참여

- 메인 세션 단독. 원인 파악은 dev 서버(3000) 직접 호출로.

### 논의

- 사용자: "다음 작업이 runs 페이지인가? 진입하니 에러." → 에이전트가 `curl /runs`로 재현: HTTP 500, Turbopack `the chunking context does not support external modules (request: node:fs/promises)`.
- 원인: `'use client'` `RunPoller` → `lib/run-labels`의 `isRunInProgress` → `@galley/pipeline` 배럴의 런타임 값(`RUN_STATUS`) → Prisma·fs가 브라우저 청크에 유입. 큐 화면 클라 컴포넌트는 `import type`만이라 안 터졌음(`queue-row-menu.ts` 주석이 그 관행을 적어 둠). 리뷰 14건에는 없던 새 문제.
- 사용자: "네가 추천한 대로 내가 썼는데, 네가 썼어도 그렇게 썼을까?" → 에이전트: 그렇다. run-labels가 "상태 매핑 한 곳"이라 거기서 가져오는 게 자연스럽고, 파일 하나만 봐서는 배럴 유입을 알 수 없다. 문제는 코드가 아니라 경계 강제 장치 부재 — 테스트(happy-dom)·typecheck·lint 모두 못 잡고 dev 서버에서 그 페이지를 열어야 드러난다. 재발 방지 후보: `server-only` 마커(간접 경로도 잡음) vs ESLint 규칙(직접 import만).
- 사용자: "같이 진행해. server-only 결정도 기록하고."

### 구현 (커밋 2)

- `e513d09` `fix(dashboard)`: `RunPoller`가 `active: boolean`만 받는다. 판정(`isRunInProgress`)은 `page.tsx`(서버)에서. run-labels import 제거, 테스트 8건 prop만 교체.
- `2b207d6` `chore(dashboard)`: `server-only@0.0.1` 설치. pipeline 런타임 값을 import하는 대시보드 파일 **11개** 첫 줄에 `import 'server-only'`(lib 10 + `api/runs/[id]/_shared.ts`). 타입만 가져오는 파일은 제외. vitest는 `test/server-only.ts` 빈 모듈로 alias(실제 패키지는 Node 기본 조건에서 던짐). `decisions/server-only-boundary.md` 신설, decisions/README·CLAUDE.md 함정·스택 표 반영.
- 가드 검증: RunPoller에 잘못된 import를 잠시 되살리자 Next가 `run-labels.ts:4:1`의 `server-only` 줄을 가리키며 실패 → 복원 후 200.

### 검증

dashboard **200**(33 files) · lint·typecheck·prettier 통과 · dev 서버 `/`·`/queue`·`/runs`·`/runs/history` 모두 200.

### 미결·다음

- B2c 리뷰 14건 결정은 그대로 대기(2026-09-13 worklog). 이 두 커밋은 그 결정과 무관하게 같은 브랜치에 들어감.
- pipeline 상태 어휘를 클라에서 써야 할 필요가 생기면 서브 진입점(`@galley/pipeline/status`)을 별도 결정 — 지금은 기각.
