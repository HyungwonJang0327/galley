# 2026-09-14

## 큐 유령 행 논의 — 현행 유지

### 참여

- 메인 세션 단독. 사용자 질문으로 시작.

### 논의

- 사용자가 `주제_큐.md`의 보류 주제 하나를 둘로 쪼개(하나는 대기로) `파일에서 다시 불러오기` → 원래 항목이 보류에 남고 새 항목 둘이 각각 생김. "어떻게 동작하는 건가".
- 에이전트: 설계대로다. 적재는 정규화 제목 매칭 upsert이고 사라진 줄은 삭제하지 않는다(decisions/queue-sync-direction.md 2026-09-12). 쪼개면 파서에게는 "삭제 + 추가 2". Run 없는 원래 항목은 `holdReason: removed-from-file`로 보류에 남고, 지우는 경로가 없다.
- 사용자: "SQLite 쓸 거면 DB 한 곳에서 관리하면 안 되나. 대시보드는 월 1~2회 검수용, 스케줄은 클로드 데스크톱 채팅으로 돌림. 오픈소스 배포도 고려."
- 에이전트: 파일을 보는 이유는 대시보드가 아니라 스케줄러다. 큐를 가장 자주 읽고 쓰는 쪽(수·토 스케줄)이 파일을 쓰는 한, DB 진실로 바꿔도 "가져오기 + 제목 매칭"이 자리만 옮겨 남는다. 이번 문제는 진실 위치가 아니라 매칭 키의 한계. 선택지 셋: (1) 유지 + Run 없는 유령 행 삭제 경로 (2) DB 진실 + 스케줄러를 Galley CLI/SQLite로 전환(INTENT 비목표 수정 필요) (3) DB 진실 + 수동 가져오기/내보내기(어중간). 오픈소스는 별개 — 파일 동기화를 선택 어댑터로 떼는 방향은 현 결정과 충돌 없음.
- 사용자: "파일 기준으로 대시보드를 덮어쓰는 건?" → 에이전트: 그게 2026-09-12 이전 전체 리셋이고 Run이 `QueueItem.id`를 키로 써서 바꾼 것. Run은 필수 FK라 Run 붙은 행은 삭제도 안 된다. 덮어쓰기 = 매칭 upsert + 미매칭 행 처리이며 차이는 그 처리뿐: (a) Run 없는 행만 삭제(스키마 무변경, 추천) (b) Run 있는 행도 삭제·Run은 제목만 보존(topicId nullable) (c) cascade(이력 유실).

### 결론·결정

- 사용자: **"우선 이렇게 두자."** 파일 진실·매칭 upsert·사라진 줄 보존 그대로. 코드·문서 변경 없음.
- 다시 꺼내면 (a)안이 정리된 추천안. 에이전트가 먼저 재제안하지 않는다.

## /runs 500 수정 + `server-only` 경계 — feat/dashboard-run-detail

### 참여

- 메인 세션 단독. 원인 파악은 dev 서버(3000) 직접 호출로.

### 논의

- 사용자: "다음 작업이 runs 페이지인가? 진입하니 에러." → 에이전트가 `curl /runs`로 재현: HTTP 500, Turbopack `the chunking context does not support external modules (request: node:fs/promises)`.
- 원인: `'use client'` `RunPoller` → `lib/run-labels`의 `isRunInProgress` → `@galley/pipeline` 배럴의 런타임 값(`RUN_STATUS`) → Prisma·fs가 브라우저 청크에 유입. 큐 화면 클라 컴포넌트는 `import type`만이라 안 터졌음(`queue-row-menu.ts` 주석이 그 관행을 적어 둠). 리뷰 14건에는 없던 새 문제.
- 사용자: "네가 추천한 대로 내가 썼는데, 네가 썼어도 그렇게 썼을까?" → 에이전트: 그렇다. run-labels가 "상태 매핑 한 곳"이라 거기서 가져오는 게 자연스럽고, 파일 하나만 봐서는 배럴 유입을 알 수 없다. 문제는 코드가 아니라 경계 강제 장치 부재 — 테스트(happy-dom)·typecheck·lint 모두 못 잡고 dev 서버에서 그 페이지를 열어야 드러난다. 재발 방지 후보: `server-only` 마커(간접 경로도 잡음) vs ESLint 규칙(직접 import만).
- 사용자: "같이 진행해. server-only 결정도 기록하고."

### 구현 (커밋 2)

- `e347eaa` `fix(dashboard)`: `RunPoller`가 `active: boolean`만 받는다. 판정(`isRunInProgress`)은 `page.tsx`(서버)에서. run-labels import 제거, 테스트 8건 prop만 교체.
- `4a1bcdf` `chore(dashboard)`: `server-only@0.0.1` 설치. pipeline 런타임 값을 import하는 대시보드 파일 **11개** 첫 줄에 `import 'server-only'`(lib 10 + `api/runs/[id]/_shared.ts`). 타입만 가져오는 파일은 제외. vitest는 `test/server-only.ts` 빈 모듈로 alias(실제 패키지는 Node 기본 조건에서 던짐). `decisions/server-only-boundary.md` 신설, decisions/README·CLAUDE.md 함정·스택 표 반영.
- 가드 검증: RunPoller에 잘못된 import를 잠시 되살리자 Next가 `run-labels.ts:4:1`의 `server-only` 줄을 가리키며 실패 → 복원 후 200.

### 검증

dashboard **200**(33 files) · lint·typecheck·prettier 통과 · dev 서버 `/`·`/queue`·`/runs`·`/runs/history` 모두 200.

### 미결·다음

- B2c 리뷰 14건 결정은 그대로 대기(2026-09-13 worklog). 이 두 커밋은 그 결정과 무관하게 같은 브랜치에 들어감.
- pipeline 상태 어휘를 클라에서 써야 할 필요가 생기면 서브 진입점(`@galley/pipeline/status`)을 별도 결정 — 지금은 기각.

## B2c 리뷰 14건 수정 — feat/dashboard-run-detail

### 참여

- 메인 세션 단독. 사용자 지시 "14번 모두 잡고가자, 리뷰 개당 커밋".

### 논의

- 에이전트가 09-13 리뷰 14건과 추천안(1·2·3·6 이번 PR / 4·12·13 시드 / 5·11 후속 / 나머지 B2d·B2e)을 다시 제시 → 사용자: **전부 이번 브랜치에서, 항목당 커밋 하나.** 🔒 파일(RunPoller·RunTimeline·page.tsx)의 리뷰 수정도 이 지시로 AI가 진행(핵심 모듈 "작성"은 그대로 사용자, 리뷰 반영은 지시에 따름).
- 항목 5 "근거 없음 n"과 항목 11(`보기`/`diff`·펼침 산출물)은 데이터 원천이 없다 — 근거 검증 결과는 BE10, 단계 산출물 파일 쓰기는 B3a(`StepRunner.artifacts` 주석) 뒤. 각각 BE13·B2e에 그대로 둔다.
- 항목 9는 앱에서 `<input>`을 스타일하지 않고 `@galley/ui`에 `Input` 프리미티브를 넣었다(Base UI Input 래퍼, Select 트리거와 같은 높이·테두리·포커스 링). 갤러리 섹션 + SplitPane 데모의 맨 input 교체. `verify:layout` 125/125(새 검사 없음).
- 항목 10 테스트를 쓰다 실제 버그 발견: carried인데 출처 시각이 없으면 `"이전 결과 · "` 꼬리가 남음 → 같이 수정.

### 구현 (커밋 15 = docs 1 + 리뷰 14)

| #   | 커밋                                          | 내용                                                                                                  |
| --- | --------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| –   | `21a3482` docs                                | 09-13 worklog 리뷰 절(워킹트리에 있던 것)                                                             |
| 1   | `45d3baa` fix(pipeline)                       | seed-dev `--yes` 없으면 지울 건수만 출력                                                              |
| 2   | `b1443b8` fix(dashboard)                      | RunPoller `start()` 중복 가드(`timer` 있으면 return) + 테스트                                         |
| 3   | `885ac43` fix(dashboard)                      | 실패 줄 `errorMessage`(코드 폴백) + "n회 시도", 테스트 2건 교체                                       |
| 4   | `8bfde4b` fix(pipeline)                       | seed-dev가 QueueItem을 만들지 않음 — 시나리오 배열, 주제 수만큼만                                     |
| 5   | `6f969cb` feat(dashboard)                     | 좌 행: `RunStepDots`(6칸, 타임라인 마커 토큰) · `lib/run-list-row.ts`(마지막 단계 미리보기·짧은 시각) |
| 6   | `aa161df` fix(dashboard)                      | `RunPoller key={selected.id}`                                                                         |
| 7   | `94eed17` refactor(dashboard)                 | `lib/run-page.ts` `pickActiveId`·`resolveRunPane` + 테스트 8                                          |
| 8   | `5901135` fix(dashboard)                      | 헤더 주제명 `<h2>`                                                                                    |
| 9   | `b9e0d2e` feat(ui) · `88e4bae` fix(dashboard) | `Input` 프리미티브(+갤러리) · ActionBar 입력 교체                                                     |
| 10  | `bce5bb7` test(dashboard)                     | 엣지 3건(carried 시각 null·420ms→"0초"·메시지·코드 둘 다 null) + 꼬리 구분자 수정                     |
| 12  | `3632a32` fix(pipeline)                       | 시드 비용 = `createModelRegistryFromEnv().default().pricing` × `calculateCostUsd`                     |
| 13  | `3ef6b0a` refactor(pipeline)                  | 시드 큐 상태 리터럴을 `QueueStatus` 타입 상수로                                                       |
| 14  | `b9e88b4` perf(dashboard)                     | `?id=` 있으면 목록·상세 `Promise.all`                                                                 |

### 검증

lint·typecheck·prettier 통과 · ui 97→**102** · pipeline 254 · dashboard 200→**221** · `verify:layout` 125/125 · dev 서버 `/runs`·`/runs?id=…`·`/runs?id=nope`·`/design` 200 · 시드 `--yes` 실제 실행 7건.

### 미결·다음

- 항목 11 → B2e(선행 B3a 산출물 파일). 항목 5 "근거 없음 n" → BE13(선행 BE10).
- **다음: PR 생성 → CI → rebase merge → todo B2c 체크.** 그 뒤 B2d(ActionBar 배선 — `Input`은 이제 있음, 수정 지시가 여러 줄이면 `Textarea`는 그때).

## PR #92 마감

- CI 첫 실행에서 `verify`만 실패 — `*.db.test.ts` 3개의 `beforeAll`(`npx prisma migrate deploy`)이 러너에서 기본 훅 타임아웃 10s 초과. 이 브랜치가 DB 테스트 파일을 늘리지 않았고 main에서는 통과하던 것이라 러너 속도 플레이크로 판단 → `144986a` pipeline vitest `hookTimeout: 30_000`(스모크 설정과 같은 값, 테스트 타임아웃은 그대로). 재실행 4개 잡 통과.
- 사용자 승인으로 `gh pr merge --rebase --delete-branch`. 위 표·09-13 worklog·todo의 브랜치 해시는 main 해시로 정정(이 절과 같은 커밋).
- 다음 단위(사용자 선택): 새 브랜치 `feat/ui-form-primitives` — Textarea · Checkbox · Tooltip · Tabs + 갤러리 맨 `<input type=search>`·`<select>` 교체. 컴포넌트당 커밋.

## 폼 프리미티브 4개 — feat/ui-form-primitives

### 참여

- 메인 세션 단독. 사용자: "textarea도 필요하고 input 관련된 다른 컴포넌트들도 있지 않겠어? 컴포넌트 갤러리에도 추가해야하고" → 범위 질문에 Textarea·Checkbox·Tooltip·Tabs·갤러리 맨 요소 교체 전부 선택.

### 구현 (컴포넌트당 커밋, 갤러리 섹션 포함)

- `ecc82b8` **Textarea** — Base UI에 textarea가 없어 native를 Input과 같은 토큰으로. `rows` 기본 3, 세로 리사이즈만, `invalid`. 테스트 6(happy-dom은 `el.rows`를 안 채워 `getAttribute('rows')`로).
- `35f60ca` **Checkbox** — Base UI Checkbox.Root+Indicator를 `<label>`로 감싸 라벨 클릭도 토글. 제어형만, `indeterminate`(aria-checked=mixed), lucide Check/Minus. 토큰 `--ui-checkbox-size` 16px. 테스트 6. 정적 데모(disabled)도 `PrimitiveDemos`에 — 서버 컴포넌트는 `onCheckedChange`를 못 넘긴다.
- `d39394b` **Tooltip** — `trigger` 요소(Menu와 같은 render 방식)·`content`·`side`·`delay`. 다크 pill(TopBar 토큰), 토큰 `--ui-tooltip-max-width` 240px. **Base UI 1.8 Popup은 `role="tooltip"`을 안 붙인다** → 래퍼가 명시. 테스트는 native `mouseenter`/`mouseleave`를 직접 만든다(testing-library `fireEvent.mouseEnter`는 React용 mouseover라 Base UI의 native 리스너가 못 듣는다; pointerenter로 pointerType 먼저). 테스트 4.
- `5c55ebe` **Tabs** — 페이지 안 상태 탭(제어형 `value`·`items{value,label,count,disabled,content}`), content 있는 항목만 Panel. URL 이동 탭(ListToolbarTab)과 같은 시각 언어(밑줄·블루). 테스트 6.
- `b11b498` 갤러리 ListToolbar의 맨 `<input type=search>`·`<select>` → `Input`·`Select`·`Checkbox` 데모(`.filterBox` 160px). 갤러리에 스타일 없는 폼 요소 0.

### 검증

lint·typecheck·prettier 통과 · ui 102→**124** · pipeline 254 · dashboard 221 · `verify:layout` **125/125**(새 검사 없음) · dev 서버 `/design` 200 · 헤드리스 Chrome 전체 스크린샷으로 새 섹션 5개 육안 확인.

### 미결·다음

- Tooltip 소비처(접힌 사이드바 라벨 — 지금은 `title`) · Tabs 소비처는 없음. 스펙 primitives 목록(layout.md §108)은 이제 Popover만 남음.
- ListToolbar 슬롯 사용처(큐 화면)의 실제 검색·필터 컨트롤 교체는 그 화면 작업 때.
- 다음: PR → CI → rebase merge → B2d(ActionBar 배선, Textarea 소비).

## PR #93 마감

- CI 4개 잡 통과. 사용자가 직접 머지(툴팁은 hover로 동작 확인). 해시는 main 기준으로 정정(이 커밋).
- B2d 범위 결정(사용자): **재실행 확인 Dialog만 먼저**(승인 Dialog는 나중) · **승인 대기 상태만 허용**(실패 실행의 재실행 진입은 별도 항목으로 쪼갬).
