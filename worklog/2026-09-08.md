# 2026-09-08

## 참여

- 메인 세션(사용자 결정 + 실행). 서브에이전트 미사용.

## 논의 내용

- **킥오프 전 과정 진행**: 인터뷰(확정본) → 규모 판단(FULL) → INTENT.md → 핵심 모듈 선택 → 기술 결정 ①~⑨ → 문서 → 에이전트 → 커맨드 → 환경(7-1·7-2).
- **핵심 모듈 2개**: 사용자가 (a) 파이프라인 상태 머신+승인 게이트, (b) 모델 어댑터 인터페이스 선택. AI는 테스트·리뷰만. (c) UI Dialog는 제외.
- **DB 접근 계층 ①**: 초안은 Drizzle 추천이었으나, 사용자 익숙도 이유로 **Prisma**로 변경(SQLite 유지). Postgres 상시 구동 마찰 때문에 Postgres-now는 기각.
- **큐 동기화 ③**: 논의 중 "DB 진실·파일 은퇴"를 검토했으나, 사용자가 **수·토 스케줄을 Phase 2 완료까지 유지**한다고 하여 원안(파일이 진실, DB 파생 캐시)으로 복귀. INTENT 완료 기준 원문 유지.
- **보류 상태 유지**(대기/후보/보류/완료 4개), **AI 후보 생성은 Phase 2**로.
- **모노레포 이유 재확인**: `@galley/ui` 독립 디자인 시스템화가 정당성의 핵심. 단일 앱/3리포 대안 기각. 사용자 납득.
- **Zenn ④**: GitHub 연동 방식 확정. `zenn-content` private 리포 생성·스캐폴드·push 완료(`ad7fcf4`). Zenn 계정 연결은 사용자 수동(웹 OAuth, Phase 2 전까지).
- **환경 함정 3개 해결**: ① vitest5(rolldown) 네이티브 바인딩 초기 누락 → `install --force`. ② pnpm10 esbuild 빌드 차단 → `onlyBuiltDependencies:["esbuild"]`. ③ tsup dts가 TS6에서 `baseUrl` TS5101 → ui tsconfig `ignoreDeprecations:"6.0"`.
- **TypeScript 버전**: 최신 7.0.2가 typescript-eslint 8 미지원 → **6.0.3 고정**.

## 결론·결정 (문서 반영 위치)

- 기술 결정 ①~⑨ + 기반·경계 결정 전부 `decisions/`에 기록, `planning.md` 확정 스택 표 반영.
- 문서 구조(CLAUDE·planning·README·COMMIT_CONVENTION·decisions 20+·worklog/todo/docs 규칙) 생성.
- 팀 에이전트 7종, 커맨드 10종 + settings.json 생성.
- 환경 7-1(모노레포 루트) + 7-2(pipeline·ui·dashboard 골격) 완료, 최소 단위 커밋으로 `galley` 리포에 push(`efe8223`).
- 툴체인 핀 결정 → `decisions/toolchain-pins.md` 신규.

## 커밋 (galley 리포)

- `8bf9a12` chore: pnpm 모노레포 루트 설정 / `e222cc6` docs: 핵심 문서 / `8f5573c` docs: decisions / `bb344f4` docs: 규칙 문서 / `90af032` chore: 에이전트 / `d29d7b1` chore: 커맨드+settings
- `2dc3c2a` chore(pipeline) / `bf79030` chore(ui) / `efe8223` chore(dashboard)
- (zenn-content 리포) `ad7fcf4` chore: Zenn 콘텐츠 리포 초기화

## 미결·다음

- **7-3**: GitHub Actions(lint·typecheck·test·@galley/ui build + gitleaks), CLAUDE.md 스택 표 실제 버전 갱신, main 브랜치 보호.
- 확인 필요: gitleaks **pre-commit 훅 도입 여부**(husky/lefthook 추가), **main 보호 설정 여부**.
- 이후 8단계: Phase 1-A/1-B todo 분해(mvp-todo + 역할별). 아직 미작성.

---

## 7-3 · CI·시크릿·main 보호 (환경 마무리)

### 논의·결정

- pre-commit 훅 vs main 보호를 설명 후 사용자 결정: **lefthook + gitleaks(pre-commit)** / **main 표준 보호(PR·CI 필수·force-push 금지)**.
- 브랜치 컨벤션 결정: **type/scope-desc** + **Rebase and merge**(원자적 커밋 보존).
- 실행: gitleaks 8.30.1 설치, lefthook 2.1.12 pre-commit 훅(가짜 GitHub PAT로 차단 검증 완료), CI(verify·gitleaks) 통과 확인 후 main 보호 활성화. 머지 옵션 rebase-only + 머지 후 브랜치 삭제.

### 커밋 (galley 리포)

- `2f638db` ci: 워크플로 / `151ce31` chore: lefthook+gitleaks / `a261378` docs: 스택 표 / `e934196` docs: 브랜치·보호 결정

### 결정 문서

- 신규: `branch-convention`, `branch-protection`. 갱신: `secret-scanning`(lefthook 확정), `CLAUDE.md`(스택 표·pnpm 빌드스크립트 함정·작업 흐름), `COMMIT_CONVENTION.md`(브랜치 절).

### 상태·다음

- **환경 세팅(7단계) 전체 완료.** main 보호 ON → 이후 모든 변경은 브랜치→PR→CI→rebase merge.
- 다음: **8단계** Phase 1-A/1-B todo 분해(승인 후 구현).

---

## 8단계 · Phase 1 todo 분해 + 첫 구현(A1)

### 논의·결정

- **Phase 1 todo 분해**(PR #2 `de49b50`): mvp-todo(마스터, 단일 출처) + 역할별 todo. Phase 1-A(A1~~A6)·1-B(B1~~B3)를 커밋 단위로, 완료조건·예정 커밋·담당·🔒(직접 작성) 태그 부착. 역할별 todo는 마스터 ID 참조로 중복 최소화.
- **A1 디자인 토큰**(PR #3 `97b55bb`): `@galley/ui`에 색·간격·타이포·라운드·그림자를 `--ui-*` CSS 변수로. 레이아웃 스펙 §1·§5 기준. index가 로드→`dist/index.css` 산출, 소비자는 `@galley/ui/styles.css`.
- **조정**: 토큰 정적 파싱 단위 테스트는 드롭. 이유 — node 타입을 브라우저용 ui에 넣거나 vitest `?raw`가 빈 문자열 반환(둘 다 과한 곁가지). 토큰은 로직 아닌 정적 값이라 빌드(CSS 컴파일·산출)로 검증, 실사용 검증은 A2 컴포넌트가 담당.

### 첫 PR 흐름 검증

- 로그(#1)·todo(#2)·A1(#3) 모두 `브랜치 → PR → CI(verify·gitleaks) → rebase merge → 브랜치 삭제`로 처리. main 보호 흐름 정상.

### 상태·다음

- 환경 완료 + Phase 1-A **A1 완료**. 다음: **A2a Button**(ui-engineer).

---

## A2a · Button (+ 컴포넌트 테스트 환경)

### 논의·결정

- **A2a Button**(PR #5): variant(primary·secondary·ghost)·size(sm·md), 토큰 변수만 참조, type 기본 button. 커밋 2개 — `e6639c7` 테스트 환경 / `70fcf28` Button. 테스트 7개 통과.
- **테스트 환경**: vitest `environment: happy-dom` + `@testing-library/react`. jsdom은 전이 의존성 `@exodus/bytes`(ESM)가 vitest fork 워커의 require와 충돌 → **happy-dom으로 교체**.
- **발견(미결)**: CI에 `prettier --check`가 없어 포맷 드리프트가 통과됨(A1 tokens.css 비정규 포맷 사례). planning 미결에 등록.

### 상태·다음

- A1·A2a 완료. 다음: **A2b Badge**(ui-engineer). (A1 tokens.css 폰트 라인 재포맷은 별도 처리 예정.)

---

## CI 정리 — prettier 검사 추가

### 논의·결정

- 사용자 요청으로 A2b 전에 CI 정리 먼저. CI verify 잡에 `format:check` 추가 + 루트 `format:check` 스크립트, `tokens.css` 포맷 정규화(A1 비정규 포맷 교정). PR #7 — `1a0829c` 포맷 정규화 / `539b516` CI 검사.
- 이제 포맷 드리프트가 CI에서 차단됨. planning 미결의 CI prettier-check 항목 해소.

### 상태·다음

- 다음: **A2b Badge**(ui-engineer).

---

## 컴포넌트 갤러리 결정 + A2b·A2c·A2d

### 논의·결정

- **컴포넌트 갤러리**(PR #9 `34b0bc7`): "앱 안에서 보이되 패키지 분리도"라는 요구 확인. in-app `/design`(대시보드 셸 안, **A3 이후**), A2b보다 앞당기지 않음. 패키지 분리에 무영향(앱→ui 단방향). Storybook은 Phase 2 정식 쇼케이스 유지. decisions/component-gallery.md + todo AG.
- **A2b Badge**(PR #10 `44e49cf`): variant(neutral·info·warning·success·danger) + pulse.
- **A2c Card**(PR #11 `08c3047`): 흰 배경·라운드·약한 그림자 컨테이너.
- **A2d PageHeader**(PR #11 `fa741df`): h1 제목 + actions 슬롯.
- 모두 토큰 변수만 참조, 컴포넌트별 테스트·스토리. 누적 테스트 18개.

### 상태·다음

- Phase 1-A: A1·A2(4종) 완료. 다음: **A3 셸**(AppShell·Sidebar·TopBar 패턴 → layout 배선), 이후 AG 갤러리에서 첫 시각 확인.

---

## A3a·A3b — 셸 패턴 + 앱 배선

### 참여

- 메인 세션(사용자 결정 + 실행). 서브에이전트 미사용.

### 논의·결정

- **A3a 셸 패턴**(PR #13 `f56db05`): `@galley/ui/patterns`에 표현 전용 4종. AppShell(TopBar·Sidebar·Content 3슬롯 그리드, `sidebarCollapsed`), SidebarGroup(접힘 시 라벨 감춤·접근성 이름 유지), SidebarItem(아이콘+라벨+배지, `isActive`→블루 틴트·`aria-current`, **`render` 요소 다형**으로 링크 주입, 접힘=아이콘만), TopBarChip(단색 pill, `isActive`·`trailing` 겸용). 도메인 무지, 토큰만 참조. 토큰 추가: topbar-chip-bg/hover, sidebar-collapsed-width. 테스트 30개(신규 12).
- **A3b 셸 배선**(PR #14 `7f8adc0`): `lib/navigation.ts` 메뉴 단일 정의(그룹→라벨·경로·lucide 아이콘) + `isNavItemActive` 순수 함수. `(dashboard)/layout.tsx`가 AppShell로 셸 고정. client `Sidebar`(usePathname 활성, 내부 Next `<Link>`를 render 주입, 외부는 새 탭 `<a>`)·`TopBar`(워드마크+워크스페이스 탭 글/설정·router.push, 모델 칩=모델명만). 루트 레이아웃에서 `@galley/ui/styles.css`+최소 globals 로드.
- **경계 유지**: 아이콘(lucide-react)은 앱이 SidebarItem에 주입 → **ui는 lucide 비의존**. 활성 판정·메뉴 정의는 앱에만.

### 발견·수정 (CI)

- A3b가 **로컬 통과·CI verify 실패**. 원인: CI `pnpm typecheck`가 `@galley/ui build`보다 먼저 실행 → dist·d.ts 부재로 `@galley/ui` 모듈 미해석(로컬은 A3a에서 이미 빌드돼 있어 통과). 앱은 빌드된 dist를 소비(ui-package-boundary)하므로 **CI에서 ui 빌드를 typecheck·test 앞으로 이동**(PR #14 `80255c3`, `ci` 커밋 분리).

### 상태·다음

- Phase 1-A: A1·A2·**A3a·A3b 완료**. `(dashboard)` 그룹에 페이지가 없어 셸은 아직 미출력(정상). 다음: **A3c**(루트 `/queue` redirect + Phase 2 자리 라우트 스텁) → 이때부터 `pnpm dev` 시각 확인 가능.

---

## A3c — 루트 redirect + 자리 라우트

### 참여

- 메인 세션. 서브에이전트 미사용.

### 논의·결정

- **A3c**(PR #16 `279ac01`): 루트 `page.tsx`를 `/queue` redirect로 교체 + `(dashboard)` 그룹 아래 메뉴 전 경로(스펙 §3) 자리 페이지 10개. Phase 1(queue·runs)은 A5/B2에서 내용 교체, Phase 2(publish·settings)는 자리만. 공용 `Placeholder`(PageHeader+Card=패턴 A 골격)로 중복 제거, content 여백 래퍼(`--ui-space-5`).
- 스모크(프로덕션 서버): `/`→307 `/queue`, `/queue`에서 셸·`aria-current="page"` 렌더, `/settings/model`에서 활성 URL별 전환 확인.

### 상태·다음

- A3a·A3b·A3c 완료. 셸이 실제 렌더됨. 다음: A3d(접힘 토글).

---

## CSS Modules 미적용 버그 → ui 빌드 tsup→Vite 교체

### 참여

- 메인 세션. 사용자가 `pnpm dev` 첫 화면 스크린샷으로 무스타일 제보.

### 논의·결정

- **증상**: 셸이 스타일 전무(다크 TopBar·그리드·활성 틴트 없음, 링크 브라우저 기본).
- **진단**: `dist/index.js`에서 `.module.css` import가 빈 객체(`{}`)로 번들 → `styles.*` 전부 `undefined` → className 미부착. CSS는 전역 클래스로만 추출돼 연결 끊김. **tsup 8은 CSS Modules 로컬 스코프 미지원**(`local-css` 로더 명시도 무시). 컴포넌트 테스트가 role/텍스트만 봐서 잠복.
- **선택지 A(Vite 라이브러리 모드)·B(소스 소비 transpilePackages)·C(tsup+플러그인)** 제시. 사용자 기준: **"나중에 패키지 분리 시 소요 최소"** → **A 확정**(산출물이 그대로 배포 가능, 앱은 이미 dist 소비 중 → 분리=publish+버전 교체만). PR #17 `97ec61e`.
- **npm 배포 소비자 장벽 점검**: '빌드 후 dev'는 우리 모노레포 dev 루프 문제일 뿐 소비자와 무관(소비자는 컴파일된 dist install→import). 실질 장벽은 **`styles.css` 수동 import 안내 1건**(표준). 미래 주의: 상태 있는 primitive(Dialog 등) 추가 시 해당 파일 **`"use client"` 배너 보존**(현재는 훅 0개라 RSC 안전).

### 결정 문서(반영 위치)

- `decisions/toolchain-pins.md`(Vite over tsup 사유·기각안·갱신 이력), `CLAUDE.md` 스택 표, `decisions/ui-package-boundary.md`·`turborepo.md`, `.claude/agents/ui-engineer.md`.

### 상태·다음

- 스타일 정상 적용 확인(렌더 HTML에 `ui_*` 스코프 클래스). 다음: **A3d**(사이드바 접힘 토글 + localStorage).

---

## A3d — 사이드바 접힘 localStorage (A3 완료)

### 참여

- 메인 세션 단독.

### 논의·결정

- **A3d**(PR #20 `421ca5f`): TopBar ☰ 토글로 사이드바 접힘/펼침, localStorage 저장, 하이드레이션 플래시 방지.
- **설계**: `SidebarProvider`(클라 컨텍스트, `collapsed`+`toggle`, 마운트 후 localStorage 복원) → `DashboardShell`이 `AppShell.sidebarCollapsed`에 배선(layout이 서버 컴포넌트라 클라 경계 필요) → `TopBar`(☰, `aria-expanded`)·`Sidebar`(프리미티브에 `collapsed` 전달)가 컨텍스트 소비.
- **플래시 방지**: root `layout.tsx` `<body>` 인라인 스크립트가 페인트 전 localStorage를 읽어 `html[data-sidebar-collapsed]` 세팅 → `globals.css`가 `--ui-sidebar-width`를 좁힘. 서버·최초 클라 렌더를 `false`로 일치시켜 하이드레이션 경고 회피(라벨은 마운트 후 복원).
- **경계 유지**: `@galley/ui` 미변경 — 순수 React `collapsed` prop 유지. `:global(html[...])` 셀렉터로 ui를 magic 속성에 묶는 대안은 배포 소비자 결합을 늘려 기각(추출 소요 최소 원칙). 저장 키/속성은 `lib/sidebar-collapse.ts` 한 곳에서 서버 스크립트·클라 Provider가 공유(드리프트 방지).
- **규칙 준수**: CLAUDE.md §4 "사이드바 접힘은 localStorage, 서버 상태로 만들지 않음" → 쿠키/서버 read 대안 배제.

### 미결·다음

- **대시보드 단위 테스트 인프라(vitest) 미도입** — A3b/A3c 선례대로 build+프로덕션 스모크로 검증. 도입 여부는 별도 결정 필요.
- 스모크: 인라인 스크립트 주입·☰ 토글(`aria-expanded="true"`)·라벨 전부 렌더·접힘 CSS 오버라이드 빌드 포함 확인. lint·typecheck·build(13 라우트)·test(ui 30/30) 통과.
- A3 전체 완료(메뉴 렌더·URL 활성·접힘 토글). 다음: **AG**(컴포넌트 갤러리 `/design`).
