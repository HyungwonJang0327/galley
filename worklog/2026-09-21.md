# 2026-09-21

## 리포 분리 질문 → 결정 기록 — docs/ui-repo-split (PR #109, 머지됨)

참여: 메인 에이전트 · 사용자.

- 사용자 질문 "npm 배포할 때 git 리포를 분리하는 게 좋을까?" → 에이전트: **분리 안 함** 추천. `packages/ui/package.json`의 `repository.directory`·`.changeset/config.json` ignore·`files: ["dist"]`로 모노레포 배포 준비가 이미 끝나 있고, 대시보드가 첫 소비자이자 검증 루프(갤러리+`verify:layout`이 같은 CI)라 분리하면 왕복 비용만 는다. 리포는 이미 PUBLIC + 루트 MIT — "나중에 오픈소스로 푼다"가 아니라 지금이 그 상태.
- 사용자 "어차피 오픈소스로 풀 거니까 윈윈?" → 맞음. npm 소비자는 같은 리포에서 실제 앱을 보고, Galley 방문자는 ui가 경계를 지켜 배포된 것을 본다.
- 결정: `decisions/ui-repo-split.md` 신설(`8c77b17`) + README 목록 + planning 표·미결 2건(회사 리포 폴더명을 CLAUDE.md·clean-room.md에 공개 상태로 둘지 / pipeline·dashboard도 루트 MIT 의도인지 — **배포 전 사용자 판단**).
- PR #108 마감 해시 정정(P4-6 브랜치 해시 11개 → main)은 이 브랜치에서 끝냄(`d1da754`). CI 4잡 통과 → 사용자 지시로 rebase merge.

## 말투 고정

- 사용자: "세션 시작할 때마다 말투가 바뀌는데 ~요체로 바꿔. 고정으로." → 메모리(galley-korean-only)에 "한국어 + 해요체 고정"으로 저장. 코드·커밋·문서는 기존 관례(서술체) 유지, 대화 출력만.

## P4-7 Skeleton — feat/ui-skeleton

참여: 메인 에이전트(구현) · reviewer(리뷰) · 사용자(결정).

### 구현 (커밋 3)

- 판정: **직접 작성** — Base UI 1.8.0에 Skeleton 서브패스 없음.
- `ea96f78` feat(ui): `components/Skeleton/` 5파일 + 배럴(`Skeleton`·`SkeletonProps`·`SkeletonRadius`·`SkeletonSize`). `width`/`height`(숫자=px, 문자열=CSS 값) · `radius` sm|md|full · `lines`(2 이상이면 span 막대를 쌓고 마지막 줄 60% — width가 있으면 `calc(w * 0.6)`) · `aria-hidden` 기본. 색 `--ui-color-border` 재사용(전용 색 토큰 없음). 펄스 1.5s + `prefers-reduced-motion`에서 없음.
- `4e9f972` feat(dashboard): 갤러리 섹션(`data-demo` skeleton-shapes·lines·card).
- `029d668` test(dashboard): `skeleton.mjs` 13검사(186→199).
- 라이트·다크 스크린샷 육안 확인 — 다크는 막대가 옅음(reviewer에 판단 요청).

### 리뷰 — high 0 · med 3 · low 8 → 사용자 "L4만 빼고 추천대로"

- **M1** `.bar`의 `flex-shrink: 0` + `width: 100%` — 폭 없는 막대가 flex 행에서 넘친다(Separator L3와 같은 패턴) → 제거, 폭을 준 막대만 인라인 `flexShrink: 0`. 갤러리 flex 행 시연 + 실측 3검사 `3e15f6a`
- **M2** "부모가 aria-busy로 알린다"는 부정확 — aria-busy는 낭독 보류 힌트일 뿐 아무것도 읽히지 않는다 → JSDoc·스토리·갤러리를 "aria-busy(보류) + 그 **밖**의 상시 `role="status"` 시각 숨김 텍스트"로 정정 `4f3dc5a`. status 텍스트를 Skeleton API에 넣지 않고 소비자 규칙으로(사용자 승인). VoiceOver 실측은 todo P7h에 얹음.
- **M3** `aria-hidden={undefined}`를 명시로 넘기면 기본이 지워짐 → 스프레드 뒤 `?? true` + 회귀 테스트 `181acb3`
- **L1** 숫자→px는 스펙 밖·선례 없음 → 유지하되 JSDoc "토큰 우선, 숫자는 토큰에 없는 치수만" + 갤러리 문구 `cef0447`(flex 행 데모는 `var(--ui-space-5)` 사용)
- **L2** 단일 모드 루트 div → **span**(display:block) 통일 — 텍스트 안에서도 유효한 중첩 `889d137`
- **L3** 테스트 누락 → 여러 줄 radius·루트 인라인 없음·문자열 width calc 3건 `3ffdd58`(M3·aria-hidden 해제는 각 커밋에)
- **L4 다크 옅음** — reviewer: 수치상 다크 고유 문제 아님(surface 대비 라이트 1.24 / 다크 1.23, 펄스 최저 둘 다 1.10), 장식이라 WCAG 비대상, 추천은 현행 유지. **사용자: 갤러리에서 직접 비교** 요청 → 임시로 "0.45·0.6 최저점 정지 + 실제 펄스" 4개를 나란히 넣고 스크린샷. 에이전트 소견: 0.45는 최저점에서 표면과 거의 구분 안 됨(사라지는 느낌), 0.6은 형태가 남음. **사용자 결정: 0.6** `54ea48d`. 임시 비교 데모는 커밋하지 않고 제거.
- **L5** 스토리 카드 폭 `--ui-dialog-width` 차용 → 제거 `11177b1`
- **L6** prop `full` ↔ 토큰 `pill` 어휘 → RF1 입력에 기록만. **L7** Badge 펄스에 reduced-motion 없음 → RF1 입력 ②에 추가. **L8** 실측 스크립트의 DOM 결합 → 관례, 조치 없음.

### 발견 — 리뷰 반영이 만든 회귀 2건(실측이 잡음)

- M2의 시각 숨김 `role="status"` span(`position: absolute`)이 안쪽 스크롤 영역에서 **body를 늘려** 셸 실측 4건 실패(FormField hidden input 때와 같은 함정). 위치 기준 부모 `.loadingWrap`(relative) 추가 `068f491`.
- M1의 flex 행 데모에서 폭 없는 Skeleton(100% 요구)이 형제 버튼을 눌러 "버/튼"으로 접힘 → 버튼 `flex-shrink: 0`(같은 커밋). 폭 없는 Skeleton은 100%-폭 요소와 같아 형제가 눌린다 — 소비자 책임.

### 검증

- ui test **258**(+17) · pipeline 254 · dashboard 245 · lint · typecheck · prettier · ui build · `verify:layout` **202/202**(+16).
- 브랜치 커밋 12(구현 3 + 리뷰 7 + 회귀 수정 1 + opacity 1). PR 미생성 → 다음: PR → CI → 머지 질문 → **P4-8 Toast**.

### 미결·다음

- todo RF1 입력 ⑥ radius 어휘(full↔pill) ⑦ Badge 펄스 reduced-motion·duration 하드코딩(Badge 1.2s·Skeleton 1.5s).
- P7h에 "aria-busy 부모 + 밖의 status 텍스트" VoiceOver 실측 추가.
- 배포 전 사용자 판단 3건: RF2(0.1.0 전 breaking 허용) · 회사 리포 폴더명 공개 유지 · 루트 MIT 범위.

## PR #110 마감

- CI 4잡 통과 → 사용자 지시로 `gh pr merge --rebase --delete-branch`. main 해시: Skeleton `ea96f78` · 갤러리 `4e9f972` · 실측 `029d668` · 리뷰 `3e15f6a`–`11177b1` · 회귀 수정 `068f491` · opacity 0.6 `54ea48d`(docs `c3b4de3`). 위 절과 todo P4-7의 해시는 main 해시로 정정(이 브랜치 feat/ui-toast 첫 커밋).

## P4-8 Toast + useToast — feat/ui-toast

참여: 메인 에이전트(구현) · reviewer(리뷰) · 사용자(결정).

### 구현 (커밋 3)

- 판정: **래핑** — Base UI 1.8 `@base-ui/react/toast`에 Provider·Viewport·Root·Title·Description·Close·`useToastManager` 전부 있음.
- `ba47385` feat(ui): `primitives/Toast/`(ToastProvider: position top-right|bottom-right · limit 3 · timeout 5000 · label '알림' · closeLabel '닫기') + `hooks/useToast/`(`toast({title,description?,tone?,duration?})`→id · `close(id?)`) + 토큰 `--ui-toast-width` 320. tone → Base UI `type`(data-type)·`priority`(danger·warning=high → `alertdialog`). Title은 h2 → div(Popover와 같은 판단).
- `655b994` 갤러리 섹션(자기 Provider, tone 4버튼 duration 0·모두 닫기) · `3e32b09` `toast.mjs` 16검사(202→218).
- **Base UI Toast에서 알게 된 것**: 닫기 버튼은 뷰포트가 펼쳐지기(hover·포커스) 전까지 `aria-hidden`(보조 기술은 Esc·F6) → 테스트는 `getByLabelText` · high priority 토스트는 포커스 전까지 `aria-hidden`이라 role+name 조회 불가 · 갤러리에 `role=region`이 둘(SplitPane 목록)이라 뷰포트는 `[role="region"][aria-live]`로 · CDP에서 React onMouseEnter는 `mouseover`(bubbles) 디스패치로.

### 리뷰 — high 0 · med 3 · low 11 → 사용자 "추천대로, M1은 (b)까지"

- **M1** `useToast` 참조 안정 주장이 부정확 — Base UI `useToastManager`는 목록이 바뀔 때마다 새 객체라 소비자가 매번 리렌더. **(b) 반영**: Provider가 `createToastManager`를 한 번 만들어 `toastManager`로 넘기고 자체 컨텍스트(`ToastContext.ts`, 배럴 비공개)로 `useToast`에 준다 — 참조 안정, 목록 변화에 리렌더 없음(테스트로 고정). reviewer가 걱정한 "자식 마운트 effect에서 띄운 첫 토스트 유실"은 **실제로 일어남**(React는 자식 effect를 부모보다 먼저 돌려 Base UI 구독 전에 발행) → 구독 전 명령을 모아 뒀다 흘려보내는 버퍼 매니저로 막음(버퍼 없이 실패 확인). `0daf4d0`
- **M2** 뷰포트가 TopBar에 겹침 — body 포털이라 소비자가 감싸서 못 바꿈. 토큰 `--ui-toast-inset`(사방) + `--ui-toast-inset-top`(위만, 기본 = inset) 추가, 대시보드 globals.css가 `calc(TopBar + inset)`으로 덧씌움. 처음엔 토큰 하나로 했다가 오른쪽 여백까지 64px가 돼 둘로 나눔(amend). `2cc4d02`
- **M3** 실측 "최신이 맨 위"가 DOM 순서만 봄(column-reverse여도 통과) → top 좌표 비교, 간격은 화면 순서로 정렬. **L8** 제목은 `aria-labelledby`로. `8cd1502`
- **L1** `type in ICONS` 프로토타입 포함 → `Object.hasOwn` `8b65004` · **L2** 아이콘 정렬 `16px` 리터럴 → 줄 높이 상자(InlineAlert 방식) + **L11** limited 단순화 주석 `5c0777f` · **L4** 훅 타입 import를 폴더 index로(M1 커밋에 포함) · **L5** 배럴 hooks 블록을 끝으로 `12cf9b2` · **L7①②③** F6 포커스·description 없음·hover 타이머 정지 테스트 `2e4dfd9` · **L7④+L9** 갤러리 위치 토글(우상단↔우하단, Provider 둘이면 F6 리스너가 싸우므로 토글 하나) + bottom-right 실측 3검사, 미사용 `data-demo="toast-count"` 제거 `59bacd4`
- 기록만: **L3** `z-index: 2` 매직 넘버(다른 팝업은 1) → RF1 입력(`--ui-z-*`), 모달 Dialog 열린 동안 토스트 낭독 여부는 README 주의 후보 → P5a · **L6** `ToastTone`=`InlineAlertTone`·아이콘 매핑 중복 → RF1 어휘 · **L10** 파일명 `Toast.tsx` ↔ export `ToastProvider` → RF1 폴더 규칙.

### 검증

- ui test **281**(+23: Toast 17 + useToast 6) · pipeline 254 · dashboard 245 · lint · typecheck · prettier · ui build · `verify:layout` **222/222**(+20). 라이트·다크 스크린샷(limit 초과 숨김·TopBar 아래 시작·우하단 토글 확인).

### 미결·다음

- PR → CI → 사용자 승인 뒤 rebase merge → **P4-9 useAwaitDialog + useConfirm**.
- 앱 셸에 `ToastProvider`를 넣는 것은 별도 작업(소비자가 생길 때 — P4-11 뒤 또는 P7).

## PR #111 마감

- CI 4잡 통과 → 사용자 지시로 `gh pr merge --rebase --delete-branch`(main `b0ea69a`). main 해시: Toast `ba47385` · 갤러리 `655b994` · 실측 `3e32b09` · 리뷰 `0daf4d0`(M1)·`2cc4d02`(M2)·`8cd1502`(M3+L8)·`8b65004`(L1)·`5c0777f`(L2+L11)·`12cf9b2`(L5)·`2e4dfd9`(L7)·`59bacd4`(L7④+L9)(docs `ba332be`·`b0ea69a`). 위 절과 todo P4-8의 해시는 main 해시로 정정(이 브랜치 feat/ui-await-dialog 첫 커밋).

## P4-9 useAwaitDialog + useConfirm — feat/ui-await-dialog

참여: 메인 에이전트(구현) · reviewer(리뷰) · 사용자(결정 "추천대로").

### 구현 (커밋 4 + docs 2)

- `e099b6c` docs — PR #111 마감(P4-8 main 해시 정정, 위 "PR #111 마감" 절).
- `f89dc1f` feat(ui) — Button `danger` variant(채운 빨강 + `accent-fg` 글자, hover 토큰). useConfirm `tone: 'danger'`의 확인 버튼에 쓸 것이 없어 먼저 추가. 갤러리 Button 섹션에 예시.
- `82df2a1` feat(ui) — `hooks/useAwaitDialog/`(index · useAwaitDialog.ts · useConfirm.tsx · 테스트 2 · 스토리 1). **스펙과 다른 점 둘**: ① `useAwaitDialog<T>(cancelValue)` — Esc·바깥 클릭·닫기·재open·언마운트가 전부 "cancel 값"으로 resolve해야 하므로 훅이 그 값을 알아야 한다(스펙 시그니처는 `()`). ② 렌더 함수가 `(resolve)`가 아니라 `{ open, onOpenChange, resolve, cancel }`을 받는다 — Dialog에 `open`/`onOpenChange`를 그대로 넘겨야 Esc 처리와 닫힘 애니메이션(open=false 렌더)이 되기 때문. `useConfirm()` → `{ confirm(options), element }`, 기본 라벨 '확인'·'취소'.
- `62fe22a` 갤러리 "useConfirm · useAwaitDialog" 섹션(삭제 확인 danger · 이름 입력 `useAwaitDialog<string|null>`) + `await-dialog.mjs` 11검사(222→233). `ed43dc0` decisions/ui-package-boundary 갱신 이력(react-await-dialog 단독 패키지 대체).
- **happy-dom 함정**: 모달이 열린 동안 바깥 버튼은 Base UI가 `aria-hidden` → `getByRole(..., { hidden: true })`로 조회(재open 테스트).

### 리뷰 — high 0 · med 4 · low 12 → 사용자 "추천대로"

reviewer가 훅 로직을 React 19 + happy-dom 스크래치로 실측: Promise 정리(재open·언마운트·settle 뒤 stray onOpenChange)·StrictMode·cancelRef stale·대비(라이트 6.47:1 · 다크 7.48:1) 전부 문제 없음.

- **M1** 렌더 함수가 돌려준 컴포넌트의 `useState`가 open 세션을 넘어 남음(**확인된 버그** — element가 첫 open 뒤 null로 안 돌아가고 같은 컴포넌트가 같은 자리라 update 취급. 이름 입력 예제에서 편집→취소→다시 열면 버린 초안이 보임). (a) 훅이 open()마다 세션 key로 서브트리 재마운트 / (b) 문서로만. **(a) 채택** — 소비자가 밟기 쉬운 함정은 훅에서 막는다. `b200942`(상태 초기화 테스트로 고정).
- **M2** danger 버튼 배경이 `--ui-color-danger-fg`(글자색 토큰) — primary는 accent/accent-hover/accent-fg 3종이 짝인데 danger만 채움 토큰이 없음. 0.1.0 뒤엔 토큰이 공개 API → **지금** `--ui-color-danger`(채움, 값은 danger-fg와 동일) 추가. `cb247cb`.
- **M3** 초기 포커스가 header 닫기 버튼(Base UI 기본 = 첫 tabbable). 위험하진 않지만(Enter=닫기=취소) 키보드로 확인까지 Tab 2번. **이 PR에서**: Dialog `initialFocus?: RefObject | false` prop(`c032c78`) + useConfirm이 danger면 취소·기본이면 확인에 포커스(APG) `b25d13e`. 반영 중 python 치환이 prettier 줄바꿈에 빗나가 본문이 그대로였던 것을 프로브로 잡음(재현 3종 전부 정상 → 파일 확인).
- **M4** 테스트 누락: 바깥(backdrop) 클릭→cancel · 닫힌 뒤 포커스가 호출 버튼으로 복귀. + **L9** "element null" 단언을 data-testid로 강화. `c672391`.
- **L2** 언마운트 뒤 stale 클로저로 open()이면 영원히 pending → `cancelValue`로 즉시 resolve(b200942에 포함) · **L1·L4·L5** JSDoc: 자식 마운트 effect에서 열면 StrictMode에서 첫 Promise가 취소됨 · settle 직후 연쇄 open은 닫히지 않고 내용만 바뀜 · 렌더 함수는 훅 금지·상태는 컴포넌트로·클로저는 open() 시점 고정(b200942) · **L6** await-dialog.mjs 두 번째 locate 에러 검사 `481075b`.
- 기록만: **L3** cancelRef useEffect 갱신(첫 렌더는 useRef 초기값이라 stale 아님) · **L7** P4-11 입력(아래) · **L8** `ConfirmTone`이 Toast·InlineAlert `tone`·Button `variant`와 또 다른 어휘 → RF1 ⑨ · **L10** 기본 라벨 한국어 하드코딩(Dialog·Toast 선례) → RF1/P5a · **L11** Button 어느 variant에도 `:focus-visible` 없음(UA 링 의존) → RF1 ② · **L12** todo 스펙 시그니처와 구현 차이 → todo에 한 줄.

### 검증

- ui test **301**(+20: Button +1 · Dialog +1 · useAwaitDialog 12 · useConfirm 6) · pipeline 254 · dashboard 245 · lint · typecheck · prettier · ui build · `verify:layout` **233/233**(+11, 리뷰 반영 뒤 재실행).

### 미결·다음

- PR → CI → 사용자 승인 뒤 rebase merge → **P4-11**(RunActionBar useConfirm 치환). P4-10은 P5 PR에.
- Base UI Toast처럼 여기서도 갤러리 Dialog 데모의 초기 포커스는 닫기 버튼 그대로(initialFocus 미지정) — 의도.

## PR #112 마감

- CI 4잡 통과 → 사용자 지시로 `gh pr merge --rebase --delete-branch`(main `0c25f93`). main 해시: Button danger `f89dc1f` · 훅 `82df2a1` · 갤러리+실측 `62fe22a` · decisions `ed43dc0` · 리뷰 `cb247cb`(M2)·`c032c78`(M3 Dialog)·`b25d13e`(M3 useConfirm)·`b200942`(M1+L2)·`c672391`(M4+L9)·`481075b`(L6)(docs `e099b6c`·`0c25f93`). 위 절과 todo P4-9의 해시는 main 해시로 정정(이 브랜치 refactor/dashboard-use-confirm 첫 커밋).
- 머지 전 사용자 질문: 회사 프로젝트의 `useDialog`(전역 스토어 + Promise, alert·confirm·prompt, 문자열 인자)와 같은가. 회사 리포는 클린룸 규칙대로 열지 않고 사용자가 스크린샷으로 훅 코드만 보여줌 → 설계 비교만(같은 점: Promise·훅·reject 없음 / 다른 점: 렌더 자리(전역 스토어 vs `{element}`)·UI 자유도·결과 타입 제네릭·재open 시 이전 Promise 처리·닫힘 중 내용 유지·리렌더 범위·alert 유무). 사용자 "커스텀 element를 넣을 수 있으면 됐다" → 현 설계 유지, Provider·문자열 축약·alert는 P4-11 `/decide` 후보.

## P4-11 RunActionBar useConfirm 치환 + useAlert — refactor/dashboard-use-confirm

참여: 메인 에이전트(구현) · reviewer(리뷰) · 사용자(결정).

### 결정 (`50dcb27`, decisions/confirm-dialog-usage.md)

- 착수 전 `/decide` 여섯 가지 → 사용자 "추천대로, ②는 설명 뒤 (a)+상태 문구, ⑥은 지금": ① footer 버튼 md 통일 · ② 확인 즉시 닫힘 + API 도는 동안 바 컨트롤 비활성 + `role=status` 진행 문구 · ③ `ConfirmOptions.children` · ④ Provider 보류(나중에 호환되게 추가 가능) · ⑤ 문자열 축약 없음 · ⑥ **useAlert 지금 추가**(사용자가 보류 추천을 뒤집음 — 사이드 프로젝트 `useDialog`와 구색).
- ②의 설명 포인트: 훅이 비동기 작업까지 알면 얇은 훅에서 벗어나고 Promise 결과가 boolean이 아니게 됨. 사이드 프로젝트도 확인 시점 resolve 구조였음.

### 구현 (커밋 4)

- `c2167a8` ui — `ConfirmOptions.children?`(Dialog children 슬롯; description은 `<p>`라 `<dl>` 불가) + 테스트.
- `7f2025b` ui — `useAlert()` → `{ alert(options): Promise<void>, element }`. `useAwaitDialog<void>(undefined)` 위에 Dialog + 확인 버튼 하나(초기 포커스 확인). 테스트 3·스토리·배럴.
- `745e988` 갤러리 훅 섹션에 "저장 안내" 데모(`닫음 N회`) + `await-dialog.mjs` alert 3검사(233→236).
- `829a0e2` **RunActionBar**: `useTransition` + `useState(plan·approveOpen)` + Dialog JSX 2개 → `useConfirm` 하나 + `busy: string | null`(진행 문구 겸 비활성 플래그). `useTransition`은 확인 대기 중에도 pending이 켜져 못 씀. 기존 테스트 9건 그대로 통과 + 진행 문구 1건.

### 리뷰 — high 0 · med 3 · low 8 → 사용자 "그렇게 하자"

- **M1** 확인 즉시 닫히며 Base UI가 되돌리는 포커스가 `disabled`가 된 버튼에 막혀 body로 떨어짐(키보드 회귀, 이전엔 Dialog가 API 동안 열려 있어 없던 문제). (a) busy 해제 다음 렌더에서 눌렀던 버튼에 포커스(effect) 채택. **happy-dom은 disabled 버튼에도 focus()가 먹어 회귀 자체는 재현 불가** — 테스트는 복귀 effect 경로만 고정(주석에 명시), Chrome 실기 확인은 미완. `06fd6d1`
- **M2** 요청 중 다른 실행을 고르면(page.tsx `key={selected.id}`) 바가 언마운트되는데 응답 뒤 `router.push`가 옛 실행의 다음 시도로 끌고 감 — **기존 useTransition 판에도 있던 버그**. mounted 가드 + 테스트. API 호출을 `runBusy(text, call, focusTarget)` 헬퍼로. `06fd6d1`
- **M3** `role=status`의 `:empty { display:none }`이 live 영역을 트리에서 제거(기존 문제, 진행 문구까지 의존하게 됨) → 제거. 빈 `<p>`는 margin 0이라 높이 0. VoiceOver 실측은 P7h. `1af651e`
- **L4·L5** 재실행 in-flight 문구 2단계 테스트 + 실패 3건에 컨트롤 재활성 단언, `clickDialogButton`을 `within(dialog).getByRole`로 `9f6f40a` · **L1·L3** `resolve()` + JSDoc "alertdialog가 아니다(끼어들기 낭독 없음, 바깥 클릭·Esc로 닫힘)" `554c285` · **L7·L8** run.mjs 헤더·decisions 결정 2 문구를 busy 구현에 맞춤 `472aa28`.
- 기록만: **L2** useAlert·useConfirm의 Dialog+footer+tone→variant 매핑 중복, `ConfirmTone`을 useAlert가 역방향 import → RF1 ⑫에 합침 · **L6** 비동기 onClick 패턴 둘(갤러리 `async () =>` vs 바 `() => void onX()`) → RF1.
- 문제 없음 확인: `run-api-client`는 fetch 예외·봉투 아닌 응답을 `REQUEST_FAILED` 값으로 돌려줘 `void onX()`에 미처리 reject 없음 · 연속 클릭은 첫 클릭 setBusy가 동기 flush돼 두 번째는 disabled · `setBusy(null)`→`confirm()` 사이 틈 프레임 없음.
- **happy-dom 함정 재확인**: 모달 열린 동안 바깥 `role=status`도 aria-hidden → `getByRole('status', { hidden: true })`.

### 검증

- ui test **305**(+4: useConfirm children 1 · useAlert 3) · pipeline 254 · dashboard **249**(+4: RunActionBar 9→13) · lint · typecheck · prettier · ui build · `verify:layout` **236/236**(+3).

### 미결·다음

- PR → CI → 사용자 승인 뒤 rebase merge. 다음은 **RF1~3**(배포 전 리팩토링) → P5 → P6. P4-10(ui-engineer-todo "남은 것: Popover" 정리)은 P5 PR에.
- M1의 Chrome 실기 확인(승인 → Enter 확인 → 실패 응답 뒤 `document.activeElement`)은 P7h 접근성 실측에 같이.

## PR #113 마감

- CI 4잡 통과 → 사용자 지시로 `gh pr merge --rebase --delete-branch`(main `1a620d5`). main 해시: decisions `50dcb27` · children `c2167a8` · useAlert `7f2025b` · 갤러리+실측 `745e988` · RunActionBar 치환 `829a0e2` · 리뷰 `06fd6d1`(M1+M2)·`1af651e`(M3)·`9f6f40a`(L4+L5)·`554c285`(L1+L3)·`472aa28`(L7+L8)(docs `25a2682`·`1a620d5`). 위 절과 todo P4-11의 해시는 main 해시로 정정(이 브랜치 refactor/ui-pre-release 첫 커밋).
- 사용자 요청으로 npm 배포까지 남은 TODO 정리(RF → P5 → P6, 배포 전 사용자 판단 3건: RF2 호환 깨는 API 허용 · 공개 리포 상태 2건 · P6d publish 확인) → RF1 착수 승인.

## RF 배포 전 리팩토링 — refactor/ui-pre-release

참여: 메인 에이전트(RF3 구현) · reviewer(RF1 감사) · 사용자(RF2 결정).

### RF1 — reviewer 감사(코드 수정 없음): 37항목, high 0

관점 5개(API 일관성·스타일·경계·테스트·산출물) + P4 입력 ①~~⑭. 산출물 확인: 빌드 정상, Base UI·lucide external, `"use client"` 배너, d.ts에 스토리·테스트 안 샘, pack 85파일 121.8kB(README 없음 → P5a). 목록은 RF-01~~37(아래 반영·보류·기각에 번호로).

### RF2 — 사용자 결정

- **0.1.0 전 호환 깨는 API 정리 허용**(소비자가 dashboard뿐, 배포 뒤엔 major). "호환을 깬다"의 의미를 설명한 뒤 결정.
- 추천대로 전부, 단 **RF-11(`isActive`·`isLast` 접두)만 현행 유지**(취향 차이에 변경 폭이 큼). 결정 필요 항목: RF-03 a(aria-label 통일) · RF-04 a(Badge tone) · RF-14 a(영어 기본 라벨) · RF-16 a(문서에 예외 명시) · RF-22b a(토큰화만) · RF-24 a(비활성 선택 통일) · RF-27 a(Toast 폴더 개명) · RF-32 a(caret) · RF-33 a(d.ts 한국어 유지) · RF-36 a(소스맵 유지).

### RF3 — 항목당 커밋(24 + 갤러리 라벨 1)

- 경계·정리: RF-29 `.gitkeep` `036c725` · RF-28 TimelineItem `'use client'` + 배너 규칙 `d4d6b9a` · RF-30 JSDoc `33f809c` · RF-31 d.ts·토큰 주석 도메인 단어 `1fb1a8a` · RF-37 exports `./package.json` `9ccfa87` · RF-32 caret + toolchain-pins 절 `b6c8078`.
- API additive: RF-09 className 3개 `1428a43` · RF-13 href `2956b46` · RF-12 Button render mergeProps(버그 수정 성격) `43f990b` · RF-08 타입 export 6개 `75591b0` · RF-05 `src/internal/tone.ts` `0cc0c83`.
- API breaking(대시보드 같은 커밋에서 따라감): RF-04 Badge `variant`→`tone` + `BadgeTone`(대시보드 lib 2·app 4·갤러리 14) `b84398f` · RF-03 ToastProvider·ActionBar `label`→`aria-label` `4ee2abe` · RF-07 Skeleton `full`→`pill` + `lg` `243b501` · RF-14 영어 기본 라벨 + `closeLabel` 옵션(대시보드·갤러리 한국어 지정) `c482398`.
- **RF-01/02는 스펙과 다르게 반영**: 판별 유니온은 FormField 안(Field.Label이 이름, children·aria-label 둘 다 없음)을 깨뜨려 불가 → `FormFieldContext`를 null=밖으로 바꿔 위치를 알고, FormField 밖 + 이름 없음이면 **개발 모드 console.warn**(`src/internal/accessibleName.ts`). NODE_ENV 분기는 Vite 라이브러리 모드가 'production'으로 굳히므로 `define`으로 보존(React·Base UI 관례). RadioGroup `aria-label` 선택으로. `0cfb136`
- 스타일 토큰: RF-18 모션 토큰 + reduced-motion 한 곳(펄스는 `animation-play-state` 토큰으로 정지) `b370894` · RF-20 z 토큰 `9a8bf50` · RF-19 포커스 링 토큰 + 6곳 추가 `e492966` · RF-22 치수 토큰 + ui-style "px 허용 범위" `c48968b` · RF-24 비활성 선택 통일 `e3f5e03`.
- 구조·문서·테스트: RF-27 `primitives/ToastProvider/` `0e46952` · RF-16/17/10 문서 `a09bb98` · RF-34/35 테스트 14건 `aa4b190`(**Tabs 방향키는 happy-dom에서 Base UI 포커스 이동이 재현되지 않아 제외** — Chrome 실측 후보) · 갤러리 Badge 소제목 `3271ade`.
- 작업 중 잡은 것: RF-03 커밋에 Toast 테스트 `label` 하나가 빠져 typecheck가 깨진 채 들어감 → fixup 후 autosquash. 원인은 `vitest | grep` 파이프가 종료 코드를 가린 것 — 이후 exit code 직접 확인으로 바꿈. `pnpm typecheck`의 dashboard는 ui `dist` 타입을 읽으므로 ui 변경 뒤엔 ui build 먼저.

### 보류(P7로) · 기각

- 보류: RF-15 비동기 onClick 패턴(type-aware lint 결정) · RF-21 Input/Select/Textarea 필드 스타일 3벌 · RF-23 아이콘 size 리터럴 · RF-26 disabled 표현 통일 · RF-31 스토리·테스트 샘플 도메인 단어 · RF-11 boolean 접두(현행) · RF-22b Button 36 vs 컨트롤 32 높이 · RF-33 d.ts JSDoc 영어화 · RF-10 rest props 코드 정리 · Tabs 방향키 Chrome 확인.
- 기각: RF-06 ConfirmTone 어휘(현행 타당) · RF-25 invalid 표현(기존 결정 재확인).

### 검증

- ui test **329**(+24) · pipeline 254 · dashboard 249 · lint · typecheck · prettier · ui build · `verify:layout` **236/236** · pack 87파일 127.7kB.
- 갤러리 스크린샷 전후(24장) 바이트 비교: 동일 14 · 차이 10. 차이는 (a) RF-24 비활성 선택 표현(radio·switch·form-field 장면, 승인된 시각 변화) (b) 펄스 배지 애니메이션 프레임(shell-scroll·split-pane·menu-4·select-8·popover — 눈으로 동일).

### 미결·다음

- PR → CI → 사용자 승인 뒤 rebase merge → **P5**(README `docs/ui-readme`, P4-10 포함) → P6.
