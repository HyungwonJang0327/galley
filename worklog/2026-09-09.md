# 2026-09-09

## 네비게이션 IA 구현 — AN1·AN2·AN3·AN6 (뼈대)

결정·계획은 [2026-09-08](./2026-09-08.md) "네비게이션·화면 구조 재정비" 참조. 이 세션은 그 구현.

### 참여

- 메인 세션 단독(사용자 승인 + 실행). 서브에이전트 미사용.

### 구현·결정

- **AN1**(`db2e8d6`): TopBar 워크스페이스 탭(글/설정) 제거. `TopBar.tsx`만 수정 — pill 2개·`isSettings`·미사용 import(useRouter·usePathname) 제거, 햄버거·Galley·모델 칩만 남김. CSS·다른 파일 무변경(워크스페이스 개념이 TopBar.tsx에 국한됨을 사전 조사로 확인).
- **AN2**(`1b31aef`): 사이드바를 사용 흐름 순으로 교체(주제:큐 / 실행:실행·이력 / 발행:발행 대기 / 설정 3 + 외부 2). `NavItem`에 `badge?` 자리 추가·Sidebar에서 전달(SidebarItem이 이미 배지 지원). 구 경로 5개 페이지 삭제 + `next.config` 308 redirect. `/design` dev 전용·`isNavItemActive`는 보존.
- **AN3**(`2b4a51a`): 루트 `page.tsx`를 승인 대기 유무로 분기(≥1→/runs, else /queue). `lib/nav-counts.ts`의 `getPendingApprovalCount()`가 서버에서 조회(decisions/navigation.md), Run 스키마(B1e) 전까지 0 반환 → 현재 사실상 /queue.
- **AN6**(`6bd4c6c`): `/runs/history`·`/publish` 빈 페이지(Placeholder) 신설 → AN2에서 사이드바가 가리키던 두 링크의 404 해소.
- **AN4·AN5 보류**: 둘 다 **A4b(주제_큐.md 파서)·A5a(ListToolbar·ListRow)** 미완에 의존. 가짜 데이터/임시 행을 넣으면 A4b·A5a 도착 시 버려질 코드라, 사용자 선택으로 **뼈대(AN1·2·3·6)를 먼저 PR**하고 AN4/AN5는 A4b→A5a 뒤 후속으로.

### 검증

- 각 단계 lint·typecheck·format·build 통과. `next-env.d.ts`는 빌드 자동생성 노이즈라 커밋서 제외.
- 프로덕션 서버 스모크: 구 경로 5개 전부 308(→/queue·/runs·/publish), `/`→307 `/queue`, `/publish`·`/runs/history` 200.
- 포맷 함정: 문서 커밋의 navigation.md·planning.md prettier 드리프트를 발견해 정규화 후 문서 커밋에 amend(push 전).

### 미결·다음

- 브랜치 `refactor/dashboard-nav-ia` → PR → CI → rebase merge.
- **AN4/AN5**: A4b(파서)·A5a(리스트 UI) 구현 후 큐 탭·행 메뉴를 실데이터로 완결. 배지 실값도 이때(대기 n) / B1e(승인 대기 n) / Phase 2(미발행 n) 주입.

## 주제_큐.md 파서 — A4b·A4c

### 참여

- 메인 세션 단독. TDD(superpowers) 스킬로 진행.

### 구현·결정

- **형식 파악**: 실제 `~/Desktop/blog/주제_큐.md`(읽기만)와 queue-sync-direction 결정 확인. 구조 = 서문(H1+안내) → `## 대기/후보/보류/완료`, 후보 섹션에 `### 카테고리` 소제목, 완료 항목은 `- YYYY-MM-DD 제목` 날짜 접두. 섹션 앞뒤·`###` 뒤 빈 줄이 원본에서 불규칙.
- **라운드트립 기준**: 결정 문서가 "구조상 동일"(섹션 구조·줄 순서·완료 날짜 유지)로 못박음 → 바이트 완전동일이 아니라 **구조 보존 파서·라이터**. 빈 줄 등 표기는 정규화. 파서가 빈 줄을 무시하므로 표기 차이는 무해.
- **A4b**(`0b2534d`): `queueFile.ts`의 `parseQueue`/`serializeQueue`(순수 함수, 서문·카테고리·완료 날짜 보존) + `Storage` 인터페이스·`LocalFsStorage`(BLOG_DIR 아래 주제_큐.md 읽기/쓰기, blogDir 주입). 배럴 노출. TDD로 단위 테스트 9개(RED→GREEN 확인).
- **A4c**(`d73e72c`): 실제 구조를 본뜬 픽스처(`__fixtures__/주제_큐.sample.md`)로 read→write→re-read 동일·섹션 이동 편집 무결성·완료 날짜 보존 테스트 3개(누적 12개).
- **strict 함정**: `noUncheckedIndexedAccess`로 정규식 캡처가 `string|undefined` → 폴백(`?? ''`)으로 좁힘. 픽스처는 prettier가 `###` 뒤 빈 줄을 넣어 정규화(serializer 출력과 동일).

### 검증

- lint·typecheck(3 프로젝트)·format·test(pipeline 12 + ui 30) 통과. `next-env.d.ts` 자동생성분은 커밋 제외.

### 미결·다음

- `feat/queue-file-parser` → **PR #30 머지 완료**(main `f1d3b96`·`192b8e0`·`829207a`).
- 다음 세션: **A4d**(파일→DB 최초 임포트 + 로드 시 재적재)부터. **결정(2026-09-09): 표시 정보 최대 보존 → QueueItem 스키마에 `category`·`completedOn` 컬럼 추가 + 마이그레이션 후 적재**(파서 QueueTopic 전 필드 보존). category/completedOn이 A4a 스키마에 없어 확장 필요.

## 주제_큐.md → SQLite 적재 — A4d

### 참여

- 메인 세션 단독. TDD(superpowers) 스킬로 진행. 설계 갈림길 2건은 사용자 결정.

### 구현·결정

- **선행**: PR #31(A4d 스키마 결정 기록)이 이미 main에 rebase merge된 상태였음을 확인 → 로컬 `docs/a4d-schema-decision` 브랜치 정리 후 `feat/queue-db-import` 시작.
- **재적재 전략(사용자 결정)**: 로드 시 **전체 리셋**(트랜잭션 `deleteMany`+`createMany`). 부분 diff/upsert는 참조 없는 현 시점 과설계(YAGNI). 파일=진실에 충실.
- **주제 키(사용자 결정)**: 전체 리셋은 `QueueItem.id`(cuid)를 매 로드 재생성 → id는 재적재에 불안정. **Phase 1-B에서 Run이 주제 참조 시 id 대신 제목 파생 슬러그를 키로** 쓰기로 decisions/queue-sync-direction.md에 미리 기록.
- **커밋 0**(`1a7d0f7`, docs): 위 두 결정을 queue-sync-direction.md에 "적재 전략(A4d)"·"주제 키는 슬러그" 절로 추가.
- **커밋 1**(`a9419bd`, feat): `QueueItem`에 `category String?`·`completedOn String?` 컬럼 + 마이그레이션(`20260909005225_add_queue_display_fields`). nullable 2열 추가 SQL.
- **커밋 2**(`03c7deb`, feat): `parsedQueueToRows`(ParsedQueue→행 배열 순수 변환, 섹션별 status·0기반 order, 필드 보존) + `importQueueFromFile`(read→parse→변환→트랜잭션). 배럴 노출.
- **테스트 전략(사용자 결정)**: 순수 변환은 단위 테스트, `importQueueFromFile`은 **실제 임시 SQLite 통합 테스트**. tmpdir DB + `prisma migrate deploy` + 실제 PrismaClient — 새 라이브러리 없이 프로젝트 첫 DB 통합 테스트 인프라를 깜. B1e Run 테스트에서 재사용.
- **TDD 함정**: 순수 변환 GREEN 단계에서 `importQueueFromFile`을 미리 써버려 철칙 위반 → 삭제하고 통합 테스트 RED(`is not a function`) 확인 후 재구현.

### 검증

- lint·typecheck(3 프로젝트)·format·test 통과. pipeline 16개(기존 12 + 신규 4: 순수 변환 2 + 통합 2), ui 30개. `dev.db`는 gitignore 확인.

### 미결·다음

- `feat/queue-db-import` → PR → CI → rebase merge.
- **슬러그 파생 구현은 B1e**(Run 스키마)에서. A4d는 컬럼만 추가하고 슬러그는 참조 생길 때.

## BM1 인터페이스(사용자 작성) + 근거 수집 구조 결정 — 세션 마감

### 참여

- 메인 세션 단독. 🔒 `ModelAdapter` 인터페이스는 사용자가 직접 작성, AI는 확인·포맷·커밋만.

### 논의·결정

- **BM1 인터페이스**(`feat/model-adapter` 브랜치 `c906727`, 미푸시): 사용자가 `packages/pipeline/src/model/ModelAdapter.ts` 작성. 작성 중 "Parsing error: Invalid character" — 원인은 `Promise<` 의 `<`가 한글 입력기에서 들어간 `‹`(U+2039). 문자 하나만 교체. 내용은 model-selection 확정 shape와 일치, ESLint·tsc 통과, Prettier 줄바꿈만 정규화. Mock 어댑터·테스트·배럴 export는 **다음 커밋**(BM1 나머지).
- **근거 수집 구조 설계**(사용자 프롬프트 기반, `/decide`): 원칙 "캐시는 어디를 볼지, 내용은 원본에서". 리포 인덱스(RepoAnalysis + pointers ≥ 1) → 주제 연결(TopicAnalysisLink) → 근거 수집(원본 조각) → 본문 → **근거 검증**(6단계) → VerificationReport. AI가 문서를 읽고 발견한 충돌 6건을 제시: 발행정보에 근거 목록이 실제로 없음(신설), 전체 리셋 적재와 연결 저장 충돌(슬러그 키 별도 테이블), 클린룸(런타임 인덱싱은 별개, 조각 유출 차단), INTENT 5단계 문구, 워커 동시 1개(Run 우선), 상태 머신 플래그.
- **사용자 결정(선택지 표 9건)**: 상한 A(16KB/20개·160KB/30개) · 인덱싱 모델 Haiku 4.5 기본 + CLI `--model` + Phase 2 Dialog Select + `Repo.lastIndexModelId` · discovered 키워드 점수만(상한 8) · 주장 추출 정규식+모델 · 본문 밑줄 Phase 2 · alias=`Repo.aliases` · 연결=`TopicAnalysisLink` · **snippet은 `DATA_DIR`에만, posts엔 포인터만**(추천 a를 뒤집음 — 회사 코드가 blog 폴더 밖으로 나갈 경로 원천 차단) · INTENT 6단계·7개 파일 갱신.
- **Haiku id 확인**: 추측 금지 지시에 따라 Anthropic 모델 문서에서 `claude-haiku-4-5-20251001`(alias `claude-haiku-4-5`, $1/$5, 200K) 확인. 은퇴 "2026-10-15 이전 없음" → 레지스트리 상수 한 줄 교체로 대비.
- 상태 머신(🔒 B1a) 인터페이스 요구사항 6개를 decisions/evidence-collection.md에 기록(단계 6·표시용 플래그·skipped·재실행 입력·승인 게이트·실행 상태 분리). 열린 판단: 본문 재실행 시 파생 단계(링크드인·Zenn) 자동 재실행 여부 — 현재는 verify만 자동.

### 검증

- 문서 PR #34(`566260a`, rebase merge, 브랜치 삭제): prettier·gitleaks·verify 통과. 제품 코드 변경 0.

### 미결·다음

- **재시작 지점**: `feat/model-adapter`를 main(`566260a`)에 rebase → BM1 나머지(Mock 어댑터·테스트·배럴 export) → BM2 레지스트리(`indexingDefault` 포함) → BM3 어댑터 7개. BE1은 BM1~BM3 뒤 + 사용자 "BE1 시작" 승인.
- 사용자: 🔒 상태 머신(B1a)을 요구사항 6개로 작성. 🔒 인터페이스 외 핵심 모듈 없음.
- `feat/home-dashboard` 로컬 브랜치는 main에 흡수됨 → 삭제 가능.
