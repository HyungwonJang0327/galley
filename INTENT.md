# INTENT.md — Galley

> **왜**만 담는 문서. 코드가 바뀌어도 이 파일은 거의 바뀌지 않는다.
> 무엇이 확정됐나 → planning.md / 왜 그렇게 정했나 → decisions/

**Galley**(교정쇄, galley proof): 조판은 끝났으나 발행 전이라 사람이 검수하는 인쇄 단계.
이 대시보드의 핵심 상태 — AI가 초안을 조판하고, 사람이 검수·승인한 뒤에야 발행으로 넘어간다.

## 문제

기술 블로그 초안을 `~/Desktop/blog/주제_큐.md`를 손으로 편집해 관리하고, 수·토 18:00 스케줄이 큐 맨 위 주제로 5종 산출물(벨로그 본문·링크드인 요약·Zenn 일본어판·발행정보·썸네일)을 만든다. 실행 이력·상태·검수 결과가 여러 파일에 흩어져 무엇이 어디까지 진행됐는지 한눈에 안 보인다. 온디맨드로 상위 모델을 돌려 검수하고 싶어도 실행→검수→재실행→발행 준비를 이어줄 도구가 없다.

## 목표 (검증 가능)

- 큐 편집(추가·순서 변경·후보↔대기↔보류 이동)이 대시보드에서 되고 `주제_큐.md`와 양방향으로 어긋나지 않는다.
- "실행" 시 단계별(근거 수집 → 벨로그 본문 → 링크드인 → Zenn → 발행정보·썸네일) 진행이 타임라인에 보이고, 산출물이 `posts/<슬러그>/`에 기존 5개 파일 구조 그대로 떨어진다.
- 승인 대기 상태에서 "수정 지시"를 입력하면 해당 단계만 재실행된다.
- 승인 후 Zenn에 下書き(초안)로 push된다. 공개 발행은 하지 않는다.
- 실행 이력(시각·모델·토큰·비용·결과)이 SQLite에 남고 목록으로 조회된다.
- 모델을 어댑터로 교체해도 파이프라인 코드가 바뀌지 않는다.

## 비목표 (하지 않을 것 + 이유)

- **자동 공개 발행(velog 공개, Zenn 公開)** — 검수 없는 발행은 이 도구의 존재 이유(사람 검수)와 정면으로 충돌한다. 항상 사람 승인 뒤 下書き까지만.
- **Phase 1~2의 배포·인증·멀티유저 구현** — 지금은 나 혼자 로컬. 다만 나중에 붙일 수 있는 구조는 유지한다(배포 가능성 유지 규칙).
- **반응형·모바일** — 관리 콘솔 밀도의 데스크톱 작업 도구. 좁은 화면 대응은 가치 대비 비용이 크다. 데스크톱 전용(min-width 1200).
- **기존 수·토 스케줄 대체** — 정기 발행 리듬은 그대로 두고, 대시보드는 온디맨드(월 1~2회, 상위 모델) 검수용으로 보완한다.
- **shadcn/ui·이모지 아이콘** — 디자인 시스템을 직접 소유해야 포트폴리오 가치가 있다. Base UI 위에 자체 토큰·컴포넌트, lucide 아이콘.

## 사용자 시나리오

1. 사용자가 큐에서 대기 맨 위 주제의 "실행"을 누른다 → 5단계 타임라인이 순서대로 채워지고 각 산출물이 `posts/<슬러그>/`에 생성된다.
2. 사용자가 벨로그 본문 단계의 diff를 보고 어투가 어색하다고 판단한다 → 하단 바에 수정 지시를 1회 입력한다 → 그 단계만 재실행되어 결과가 갱신된다.
3. 사용자가 전체를 승인한다 → Zenn에 下書き로 push되고, 실행 이력에 모델·토큰·비용·결과가 기록된다.

## 성공 지표 (정량)

- 주제 1건을 초안 5종 완성 + Zenn 下書き까지 올리는 데 드는 사람 손 조작이 **파일 수동 편집 0회**(대시보드 안에서만)로 끝난다.
- 실행 1건당 단계별 토큰·비용·소요 시간이 **100% 기록**되어 이력에서 조회된다.
- `주제_큐.md`와 DB 큐 상태 불일치 **0건**(양방향 동기화 검증 테스트 통과).

## 제약

- **기간**: Phase 1 = 2주(1-A 1주차 / 1-B 2주차), 디자인 시스템 병행.
- **스택**: pnpm 모노레포(apps/dashboard + @galley/ui + @galley/pipeline), Next.js App Router, Base UI 헤드리스, SQLite, TypeScript strict, Vitest, lucide. (SQLite 드라이버·DnD·실행 위치 등 갈리는 지점은 decisions/에서 확정.)
- **실행**: 로컬 우선. 배포는 보류하되 가능성은 유지("막지 않기").
- **예산**: 개인 프로젝트. 비용은 모델 호출 API 사용료뿐이며 실행마다 기록해 통제한다.

## 포트폴리오 관점

AI 초안 파이프라인을 큐·상태·승인 게이트로 지휘하는 프론트엔드 — "AI가 글을 쓴다"가 아니라 "사람이 검수하는 파이프라인을 설계·운영한다"를 증명한다.
