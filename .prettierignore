node_modules
dist
.next
out
coverage
pnpm-lock.yaml
next-env.d.ts

# 어투 프롬프트는 소스가 아니라 모델 입력 — 포맷터가 내용(=promptHash)을 바꾸지 않게(decisions/tone-prompts.md)
.galley/prompts

# 큐 픽스처는 파일 형식 그대로가 테스트 대상(줄 끝 탭·빈 줄 — 바이트 동일 라운드트립)
packages/pipeline/src/queue/__fixtures__/*.md
