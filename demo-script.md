# 데모 시나리오 — Galley

면접·발표용. 공개 발행은 시연에서도 하지 않는다(Zenn 下書き까지). 근거 수집 구조는 decisions/evidence-collection.md.

## 사전 준비 체크리스트

- [ ] `.env`: `DATABASE_URL`·`BLOG_DIR`·`ZENN_CONTENT_DIR`·`ANTHROPIC_API_KEY`(또는 Mock 어댑터, dev 전용)
- [ ] `.galley/redact.json`에 회사명·도메인 패턴 등록 확인
- [ ] 리포 인덱스 준비: `pnpm --filter @galley/pipeline index <리포 경로> --name spacehome --read-only` → `/settings/repos`(Phase 2) 또는 CLI 출력에서 `ready` 확인
- [ ] 워커 실행 중(`pnpm --filter @galley/pipeline worker`, TopBar 생존 점 초록)
- [ ] `zenn-content` 리포 연결 확인
- [ ] 샘플 주제가 큐 `대기` 맨 위에 있고 "근거 n건"(n ≥ 1) 표시. 힌트 괄호 예: `(spacehome, react-router)`
- [ ] 모델 선택: 기본 Claude Opus 5(Settings), 시연 중 바꾸지 않음
- [ ] 플랜 B 자료: 완료된 실행 1건의 스크린샷·녹화(타임라인 6줄·검증 결과·下書き 화면)

## 시연 순서

1. **큐** `/queue` — 대기 탭. 맨 위 주제 행의 보조 텍스트 "근거 3건" 확인 → ⋮ **근거 편집**: 연결된 분석 글 3개를 보여주고 1개 제거(글 범위 밖) → 닫기 → "근거 2건".
2. **실행 시작** — `맨 위 실행` → Dialog(주제명·모델 Select·근거 목록 2건) → `실행`. Run이 `queued` → 워커가 집어가 `running`.
3. **실행 상세** `/runs?id=` — 타임라인 6줄이 순서대로 채워진다.
   - 근거 수집 줄: "linked 2 · discovered 3" → 펼쳐 경로·커밋·조각 미리보기(회사명이 `[REDACTED]`로 치환된 것, 그리고 조각은 `DATA_DIR`에만 있고 blog 폴더의 `evidence.json`에는 포인터만 있음을 강조).
   - 벨로그 본문 줄 → 펼쳐 마크다운 미리보기.
   - **근거 검증 줄: 주황 배지 "근거 없음 1 · 불확실 0"** → 펼쳐 주장 목록. 예: "월 주문 1,000건" supported(커밋 링크), "스토어 약 50개" supported, "재시도 3회" **unsupported**(근거 없음).
4. **수정 지시** — ActionBar 단계 Select=**벨로그 본문**, 입력 "재시도 횟수는 근거에 없으니 그 문장을 빼거나 커밋에서 확인해" → `재실행`. 확인 Dialog가 **"다시 도는 단계: 본문 → 검증 → 링크드인 → Zenn → 발행정보"** 를 실행 전에 보여준다(재실행 = 시작 단계 + 이후 전부). 근거 수집은 범위 밖이라 **`carried`** — 연한 회색 체크 + "이전 결과 · {원래 실행 시각}". **누락이 아니라 이전 결과를 그대로 쓴다는 표시**임을 짚는다.
   - _변형 시연_: Select를 비우면 지시의 "근거"·"커밋" 때문에 시작 단계가 **근거 수집**이 되어 6단계 전부 다시 돈다 — **Select 지정이 텍스트 판단보다 우선**임을 보여주는 장면.
5. **재검증 통과** — 근거 검증 줄이 "근거 없음 0"으로 회색 전환. `diff`로 본문 변경 확인. 뒤이어 링크드인·Zenn·발행정보도 새 본문 기준으로 다시 돌아 `fresh`가 된다(발행정보 `## 근거` 목록도 같은 EvidenceBundle에서 다시 생성 — 본문과 근거가 어긋나지 않는 이유).
6. **승인** — 6단계 산출물이 다 준비된 `승인 대기`에서 `승인` → Dialog 확인 → 완료. 승인이 여는 것은 **Zenn 下書き push**(`published: false`)이지 산출물 생성이 아니다 — 발행 게이트(decisions/publish-gate·zenn-push). `posts/<슬러그>/` 파일(발행정보 `## 근거` 목록: 커밋 해시·경로·날짜, 썸네일)을 함께 확인.
7. **이력** — 실행 이력에 모델·토큰·비용(인덱싱 비용은 별도 표) 기록 확인. 홈 `/`의 "지금 할 일"이 비었음을 보여주고 마무리.

## 실패 대비 플랜 B

- **모델 API 실패·네트워크**: TopBar 칩에서 Mock 어댑터로 전환(dev 전용) → 고정 텍스트로 흐름만 시연. 실제 결과는 준비된 스크린샷.
- **실행 시간이 길다**: **6단계까지 돌려 `승인 대기`가 된** Run을 미리 만들어 두고 4번(수정 지시)부터 시작(승인·수정 지시는 승인 대기에서만 받는다). 근거 검증 unsupported 1건은 픽스처 초안으로 재현 가능.
- **워커가 죽음**: 생존 점 회색 → 워커 재기동 → `interrupted` Run이 완료 단계 다음부터 재개되는 장면을 오히려 보여준다.
- **인덱스가 stale**: 재인덱싱은 시연 전에 끝내 둔다. 시연 중이면 CLI 증분 재인덱싱은 건너뛰고 기존 인덱스로 진행.
